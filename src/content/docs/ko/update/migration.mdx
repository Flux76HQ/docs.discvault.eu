---
title: '업데이트 및 마이그레이션: DiscVault 26 마이그레이션'
description: '작업별 안내: 업데이트 및 마이그레이션: DiscVault 26 마이그레이션: `/migration/readiness` · `ready_for_confirmation` · `/data/discvault.db` · SQLite.'
pageId: 'update-migration'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · 웹" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## 목표 및 결과

이 페이지 전용 절차를 수행합니다: **업데이트 및 마이그레이션: DiscVault 26 마이그레이션**. 이 절차에서 다루는 항목: `/migration/readiness` · `ready_for_confirmation` · `/data/discvault.db` · SQLite.

## 적용 대상

이 페이지의 적용 범위: **업데이트 및 마이그레이션: DiscVault 26 마이그레이션** (Docker · 웹). 채널 및 데이터 경계: DiscVault · `ghcr.io/helmerznl/discvault:latest`; DiscVault 26; 읽기 전용 기존 데이터 원본이며 현재 런타임 데이터베이스가 아님 · SQLite → PostgreSQL; DiscVault · `ghcr.io/helmerznl/discvault:beta`; DiscVault 26; 읽기 전용 기존 데이터 원본이며 현재 런타임 데이터베이스가 아님 · SQLite → PostgreSQL. 이 페이지의 전용 확인 지점: `/migration/readiness` · `ready_for_confirmation` · `/data/discvault.db` · SQLite.

### DiscVault v26 안정판

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- DiscVault 26
- 읽기 전용 기존 데이터 원본이며 현재 런타임 데이터베이스가 아님 · SQLite → PostgreSQL

### DiscVault v26 베타

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- DiscVault 26
- 읽기 전용 기존 데이터 원본이며 현재 런타임 데이터베이스가 아님 · SQLite → PostgreSQL

## 사전 요구 사항

- 읽기 전용 기존 데이터 원본이며 현재 런타임 데이터베이스가 아님 · `/data/discvault.db`
- 동일한 쓰기 중지 시간의 PostgreSQL 덤프와 파일 아카이브
- 소유자 접근 권한

## 절차

1. **백업:** 읽기 전용 기존 데이터 원본이며 현재 런타임 데이터베이스가 아님 · 동일한 쓰기 중지 시간의 PostgreSQL 덤프와 파일 아카이브
2. **검사:** `/api/next/migration/readiness` · `ready_for_confirmation`
3. **실행:** `POST /api/next/migration/start`
4. **비교:** 준비 상태가 확인되고 마이그레이션 실패 레코드가 0개이며 개수와 미디어 샘플이 일치함

### 명령 및 설정

#### 두 채널

```bash
curl --fail http://localhost:6080/api/next/migration/readiness
curl --fail http://localhost:6080/api/next/migration/status
curl --fail -X POST http://localhost:6080/api/next/migration/start \
  -H 'Content-Type: application/json' -d '{}'
curl --fail http://localhost:6080/api/next/migration/status
```

## 예상 결과

다음 결과의 승인 조건: **업데이트 및 마이그레이션: DiscVault 26 마이그레이션** 아래 결과가 모두 충족된 경우에만: 준비 상태가 확인되고 마이그레이션 실패 레코드가 0개이며 개수와 미디어 샘플이 일치함. 이 페이지의 전용 확인 지점: `/migration/readiness` · `ready_for_confirmation` · `/data/discvault.db` · SQLite.

- 준비 상태가 확인되고 마이그레이션 실패 레코드가 0개이며 개수와 미디어 샘플이 일치함

## 안전 및 롤백

:::caution
안전한 복구 경계: **업데이트 및 마이그레이션: DiscVault 26 마이그레이션** 시작하거나 계속하기 전에 다음 작업이 필요합니다: DiscVault 쓰기 작업을 중지하고 일치하는 PostgreSQL 및 `/data` 백업을 복원하며 일부 데이터가 든 데이터베이스로 가져오기를 반복하지 않음. 이 페이지의 전용 확인 지점: `/migration/readiness` · `ready_for_confirmation` · `/data/discvault.db` · SQLite.

- DiscVault 쓰기 작업을 중지하고 일치하는 PostgreSQL 및 `/data` 백업을 복원하며 일부 데이터가 든 데이터베이스로 가져오기를 반복하지 않음
:::

## 다음 단계

[구성: 환경 변수](/ko/configure/environment/)

## 출처 및 상태

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- 출처 및 상태: `2026-07-14`
- DiscVault Docs: `0.1.0`
