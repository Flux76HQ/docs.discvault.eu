---
title: '설치: 설치 방법'
description: '작업별 안내: 설치: 설치 방법: `:latest` · `:beta` · `:legacy` · `DISCVAULT_IMAGE` · PostgreSQL `/data` · `/update/migration/`.'
pageId: 'install-index'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault', 'helmerzNL/DiscVault.EU']
lastVerified: '2026-07-20'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault · helmerzNL/DiscVault.EU" verified="2026-07-20" />

## 목표 및 결과

이 페이지 전용 절차를 수행합니다: **설치: 설치 방법**. 이 절차에서 다루는 항목: `:latest` · `:beta` · `:legacy` · `DISCVAULT_IMAGE` · PostgreSQL `/data` · `/update/migration/`.

## 적용 대상

이 페이지의 적용 범위: **설치: 설치 방법** (Docker · Unraid). 채널 및 데이터 경계: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `:beta`; PostgreSQL `17` `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `:beta`; PostgreSQL `17` `/data`. 이 페이지의 전용 확인 지점: `:latest` · `:beta` · `:legacy` · `DISCVAULT_IMAGE` · PostgreSQL `/data` · `/update/migration/`.

### DiscVault v26 안정판

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `:beta`
- PostgreSQL `17` `/data`

### DiscVault v26 베타

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `:beta`
- PostgreSQL `17` `/data`

## 사전 요구 사항

- Docker Engine `24+`
- Docker Compose `v2`

## 절차

1. **선택:** [설치: Docker Compose](/ko/install/docker-compose/) · DiscVault 26
2. **선택:** [설치: Docker run](/ko/install/docker-run/)
3. **선택:** [설치: Unraid](/ko/install/unraid/) · Unraid
4. **선택:** 새 설치는 DiscVault v26 `:latest` 또는 `:beta`를 사용하며, 고정된 `:legacy` 이미지는 아직 마이그레이션할 준비가 안 된 기존 이전 세대 설치에만 사용함
5. **보존:** 이전 세대의 볼륨, 환경 변수, 포트와 데이터 경로를 그대로 유지하고 v26 PostgreSQL Compose 토폴로지의 이미지를 `:legacy`로 바꾸지 않음
6. **열기:** [업데이트 및 마이그레이션: DiscVault 26 마이그레이션](/ko/update/migration/) · → DiscVault 26
7. **기록:** `DISCVAULT_IMAGE` · 기존 또는 legacy 배포에만 해당: 일치하는 백업 세트

### 작업 경로

- [설치: Docker Compose](/ko/install/docker-compose/) — DiscVault 26 `release/v26-beta` `DISCVAULT_NEXT_IMAGE`
- [설치: Docker run](/ko/install/docker-run/) — DiscVault 26 Docker run PostgreSQL `/data` `DATABASE_URL`
- [설치: 영구 데이터 및 PostgreSQL](/ko/install/storage-postgresql/) — DiscVault 26 PostgreSQL `/data`
- [설치: Unraid](/ko/install/unraid/) — DiscVault 26 Unraid PostgreSQL `/data`
- [업데이트 및 마이그레이션: DiscVault 26 마이그레이션](/ko/update/migration/) — → DiscVault 26

## 예상 결과

다음 결과의 승인 조건: **설치: 설치 방법** 아래 결과가 모두 충족된 경우에만: 선택한 릴리스 채널; PostgreSQL `/data`; 이전 세대의 볼륨, 환경 변수, 포트와 데이터 경로를 그대로 유지하고 v26 PostgreSQL Compose 토폴로지의 이미지를 `:legacy`로 바꾸지 않음. 이 페이지의 전용 확인 지점: `:latest` · `:beta` · `:legacy` · `DISCVAULT_IMAGE` · PostgreSQL `/data` · `/update/migration/`.

- 선택한 릴리스 채널
- PostgreSQL `/data`
- 이전 세대의 볼륨, 환경 변수, 포트와 데이터 경로를 그대로 유지하고 v26 PostgreSQL Compose 토폴로지의 이미지를 `:legacy`로 바꾸지 않음

## 안전 및 롤백

:::caution
안전한 복구 경계: **설치: 설치 방법** 시작하거나 계속하기 전에 다음 작업이 필요합니다: Legacy 분기만 해당: 백업 후 기존 구세대 배포의 이미지 태그만 `:legacy`로 바꾸고 토폴로지를 유지한 채 가져와 다시 만든 다음 컬렉션을 확인하고 중지함. 이 페이지에서 해당 기존 배포를 v26 토폴로지로 전환하지 않음; 고정된 `:legacy` 이미지에는 DiscVault v26 기능, 마이그레이션 개선 또는 향후 인터페이스 업데이트가 제공되지 않음; 기존 또는 legacy 배포에만 해당: 일치하는 백업 세트; 릴리스 정보와 기록된 불변 이미지 다이제스트. 이 페이지의 전용 확인 지점: `:latest` · `:beta` · `:legacy` · `DISCVAULT_IMAGE` · PostgreSQL `/data` · `/update/migration/`.

- Legacy 분기만 해당: 백업 후 기존 구세대 배포의 이미지 태그만 `:legacy`로 바꾸고 토폴로지를 유지한 채 가져와 다시 만든 다음 컬렉션을 확인하고 중지함. 이 페이지에서 해당 기존 배포를 v26 토폴로지로 전환하지 않음
- 고정된 `:legacy` 이미지에는 DiscVault v26 기능, 마이그레이션 개선 또는 향후 인터페이스 업데이트가 제공되지 않음
- 기존 또는 legacy 배포에만 해당: 일치하는 백업 세트
- 릴리스 정보와 기록된 불변 이미지 다이제스트
:::

## 다음 단계

[설치: Docker Compose](/ko/install/docker-compose/)

## 출처 및 상태

- [`helmerzNL/DiscVault@4352c060ccd6`](https://github.com/helmerzNL/DiscVault/commit/4352c060ccd6fd625a828f6e20c24f111c9ef743)
- [`helmerzNL/DiscVault.EU@583f85c55dc2`](https://github.com/helmerzNL/DiscVault.EU/commit/583f85c55dc22b63368f997a3b076a093ca0afa1)
- 출처 및 상태: `2026-07-20`
- DiscVault Docs: `0.1.6`
