---
title: '설치: 영구 데이터 및 PostgreSQL'
description: '작업별 안내: 설치: 영구 데이터 및 PostgreSQL: `DISCVAULT_POSTGRES_DATA` · `DISCVAULT_DATA_DIR` · `postgres` · `next-api`.'
pageId: 'install-storage-postgresql'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## 목표 및 결과

이 페이지 전용 절차를 수행합니다: **설치: 영구 데이터 및 PostgreSQL**. 이 절차에서 다루는 항목: `DISCVAULT_POSTGRES_DATA` · `DISCVAULT_DATA_DIR` · `postgres` · `next-api`.

## 적용 대상

이 페이지의 적용 범위: **설치: 영구 데이터 및 PostgreSQL** (Docker · Unraid). 채널 및 데이터 경계: DiscVault · `ghcr.io/helmerznl/discvault:latest`; PostgreSQL; `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; PostgreSQL; `/data`. 이 페이지의 전용 확인 지점: `DISCVAULT_POSTGRES_DATA` · `DISCVAULT_DATA_DIR` · `postgres` · `next-api`.

### DiscVault v26 안정판

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- PostgreSQL
- `/data`

### DiscVault v26 베타

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- PostgreSQL
- `/data`

## 사전 요구 사항

- `DISCVAULT_POSTGRES_DATA`
- 실시간 데이터 경로 외부의 백업 대상
- PostgreSQL `17`

## 절차

1. **검사:** `DISCVAULT_POSTGRES_DATA` · `/var/lib/postgresql/data`
2. **검사:** `DISCVAULT_DATA_DIR` · `/data`
3. **확인:** `postgres` · `healthy` · `next-api`
4. **기록:** 동일한 쓰기 중지 시간의 PostgreSQL 덤프와 파일 아카이브

### 명령 및 설정

#### 두 채널

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml ps
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml exec -T postgres sh -c 'pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"'
docker inspect discvault-next-api-1 --format '{{range .Mounts}}{{.Source}} -> {{.Destination}}{{println}}{{end}}'
```

## 예상 결과

다음 결과의 승인 조건: **설치: 영구 데이터 및 PostgreSQL** 아래 결과가 모두 충족된 경우에만: PostgreSQL · `healthy`; `/data`; 일치하는 백업 세트. 이 페이지의 전용 확인 지점: `DISCVAULT_POSTGRES_DATA` · `DISCVAULT_DATA_DIR` · `postgres` · `next-api`.

- PostgreSQL · `healthy`
- `/data`
- 일치하는 백업 세트

## 안전 및 롤백

:::caution
안전한 복구 경계: **설치: 영구 데이터 및 PostgreSQL** 시작하거나 계속하기 전에 다음 작업이 필요합니다: PostgreSQL과 `/data`는 별도 저장소이며 일치하는 하나의 백업 세트가 필요함; 실시간 데이터 경로 외부의 백업 대상. 이 페이지의 전용 확인 지점: `DISCVAULT_POSTGRES_DATA` · `DISCVAULT_DATA_DIR` · `postgres` · `next-api`.

- PostgreSQL과 `/data`는 별도 저장소이며 일치하는 하나의 백업 세트가 필요함
- 실시간 데이터 경로 외부의 백업 대상
:::

## 다음 단계

[설치: 리버스 프록시 및 패스키](/ko/install/reverse-proxy-passkeys/)

## 출처 및 상태

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- 출처 및 상태: `2026-07-14`
- DiscVault Docs: `0.1.0`
