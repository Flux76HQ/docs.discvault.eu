---
title: '설치: 리버스 프록시 및 패스키'
description: '작업별 안내: 설치: 리버스 프록시 및 패스키: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.'
pageId: 'install-reverse-proxy-passkeys'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · 웹" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## 목표 및 결과

이 페이지 전용 절차를 수행합니다: **설치: 리버스 프록시 및 패스키**. 이 절차에서 다루는 항목: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

## 적용 대상

이 페이지의 적용 범위: **설치: 리버스 프록시 및 패스키** (Docker · 웹). 채널 및 데이터 경계: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `RP_ID` `RP_ORIGINS`; `6080` API `5000`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `RP_ID` `RP_ORIGINS`; `6080` API `5000`. 이 페이지의 전용 확인 지점: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

### DiscVault v26 안정판

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `RP_ID` `RP_ORIGINS`
- `6080` API `5000`

### DiscVault v26 베타

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `RP_ID` `RP_ORIGINS`
- `6080` API `5000`

## 사전 요구 사항

- DNS
- TLS
- `6080`

## 절차

1. **구성:** `RP_ID`
2. **구성:** `RP_ORIGINS` · HTTPS
3. **구성:** `http://127.0.0.1:6080`
4. **테스트:** `GET /api/next/health`

### 명령 및 설정

#### 두 채널

```text
RP_ID=discvault.example.com
RP_ORIGINS=https://discvault.example.com
curl --fail https://discvault.example.com/api/next/health
```

## 예상 결과

다음 결과의 승인 조건: **설치: 리버스 프록시 및 패스키** 아래 결과가 모두 충족된 경우에만: HTTPS · HTTP `2xx`; `RP_ID`. 이 페이지의 전용 확인 지점: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

- HTTPS · HTTP `2xx`
- `RP_ID`

## 안전 및 롤백

:::caution
안전한 복구 경계: **설치: 리버스 프록시 및 패스키** 시작하거나 계속하기 전에 다음 작업이 필요합니다: 프록시와 RP 값을 함께 복원하고 소유자 복구 수단을 유지함. 이 페이지의 전용 확인 지점: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

- 프록시와 RP 값을 함께 복원하고 소유자 복구 수단을 유지함
:::

## 다음 단계

[설치: 첫 시작 및 상태 검사](/ko/install/first-start-health/)

## 출처 및 상태

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- 출처 및 상태: `2026-07-14`
- DiscVault Docs: `0.1.0`
