---
title: 'PWA: 오프라인 동작'
description: '작업별 안내: PWA: 오프라인 동작: `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.'
pageId: 'pwa-offline'
products: ['pwa']
platforms: ['web', 'ios', 'android']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="웹 · iOS · Android" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## 목표 및 결과

이 페이지 전용 절차를 수행합니다: **PWA: 오프라인 동작**. 이 절차에서 다루는 항목: `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.

## 적용 대상

이 페이지의 적용 범위: **PWA: 오프라인 동작** (웹 · iOS · Android). 채널 및 데이터 경계: DiscVault · `ghcr.io/helmerznl/discvault:latest`; 캐시된 화면은 열릴 수 있지만 최신 데이터와 쓰기에는 네트워크가 필요함 · `Service Worker` · `CacheStorage`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; 캐시된 화면은 열릴 수 있지만 최신 데이터와 쓰기에는 네트워크가 필요함 · `Service Worker` · `CacheStorage`. 이 페이지의 전용 확인 지점: `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.

### DiscVault v26 안정판

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- 캐시된 화면은 열릴 수 있지만 최신 데이터와 쓰기에는 네트워크가 필요함 · `Service Worker` · `CacheStorage`

### DiscVault v26 베타

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- 캐시된 화면은 열릴 수 있지만 최신 데이터와 쓰기에는 네트워크가 필요함 · `Service Worker` · `CacheStorage`

## 사전 요구 사항

- PWA
- `CacheStorage`

## 절차

1. **열기:** 라이브러리 → 검색
2. **테스트:** 비행기 모드 · `Service Worker`
3. **기록:** `navigator.onLine`
4. **다시 연결:** HTTPS



## 예상 결과

다음 결과의 승인 조건: **PWA: 오프라인 동작** 아래 결과가 모두 충족된 경우에만: 캐시된 화면은 열릴 수 있지만 최신 데이터와 쓰기에는 네트워크가 필요함; `Service Worker` · `navigator.onLine`. 이 페이지의 전용 확인 지점: `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.

- 캐시된 화면은 열릴 수 있지만 최신 데이터와 쓰기에는 네트워크가 필요함
- `Service Worker` · `navigator.onLine`

## 안전 및 롤백

:::caution
안전한 복구 경계: **PWA: 오프라인 동작** 시작하거나 계속하기 전에 다음 작업이 필요합니다: 캐시된 화면은 열릴 수 있지만 최신 데이터와 쓰기에는 네트워크가 필요함 · DiscVault. 이 페이지의 전용 확인 지점: `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.

- 캐시된 화면은 열릴 수 있지만 최신 데이터와 쓰기에는 네트워크가 필요함 · DiscVault
:::

## 다음 단계

[문제 해결](/ko/troubleshooting/)

## 출처 및 상태

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- 출처 및 상태: `2026-07-14`
- DiscVault Docs: `0.1.0`
