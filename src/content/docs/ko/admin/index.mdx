---
title: '관리: 로그, 작업 및 보안'
description: '작업별 안내: 관리: 로그, 작업 및 보안: `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.'
pageId: 'admin-index'
products: ['server']
platforms: ['docker', 'unraid', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid · 웹" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## 목표 및 결과

이 페이지 전용 절차를 수행합니다: **관리: 로그, 작업 및 보안**. 이 절차에서 다루는 항목: `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.

## 적용 대상

이 페이지의 적용 범위: **관리: 로그, 작업 및 보안** (Docker · Unraid · 웹). 채널 및 데이터 경계: DiscVault · `ghcr.io/helmerznl/discvault:latest`; API MCP PostgreSQL; DiscVault · `ghcr.io/helmerznl/discvault:beta`; API MCP PostgreSQL. 이 페이지의 전용 확인 지점: `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.

### DiscVault v26 안정판

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- API MCP PostgreSQL

### DiscVault v26 베타

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- API MCP PostgreSQL

## 사전 요구 사항

- 소유자 접근 권한
- 일치하는 백업 세트
- 개인 정보를 가린 타임스탬프 로그

## 절차

1. **검사:** `/api/next/health`
2. **검사:** 개인 정보를 가린 타임스탬프 로그
3. **확인:** `next-worker`
4. **보존:** `--tail=200`

### 명령 및 설정

#### 두 채널

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml logs --tail=200 next-api next-worker next-mcp postgres
curl --fail http://localhost:6080/api/next/jobs
curl --fail http://localhost:6080/api/next/health
```

## 예상 결과

다음 결과의 승인 조건: **관리: 로그, 작업 및 보안** 아래 결과가 모두 충족된 경우에만: 실패 계층 하나를 격리하고 타임스탬프, 상태, 이미지 다이제스트 및 비공개 처리한 오류를 기록함; 일치하는 백업 세트. 이 페이지의 전용 확인 지점: `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.

- 실패 계층 하나를 격리하고 타임스탬프, 상태, 이미지 다이제스트 및 비공개 처리한 오류를 기록함
- 일치하는 백업 세트

## 안전 및 롤백

:::caution
안전한 복구 경계: **관리: 로그, 작업 및 보안** 시작하거나 계속하기 전에 다음 작업이 필요합니다: 볼륨, `/data`, 브라우저 자격 증명 또는 네이티브 앱 저장소를 먼저 삭제하지 말고 비공개 처리한 로그를 보존하며 일치하는 백업만 복원함. 이 페이지의 전용 확인 지점: `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.

- 볼륨, `/data`, 브라우저 자격 증명 또는 네이티브 앱 저장소를 먼저 삭제하지 말고 비공개 처리한 로그를 보존하며 일치하는 백업만 복원함
:::

## 다음 단계

[문제 해결](/ko/troubleshooting/)

## 출처 및 상태

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- 출처 및 상태: `2026-07-14`
- DiscVault Docs: `0.1.0`
