---
title: '시작: 개요'
description: '작업별 안내: 시작: 개요: Docker Engine `24+` · Docker Compose `v2` · PostgreSQL `17` · HTTPS.'
pageId: 'start-requirements'
products: ['server', 'pwa', 'ios', 'android']
platforms: ['all']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="모든 플랫폼" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## 목표 및 결과

이 페이지 전용 절차를 수행합니다: **시작: 개요**. 이 절차에서 다루는 항목: Docker Engine `24+` · Docker Compose `v2` · PostgreSQL `17` · HTTPS.

## 적용 대상

이 페이지의 적용 범위: **시작: 개요** (모든 플랫폼). 채널 및 데이터 경계: DiscVault · `ghcr.io/helmerznl/discvault:latest`; Docker Compose `v2` · PostgreSQL `17` · `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; Docker Compose `v2` · PostgreSQL `17` · `/data`. 이 페이지의 전용 확인 지점: Docker Engine `24+` · Docker Compose `v2` · PostgreSQL `17` · HTTPS.

### DiscVault v26 안정판

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- Docker Compose `v2` · PostgreSQL `17` · `/data`

### DiscVault v26 베타

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- Docker Compose `v2` · PostgreSQL `17` · `/data`

## 사전 요구 사항

- Docker Engine `24+`
- 쓰기 가능한 영구 저장소와 백업 여유 공간
- DNS TLS

## 절차

1. **검사:** Docker Engine `≥24` · Docker Compose `v2`
2. **검사:** `≥2 GB` · 쓰기 가능한 영구 저장소와 백업 여유 공간
3. **구성:** DNS · TLS · HTTPS · `RP_ID` · `RP_ORIGINS`
4. **기록:** 선택한 릴리스 채널

### 명령 및 설정

#### 두 채널

```bash
docker version --format '{{.Server.Version}}'
docker compose version
curl --version
```

## 예상 결과

다음 결과의 승인 조건: **시작: 개요** 아래 결과가 모두 충족된 경우에만: Docker Engine `≥24`; Docker Compose `v2`; HTTPS DNS. 이 페이지의 전용 확인 지점: Docker Engine `24+` · Docker Compose `v2` · PostgreSQL `17` · HTTPS.

- Docker Engine `≥24`
- Docker Compose `v2`
- HTTPS DNS

## 안전 및 롤백

:::caution
안전한 복구 경계: **시작: 개요** 시작하거나 계속하기 전에 다음 작업이 필요합니다: 읽기 전용 검사는 데이터를 변경하지 않으며 누락된 전제 조건을 해결한 후 계속함. 이 페이지의 전용 확인 지점: Docker Engine `24+` · Docker Compose `v2` · PostgreSQL `17` · HTTPS.

- 읽기 전용 검사는 데이터를 변경하지 않으며 누락된 전제 조건을 해결한 후 계속함
:::

## 다음 단계

[설치: 설치 방법](/ko/install/)

## 출처 및 상태

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- 출처 및 상태: `2026-07-14`
- DiscVault Docs: `0.1.0`
