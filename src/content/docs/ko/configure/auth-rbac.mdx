---
title: '구성: 인증 및 RBAC'
description: '작업별 안내: 구성: 인증 및 RBAC: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h` · iOS/iPadOS `16+` · Android `9+`.'
pageId: 'configure-auth-rbac'
products: ['server']
platforms: ['web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault', 'helmerzNL/DiscVault.EU']
lastVerified: '2026-07-15'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="웹" version="DiscVault v26" source="helmerzNL/DiscVault · helmerzNL/DiscVault.EU" verified="2026-07-15" />

## 목표 및 결과

이 페이지 전용 절차를 수행합니다: **구성: 인증 및 RBAC**. 이 절차에서 다루는 항목: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h` · iOS/iPadOS `16+` · Android `9+`.

## 적용 대상

이 페이지의 적용 범위: **구성: 인증 및 RBAC** (웹). 채널 및 데이터 경계: DiscVault · `ghcr.io/helmerznl/discvault:latest`; WebAuthn; 패스키는 암호화 키 쌍으로, 개인 키는 기기나 신뢰할 수 있는 자격 증명 관리자에 남고 DiscVault는 공개 키만 저장함; 패스키는 피싱에 강하고 사이트별로 고유하며, 재사용 가능한 계정 암호 대신 기기 잠금 해제로 로그인함; 시간 제한 초대 코드는 등록을 시작하지만 재사용 가능한 계정 암호가 아니며 일반 로그인은 패스키를 사용함; DiscVault · `ghcr.io/helmerznl/discvault:beta`; WebAuthn; 패스키는 암호화 키 쌍으로, 개인 키는 기기나 신뢰할 수 있는 자격 증명 관리자에 남고 DiscVault는 공개 키만 저장함; 패스키는 피싱에 강하고 사이트별로 고유하며, 재사용 가능한 계정 암호 대신 기기 잠금 해제로 로그인함; 시간 제한 초대 코드는 등록을 시작하지만 재사용 가능한 계정 암호가 아니며 일반 로그인은 패스키를 사용함. 이 페이지의 전용 확인 지점: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h` · iOS/iPadOS `16+` · Android `9+`.

### DiscVault v26 안정판

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- WebAuthn
- 패스키는 암호화 키 쌍으로, 개인 키는 기기나 신뢰할 수 있는 자격 증명 관리자에 남고 DiscVault는 공개 키만 저장함
- 패스키는 피싱에 강하고 사이트별로 고유하며, 재사용 가능한 계정 암호 대신 기기 잠금 해제로 로그인함
- 시간 제한 초대 코드는 등록을 시작하지만 재사용 가능한 계정 암호가 아니며 일반 로그인은 패스키를 사용함

### DiscVault v26 베타

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- WebAuthn
- 패스키는 암호화 키 쌍으로, 개인 키는 기기나 신뢰할 수 있는 자격 증명 관리자에 남고 DiscVault는 공개 키만 저장함
- 패스키는 피싱에 강하고 사이트별로 고유하며, 재사용 가능한 계정 암호 대신 기기 잠금 해제로 로그인함
- 시간 제한 초대 코드는 등록을 시작하지만 재사용 가능한 계정 암호가 아니며 일반 로그인은 패스키를 사용함

## 사전 요구 사항

- 소유자 접근 권한
- [설치: 리버스 프록시 및 패스키](/ko/install/reverse-proxy-passkeys/) · HTTPS FQDN
- 두 번째 복구 방법

## 절차

1. **검사:** 패스키는 암호화 키 쌍으로, 개인 키는 기기나 신뢰할 수 있는 자격 증명 관리자에 남고 DiscVault는 공개 키만 저장함
2. **비교:** 패스키는 피싱에 강하고 사이트별로 고유하며, 재사용 가능한 계정 암호 대신 기기 잠금 해제로 로그인함 · 시간 제한 초대 코드는 등록을 시작하지만 재사용 가능한 계정 암호가 아니며 일반 로그인은 패스키를 사용함
3. **확인:** Windows에는 최신 패스키 지원 브라우저와 최소 PIN이 설정된 Windows Hello가 필요하며 기본 패스키 관리는 Windows 11 22H2 이상을 권장함
4. **확인:** macOS에는 Ventura 13 이상과 iCloud 키체인 또는 다른 패스키 지원 자격 증명 관리자가 필요함
5. **확인:** iPhone과 iPad에는 iOS 또는 iPadOS 16 이상, iCloud 키체인과 2단계 인증이 필요하고 Android에는 9 이상, 패스키 제공자와 화면 잠금이 필요함
6. **생성:** 휴대폰과 데스크톱 기기처럼 둘 이상의 패스키를 등록하고 기기 교체나 분실 전에 독립적인 소유자 복구 경로를 유지함
7. **열기:** 관리 → 보안 → 인증 활성화
8. **생성:** 관리 → 사용자 → 48시간 초대 생성
9. **구성:** 관리 → 역할 → 기본 / 고급
10. **테스트:** WebAuthn

### 명령 및 설정

#### 두 채널

```bash
curl --fail http://localhost:6080/api/next/auth/status
curl --fail http://localhost:6080/api/next/auth/rbac
```

## 예상 결과

다음 결과의 승인 조건: **구성: 인증 및 RBAC** 아래 결과가 모두 충족된 경우에만: 소유자 패스키 로그인이 작동하고 초대 전용 등록이 설정과 일치하며 테스트 사용자에게 할당된 권한만 있음; 패스키는 피싱에 강하고 사이트별로 고유하며, 재사용 가능한 계정 암호 대신 기기 잠금 해제로 로그인함; 시간 제한 초대 코드는 등록을 시작하지만 재사용 가능한 계정 암호가 아니며 일반 로그인은 패스키를 사용함. 이 페이지의 전용 확인 지점: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h` · iOS/iPadOS `16+` · Android `9+`.

- 소유자 패스키 로그인이 작동하고 초대 전용 등록이 설정과 일치하며 테스트 사용자에게 할당된 권한만 있음
- 패스키는 피싱에 강하고 사이트별로 고유하며, 재사용 가능한 계정 암호 대신 기기 잠금 해제로 로그인함
- 시간 제한 초대 코드는 등록을 시작하지만 재사용 가능한 계정 암호가 아니며 일반 로그인은 패스키를 사용함

## 안전 및 롤백

:::caution
안전한 복구 경계: **구성: 인증 및 RBAC** 시작하거나 계속하기 전에 다음 작업이 필요합니다: 휴대폰과 데스크톱 기기처럼 둘 이상의 패스키를 등록하고 기기 교체나 분실 전에 독립적인 소유자 복구 경로를 유지함; 소유자 세션과 두 번째 복구 수단을 유지하고 마지막 소유자 패스키를 제거하지 않음. 이 페이지의 전용 확인 지점: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h` · iOS/iPadOS `16+` · Android `9+`.

- 휴대폰과 데스크톱 기기처럼 둘 이상의 패스키를 등록하고 기기 교체나 분실 전에 독립적인 소유자 복구 경로를 유지함
- 소유자 세션과 두 번째 복구 수단을 유지하고 마지막 소유자 패스키를 제거하지 않음
:::

## 다음 단계

[구성: 플러그인 및 메타데이터](/ko/configure/plugins-metadata/)

## 출처 및 상태

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- [`helmerzNL/DiscVault.EU@583f85c55dc2`](https://github.com/helmerzNL/DiscVault.EU/commit/583f85c55dc22b63368f997a3b076a093ca0afa1)
- 출처 및 상태: `2026-07-15`
- DiscVault Docs: `0.1.4`
