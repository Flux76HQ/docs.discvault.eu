---
title: '구성: 인증 및 RBAC'
description: '작업별 안내: 구성: 인증 및 RBAC: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h`.'
pageId: 'configure-auth-rbac'
products: ['server']
platforms: ['web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="웹" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## 목표 및 결과

이 페이지 전용 절차를 수행합니다: **구성: 인증 및 RBAC**. 이 절차에서 다루는 항목: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h`.

## 적용 대상

이 페이지의 적용 범위: **구성: 인증 및 RBAC** (웹). 채널 및 데이터 경계: DiscVault · `ghcr.io/helmerznl/discvault:latest`; WebAuthn; DiscVault · `ghcr.io/helmerznl/discvault:beta`; WebAuthn. 이 페이지의 전용 확인 지점: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h`.

### DiscVault v26 안정판

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- WebAuthn

### DiscVault v26 베타

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- WebAuthn

## 사전 요구 사항

- 소유자 접근 권한
- HTTPS
- 두 번째 복구 방법

## 절차

1. **열기:** 관리 → 보안 → 인증 활성화
2. **생성:** 관리 → 사용자 → 48시간 초대 생성
3. **구성:** 관리 → 역할 → 기본 / 고급
4. **테스트:** `48 h`

### 명령 및 설정

#### 두 채널

```bash
curl --fail http://localhost:6080/api/next/auth/status
curl --fail http://localhost:6080/api/next/auth/rbac
```

## 예상 결과

다음 결과의 승인 조건: **구성: 인증 및 RBAC** 아래 결과가 모두 충족된 경우에만: 소유자 패스키 로그인이 작동하고 초대 전용 등록이 설정과 일치하며 테스트 사용자에게 할당된 권한만 있음. 이 페이지의 전용 확인 지점: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h`.

- 소유자 패스키 로그인이 작동하고 초대 전용 등록이 설정과 일치하며 테스트 사용자에게 할당된 권한만 있음

## 안전 및 롤백

:::caution
안전한 복구 경계: **구성: 인증 및 RBAC** 시작하거나 계속하기 전에 다음 작업이 필요합니다: 소유자 세션과 두 번째 복구 수단을 유지하고 마지막 소유자 패스키를 제거하지 않음. 이 페이지의 전용 확인 지점: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h`.

- 소유자 세션과 두 번째 복구 수단을 유지하고 마지막 소유자 패스키를 제거하지 않음
:::

## 다음 단계

[구성: 플러그인 및 메타데이터](/ko/configure/plugins-metadata/)

## 출처 및 상태

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- 출처 및 상태: `2026-07-14`
- DiscVault Docs: `0.1.0`
