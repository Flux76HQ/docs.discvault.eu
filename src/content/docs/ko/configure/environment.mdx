---
title: '구성: 환경 변수'
description: '작업별 안내: 구성: 환경 변수: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.'
pageId: 'configure-environment'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## 목표 및 결과

이 페이지 전용 절차를 수행합니다: **구성: 환경 변수**. 이 절차에서 다루는 항목: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

## 적용 대상

이 페이지의 적용 범위: **구성: 환경 변수** (Docker · Unraid). 채널 및 데이터 경계: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `.env`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `.env`. 이 페이지의 전용 확인 지점: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

### DiscVault v26 안정판

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `.env`

### DiscVault v26 베타

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `.env`

## 사전 요구 사항

- 비공개 DiscVault 런타임 파일
- OpenSSL
- 계획된 재시작 시간

## 절차

1. **생성:** `/opt/discvault/.env` · 소유자 전용 파일 권한(0600)
2. **구성:** `DISCVAULT_IMAGE` · `RP_ORIGINS`
3. **확인:** `chmod 0600` · `docker compose config --quiet`
4. **시작:** 일반 설정 변경 중에는 JWT 및 PostgreSQL 비밀을 바꾸지 않음

### 명령 및 설정

#### DiscVault v26 안정판

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:latest
```

#### DiscVault v26 베타

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:beta
```

#### 두 채널

```bash
set -euo pipefail
test -n "$DISCVAULT_IMAGE"
case "$DISCVAULT_IMAGE" in
  ghcr.io/helmerznl/discvault:latest|ghcr.io/helmerznl/discvault:beta) ;;
  *) printf '%s\n' "$DISCVAULT_IMAGE" >&2; exit 64 ;;
esac
install -d -m 0700 /opt/discvault
install -d -m 0750 /srv/discvault
install -d -o 70 -g 70 -m 0700 /srv/discvault-postgres
cd /opt/discvault
test -f compose.yaml
umask 077
ENV_FILE=/opt/discvault/.env
test ! -e "$ENV_FILE"
ENV_TMP="$(mktemp "$ENV_FILE.tmp.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
POSTGRES_PASSWORD="$(openssl rand -hex 32)"
JWT_SECRET="$(openssl rand -hex 32)"
printf '%s\n' \
  "DISCVAULT_IMAGE=$DISCVAULT_IMAGE" \
  'DISCVAULT_DATA_DIR=/srv/discvault' \
  'DISCVAULT_POSTGRES_DATA=/srv/discvault-postgres' \
  'DISCVAULT_API_PORT=6080' \
  'DISCVAULT_MCP_PORT=6090' \
  'DISCVAULT_NETWORK_NAME=discvault' \
  'DISCVAULT_API_WORKERS=2' \
  'DISCVAULT_API_TIMEOUT=180' \
  'DISCVAULT_WORKER_ID=worker-1' \
  'DISCVAULT_WORKER_POLL_INTERVAL=2' \
  'POSTGRES_DB=discvault' \
  'POSTGRES_USER=discvault' \
  "POSTGRES_PASSWORD=$POSTGRES_PASSWORD" \
  "JWT_SECRET=$JWT_SECRET" \
  'TZ=Europe/Amsterdam' \
  'RP_ID=localhost' \
  'RP_NAME=DiscVault' \
  'RP_ORIGINS=http://localhost:6080' > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
ln "$ENV_TMP" "$ENV_FILE"
rm -f "$ENV_TMP"
trap - EXIT
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml config --quiet
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml pull
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml up -d
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml ps
curl --fail http://localhost:6080/api/next/health
```

## 예상 결과

다음 결과의 승인 조건: **구성: 환경 변수** 아래 결과가 모두 충족된 경우에만: 소유자 전용 파일 권한(0600); 비공개 파일이 모드 600이고 Compose 검증이 성공하며 선택한 채널이 정상 상태가 됨. 이 페이지의 전용 확인 지점: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

- 소유자 전용 파일 권한(0600)
- 비공개 파일이 모드 600이고 Compose 검증이 성공하며 선택한 채널이 정상 상태가 됨

## 안전 및 롤백

:::caution
안전한 복구 경계: **구성: 환경 변수** 시작하거나 계속하기 전에 다음 작업이 필요합니다: 문서 저장소의 환경 파일은 DiscVault 런타임 파일이 아님 · `.env.example`; 일반 설정 변경 중에는 JWT 및 PostgreSQL 비밀을 바꾸지 않음 · `JWT_SECRET` · `POSTGRES_PASSWORD`; 실패하면 서비스를 중지한 채 이전 비공개 환경 파일을 복원하고 검증한 다음 비밀을 교체하지 않고 동일한 이미지를 다시 생성함. 이 페이지의 전용 확인 지점: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

- 문서 저장소의 환경 파일은 DiscVault 런타임 파일이 아님 · `.env.example`
- 일반 설정 변경 중에는 JWT 및 PostgreSQL 비밀을 바꾸지 않음 · `JWT_SECRET` · `POSTGRES_PASSWORD`
- 실패하면 서비스를 중지한 채 이전 비공개 환경 파일을 복원하고 검증한 다음 비밀을 교체하지 않고 동일한 이미지를 다시 생성함
:::

## 다음 단계

[구성: 인증 및 RBAC](/ko/configure/auth-rbac/)

## 출처 및 상태

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- 출처 및 상태: `2026-07-14`
- DiscVault Docs: `0.1.0`
