---
title: 'Installation: Omvendt proxy og adgangsnøgler'
description: 'Opgavespecifik vejledning til Installation: Omvendt proxy og adgangsnøgler: FQDN · `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.'
pageId: 'install-reverse-proxy-passkeys'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault', 'helmerzNL/DiscVault.EU']
lastVerified: '2026-07-15'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · web" version="DiscVault v26" source="helmerzNL/DiscVault · helmerzNL/DiscVault.EU" verified="2026-07-15" />

## Mål og resultat

Gennemfør den sidespecifikke arbejdsgang for **Installation: Omvendt proxy og adgangsnøgler**. Denne fremgangsmåde arbejder med FQDN · `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

## Gælder for

Denne side gælder kun for **Installation: Omvendt proxy og adgangsnøgler** (Docker · web). Kanal- og datagrænserne er: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `RP_ID` `RP_ORIGINS`; `6080` API `5000`; uden for localhost-undtagelsen kræver passkeys et stabilt fuldt kvalificeret domænenavn over betroet HTTPS; en ren IP-adresse eller almindelig HTTP understøttes ikke; `RP_ID` er værtsnavnet uden skema eller port, mens `RP_ORIGINS` er den nøjagtige fulde HTTPS-origin; begge skal svare til adressen i browseren; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `RP_ID` `RP_ORIGINS`; `6080` API `5000`; uden for localhost-undtagelsen kræver passkeys et stabilt fuldt kvalificeret domænenavn over betroet HTTPS; en ren IP-adresse eller almindelig HTTP understøttes ikke; `RP_ID` er værtsnavnet uden skema eller port, mens `RP_ORIGINS` er den nøjagtige fulde HTTPS-origin; begge skal svare til adressen i browseren. Sidespecifikke kontrolpunkter: FQDN · `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `RP_ID` `RP_ORIGINS`
- `6080` API `5000`
- uden for localhost-undtagelsen kræver passkeys et stabilt fuldt kvalificeret domænenavn over betroet HTTPS; en ren IP-adresse eller almindelig HTTP understøttes ikke
- `RP_ID` er værtsnavnet uden skema eller port, mens `RP_ORIGINS` er den nøjagtige fulde HTTPS-origin; begge skal svare til adressen i browseren

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `RP_ID` `RP_ORIGINS`
- `6080` API `5000`
- uden for localhost-undtagelsen kræver passkeys et stabilt fuldt kvalificeret domænenavn over betroet HTTPS; en ren IP-adresse eller almindelig HTTP understøttes ikke
- `RP_ID` er værtsnavnet uden skema eller port, mens `RP_ORIGINS` er den nøjagtige fulde HTTPS-origin; begge skal svare til adressen i browseren

## Forudsætninger

- FQDN
- TLS
- `RP_ID`

## Fremgangsmåde

1. **Vælg:** FQDN · uden for localhost-undtagelsen kræver passkeys et stabilt fuldt kvalificeret domænenavn over betroet HTTPS; en ren IP-adresse eller almindelig HTTP understøttes ikke
2. **Konfigurér:** `RP_ID`
3. **Konfigurér:** `RP_ORIGINS` · HTTPS
4. **Konfigurér:** `http://127.0.0.1:6080`
5. **Test:** `GET /api/next/health`
6. **Registrér:** vælg værtsnavnet før registrering af passkeys; en senere ændring kræver ny registrering, mens localhost kun er en lokal opsætningsundtagelse

### Kommandoer og indstillinger

#### Begge kanaler

```text
RP_ID=discvault.example.com
RP_ORIGINS=https://discvault.example.com
curl --fail https://discvault.example.com/api/next/health
```

## Forventet resultat

Godkend resultatet for **Installation: Omvendt proxy og adgangsnøgler** kun når alle resultater nedenfor er opfyldt: HTTPS · HTTP `2xx`; `RP_ID`; `RP_ID` er værtsnavnet uden skema eller port, mens `RP_ORIGINS` er den nøjagtige fulde HTTPS-origin; begge skal svare til adressen i browseren. Sidespecifikke kontrolpunkter: FQDN · `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

- HTTPS · HTTP `2xx`
- `RP_ID`
- `RP_ID` er værtsnavnet uden skema eller port, mens `RP_ORIGINS` er den nøjagtige fulde HTTPS-origin; begge skal svare til adressen i browseren

## Sikkerhed og tilbagerulning

:::caution
Den sikre gendannelsesgrænse for **Installation: Omvendt proxy og adgangsnøgler** kræver disse handlinger, før noget startes eller fortsættes: vælg værtsnavnet før registrering af passkeys; en senere ændring kræver ny registrering, mens localhost kun er en lokal opsætningsundtagelse; gendan proxy- og RP-værdier sammen, og behold en gendannelsesmetode for ejeren. Sidespecifikke kontrolpunkter: FQDN · `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

- vælg værtsnavnet før registrering af passkeys; en senere ændring kræver ny registrering, mens localhost kun er en lokal opsætningsundtagelse
- gendan proxy- og RP-værdier sammen, og behold en gendannelsesmetode for ejeren
:::

## Næste trin

[Installation: Første start og sundhedskontrol](/da/install/first-start-health/)

## Kilde og status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- [`helmerzNL/DiscVault.EU@583f85c55dc2`](https://github.com/helmerzNL/DiscVault.EU/commit/583f85c55dc22b63368f997a3b076a093ca0afa1)
- Kilde og status: `2026-07-15`
- DiscVault Docs: `0.1.4`
