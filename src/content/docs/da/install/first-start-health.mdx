---
title: 'Installation: Første start og sundhedskontrol'
description: 'Opgavespecifik vejledning til Installation: Første start og sundhedskontrol: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.'
pageId: 'install-first-start-health'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · web" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Mål og resultat

Gennemfør den sidespecifikke arbejdsgang for **Installation: Første start og sundhedskontrol**. Denne fremgangsmåde arbejder med `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

## Gælder for

Denne side gælder kun for **Installation: Første start og sundhedskontrol** (Docker · web). Kanal- og datagrænserne er: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `/api/next/auth/status`; `6080` → API `5000`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `/api/next/auth/status`; `6080` → API `5000`. Sidespecifikke kontrolpunkter: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `/api/next/auth/status`
- `6080` → API `5000`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `/api/next/auth/status`
- `6080` → API `5000`

## Forudsætninger

- `/api/next/health`
- `/api/next/auth/status`

## Fremgangsmåde

1. **Kontrollér:** `postgres`
2. **Bekræft:** `GET /api/next/health` · HTTP `2xx`
3. **Åbn:** `6080:5000`
4. **Bekræft:** `GET /api/next/auth/status` · WebAuthn

### Kommandoer og indstillinger

#### Begge kanaler

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml ps
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml logs --tail=100 next-api next-worker next-mcp postgres
curl --fail http://localhost:6080/api/next/health
curl --fail http://localhost:6080/api/next/auth/status
```

## Forventet resultat

Godkend resultatet for **Installation: Første start og sundhedskontrol** kun når alle resultater nedenfor er opfyldt: sundhed lykkes, den konfigurerede origin åbner, og den første konto er ejer. Sidespecifikke kontrolpunkter: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

- sundhed lykkes, den konfigurerede origin åbner, og den første konto er ejer

## Sikkerhed og tilbagerulning

:::caution
Den sikre gendannelsesgrænse for **Installation: Første start og sundhedskontrol** kræver disse handlinger, før noget startes eller fortsættes: tidsstemplede logfiler med private data fjernet; tilsvarende backupsæt. Sidespecifikke kontrolpunkter: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

- tidsstemplede logfiler med private data fjernet
- tilsvarende backupsæt
:::

## Næste trin

[Opdatering og migrering: Sikkerhedskopi](/da/update/backup/)

## Kilde og status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Kilde og status: `2026-07-14`
- DiscVault Docs: `0.1.0`
