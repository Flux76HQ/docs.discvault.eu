---
title: 'Konfiguration: Miljøvariabler'
description: 'Opgavespecifik vejledning til Konfiguration: Miljøvariabler: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.'
pageId: 'configure-environment'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Mål og resultat

Gennemfør den sidespecifikke arbejdsgang for **Konfiguration: Miljøvariabler**. Denne fremgangsmåde arbejder med `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

## Gælder for

Denne side gælder kun for **Konfiguration: Miljøvariabler** (Docker · Unraid). Kanal- og datagrænserne er: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `.env`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `.env`. Sidespecifikke kontrolpunkter: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `.env`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `.env`

## Forudsætninger

- privat DiscVault-runtimefil
- OpenSSL
- planlagt genstartsvindue

## Fremgangsmåde

1. **Opret:** `/opt/discvault/.env` · filtilladelse kun for ejer (0600)
2. **Konfigurér:** `DISCVAULT_IMAGE` · `RP_ORIGINS`
3. **Bekræft:** `chmod 0600` · `docker compose config --quiet`
4. **Start:** bevar JWT- og PostgreSQL-hemmeligheder ved almindelige konfigurationsændringer

### Kommandoer og indstillinger

#### DiscVault v26 – stabil

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:latest
```

#### DiscVault v26 – beta

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:beta
```

#### Begge kanaler

```bash
set -euo pipefail
test -n "$DISCVAULT_IMAGE"
case "$DISCVAULT_IMAGE" in
  ghcr.io/helmerznl/discvault:latest|ghcr.io/helmerznl/discvault:beta) ;;
  *) printf '%s\n' "$DISCVAULT_IMAGE" >&2; exit 64 ;;
esac
install -d -m 0700 /opt/discvault
install -d -m 0750 /srv/discvault
install -d -o 70 -g 70 -m 0700 /srv/discvault-postgres
cd /opt/discvault
test -f compose.yaml
umask 077
ENV_FILE=/opt/discvault/.env
test ! -e "$ENV_FILE"
ENV_TMP="$(mktemp "$ENV_FILE.tmp.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
POSTGRES_PASSWORD="$(openssl rand -hex 32)"
JWT_SECRET="$(openssl rand -hex 32)"
printf '%s\n' \
  "DISCVAULT_IMAGE=$DISCVAULT_IMAGE" \
  'DISCVAULT_DATA_DIR=/srv/discvault' \
  'DISCVAULT_POSTGRES_DATA=/srv/discvault-postgres' \
  'DISCVAULT_API_PORT=6080' \
  'DISCVAULT_MCP_PORT=6090' \
  'DISCVAULT_NETWORK_NAME=discvault' \
  'DISCVAULT_API_WORKERS=2' \
  'DISCVAULT_API_TIMEOUT=180' \
  'DISCVAULT_WORKER_ID=worker-1' \
  'DISCVAULT_WORKER_POLL_INTERVAL=2' \
  'POSTGRES_DB=discvault' \
  'POSTGRES_USER=discvault' \
  "POSTGRES_PASSWORD=$POSTGRES_PASSWORD" \
  "JWT_SECRET=$JWT_SECRET" \
  'TZ=Europe/Amsterdam' \
  'RP_ID=localhost' \
  'RP_NAME=DiscVault' \
  'RP_ORIGINS=http://localhost:6080' > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
ln "$ENV_TMP" "$ENV_FILE"
rm -f "$ENV_TMP"
trap - EXIT
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml config --quiet
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml pull
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml up -d
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml ps
curl --fail http://localhost:6080/api/next/health
```

## Forventet resultat

Godkend resultatet for **Konfiguration: Miljøvariabler** kun når alle resultater nedenfor er opfyldt: filtilladelse kun for ejer (0600); den private fil har tilstand 600, Compose-valideringen lykkes, og den valgte kanal bliver sund. Sidespecifikke kontrolpunkter: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

- filtilladelse kun for ejer (0600)
- den private fil har tilstand 600, Compose-valideringen lykkes, og den valgte kanal bliver sund

## Sikkerhed og tilbagerulning

:::caution
Den sikre gendannelsesgrænse for **Konfiguration: Miljøvariabler** kræver disse handlinger, før noget startes eller fortsættes: dokumentationslagerets miljøfil er aldrig en DiscVault-runtimefil · `.env.example`; bevar JWT- og PostgreSQL-hemmeligheder ved almindelige konfigurationsændringer · `JWT_SECRET` · `POSTGRES_PASSWORD`; ved fejl skal tjenesterne forblive stoppet; gendan og validér den tidligere private miljøfil, og genskab derefter samme image uden at rotere hemmeligheder. Sidespecifikke kontrolpunkter: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

- dokumentationslagerets miljøfil er aldrig en DiscVault-runtimefil · `.env.example`
- bevar JWT- og PostgreSQL-hemmeligheder ved almindelige konfigurationsændringer · `JWT_SECRET` · `POSTGRES_PASSWORD`
- ved fejl skal tjenesterne forblive stoppet; gendan og validér den tidligere private miljøfil, og genskab derefter samme image uden at rotere hemmeligheder
:::

## Næste trin

[Konfiguration: Godkendelse og RBAC](/da/configure/auth-rbac/)

## Kilde og status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Kilde og status: `2026-07-14`
- DiscVault Docs: `0.1.0`
