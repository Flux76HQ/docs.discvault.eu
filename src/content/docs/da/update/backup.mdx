---
title: 'Opdatering og migrering: Sikkerhedskopi'
description: 'Opgavespecifik vejledning til Opdatering og migrering: Sikkerhedskopi: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.'
pageId: 'update-backup'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Mål og resultat

Gennemfør den sidespecifikke arbejdsgang for **Opdatering og migrering: Sikkerhedskopi**. Denne fremgangsmåde arbejder med `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

## Gælder for

Denne side gælder kun for **Opdatering og migrering: Sikkerhedskopi** (Docker · Unraid). Kanal- og datagrænserne er: DiscVault · `ghcr.io/helmerznl/discvault:latest`; PostgreSQL-dump og filarkiv fra samme vindue med stoppede skrivere; PostgreSQL `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; PostgreSQL-dump og filarkiv fra samme vindue med stoppede skrivere; PostgreSQL `/data`. Sidespecifikke kontrolpunkter: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- PostgreSQL-dump og filarkiv fra samme vindue med stoppede skrivere
- PostgreSQL `/data`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- PostgreSQL-dump og filarkiv fra samme vindue med stoppede skrivere
- PostgreSQL `/data`

## Forudsætninger

- backupmål uden for den aktive datasti
- skrivbart vedvarende lager og ledig backupplads

## Fremgangsmåde

1. **Registrér:** `PREVIOUS_IMAGE` · `sha256`
2. **Stop:** `next-api` `next-worker` `next-mcp`
3. **Sikkerhedskopiér:** `pg_dump -Fc`
4. **Start:** `next-api` → → MCP

### Kommandoer og indstillinger

#### Begge kanaler

```bash
set -euo pipefail
cd /opt/discvault
install -d -m 0700 /srv/backups
BACKUP_ID="$(date -u +%Y%m%dT%H%M%SZ)"
API_ID="$(docker compose --env-file .env -p discvault -f compose.yaml ps -q next-api)"
IMAGE_ID="$(docker inspect "$API_ID" --format '{{.Image}}')"
PREVIOUS_IMAGE="$(docker image inspect "$IMAGE_ID" --format '{{index .RepoDigests 0}}')"
test -n "$PREVIOUS_IMAGE"
printf 'BACKUP_ID=%s\nPREVIOUS_IMAGE=%s\n' "$BACKUP_ID" "$PREVIOUS_IMAGE" \
  > "/srv/backups/discvault-$BACKUP_ID.env"
chmod 0600 "/srv/backups/discvault-$BACKUP_ID.env"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_dump -Fc -U "$POSTGRES_USER" "$POSTGRES_DB"' \
  > "/srv/backups/discvault-$BACKUP_ID.dump"
tar --acls --xattrs -C /srv -czf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz" discvault
docker compose --env-file .env -p discvault -f compose.yaml start next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml start next-worker next-mcp
test -s "/srv/backups/discvault-$BACKUP_ID.dump"
gzip -t "/srv/backups/discvault-data-$BACKUP_ID.tgz"
```

## Forventet resultat

Godkend resultatet for **Opdatering og migrering: Sikkerhedskopi** kun når alle resultater nedenfor er opfyldt: arkiv og dump er ikke tomme, kan læses i en gendannelsesprøve og er knyttet til et uforanderligt image-digest. Sidespecifikke kontrolpunkter: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- arkiv og dump er ikke tomme, kan læses i en gendannelsesprøve og er knyttet til et uforanderligt image-digest

## Sikkerhed og tilbagerulning

:::caution
Den sikre gendannelsesgrænse for **Opdatering og migrering: Sikkerhedskopi** kræver disse handlinger, før noget startes eller fortsættes: hvis en backupkommando fejler, start kun de uændrede tjenester igen og annuller opdateringen; PostgreSQL-dump og filarkiv fra samme vindue med stoppede skrivere. Sidespecifikke kontrolpunkter: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- hvis en backupkommando fejler, start kun de uændrede tjenester igen og annuller opdateringen
- PostgreSQL-dump og filarkiv fra samme vindue med stoppede skrivere
:::

## Næste trin

[Opdatering og migrering: Gendannelse](/da/update/restore/)

## Kilde og status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Kilde og status: `2026-07-14`
- DiscVault Docs: `0.1.0`
