---
title: 'Mise à jour et migration: Sauvegarde'
description: 'Instructions propres à la tâche : Mise à jour et migration: Sauvegarde: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.'
pageId: 'update-backup'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Objectif et résultat

Suivez le déroulement propre à cette page pour **Mise à jour et migration: Sauvegarde**. Cette procédure utilise `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

## S’applique à

Cette page s’applique uniquement à **Mise à jour et migration: Sauvegarde** (Docker · Unraid). Les limites de canal et de données sont : DiscVault · `ghcr.io/helmerznl/discvault:latest`; dump PostgreSQL et archive des fichiers de la même fenêtre sans écriture; PostgreSQL `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; dump PostgreSQL et archive des fichiers de la même fenêtre sans écriture; PostgreSQL `/data`. Points de contrôle propres à la page : `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

### DiscVault v26 stable

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- dump PostgreSQL et archive des fichiers de la même fenêtre sans écriture
- PostgreSQL `/data`

### DiscVault v26 bêta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- dump PostgreSQL et archive des fichiers de la même fenêtre sans écriture
- PostgreSQL `/data`

## Prérequis

- destination hors du chemin de données actif
- stockage persistant inscriptible et espace de sauvegarde libre

## Procédure

1. **Consigner:** `PREVIOUS_IMAGE` · `sha256`
2. **Arrêter:** `next-api` `next-worker` `next-mcp`
3. **Sauvegarder:** `pg_dump -Fc`
4. **Démarrer:** `next-api` → → MCP

### Commandes et réglages

#### Les deux canaux

```bash
set -euo pipefail
cd /opt/discvault
install -d -m 0700 /srv/backups
BACKUP_ID="$(date -u +%Y%m%dT%H%M%SZ)"
API_ID="$(docker compose --env-file .env -p discvault -f compose.yaml ps -q next-api)"
IMAGE_ID="$(docker inspect "$API_ID" --format '{{.Image}}')"
PREVIOUS_IMAGE="$(docker image inspect "$IMAGE_ID" --format '{{index .RepoDigests 0}}')"
test -n "$PREVIOUS_IMAGE"
printf 'BACKUP_ID=%s\nPREVIOUS_IMAGE=%s\n' "$BACKUP_ID" "$PREVIOUS_IMAGE" \
  > "/srv/backups/discvault-$BACKUP_ID.env"
chmod 0600 "/srv/backups/discvault-$BACKUP_ID.env"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_dump -Fc -U "$POSTGRES_USER" "$POSTGRES_DB"' \
  > "/srv/backups/discvault-$BACKUP_ID.dump"
tar --acls --xattrs -C /srv -czf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz" discvault
docker compose --env-file .env -p discvault -f compose.yaml start next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml start next-worker next-mcp
test -s "/srv/backups/discvault-$BACKUP_ID.dump"
gzip -t "/srv/backups/discvault-data-$BACKUP_ID.tgz"
```

## Résultat attendu

Validez le résultat de **Mise à jour et migration: Sauvegarde** uniquement lorsque tous les résultats suivants sont confirmés : l’archive et le dump ne sont pas vides, sont lisibles lors d’un essai et liés à un digest d’image immuable. Points de contrôle propres à la page : `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- l’archive et le dump ne sont pas vides, sont lisibles lors d’un essai et liés à un digest d’image immuable

## Sécurité et retour arrière

:::caution
La limite de récupération sûre pour **Mise à jour et migration: Sauvegarde** impose ces actions avant tout démarrage ou toute poursuite : si une commande de sauvegarde échoue, redémarrez uniquement les services inchangés et annulez la mise à jour; dump PostgreSQL et archive des fichiers de la même fenêtre sans écriture. Points de contrôle propres à la page : `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- si une commande de sauvegarde échoue, redémarrez uniquement les services inchangés et annulez la mise à jour
- dump PostgreSQL et archive des fichiers de la même fenêtre sans écriture
:::

## Étape suivante

[Mise à jour et migration: Restauration](/fr/update/restore/)

## Source et état

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Source et état: `2026-07-14`
- DiscVault Docs: `0.1.0`
