---
title: 'Встановлення: Зворотний проксі та ключі доступу'
description: 'Спеціальні інструкції для завдання: Встановлення: Зворотний проксі та ключі доступу: FQDN · `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.'
pageId: 'install-reverse-proxy-passkeys'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault', 'helmerzNL/DiscVault.EU']
lastVerified: '2026-07-15'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · веб" version="DiscVault v26" source="helmerzNL/DiscVault · helmerzNL/DiscVault.EU" verified="2026-07-15" />

## Мета й результат

Виконайте спеціальну процедуру сторінки для **Встановлення: Зворотний проксі та ключі доступу**. У цій процедурі використовуються FQDN · `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

## Застосовується до

Ця сторінка стосується лише **Встановлення: Зворотний проксі та ключі доступу** (Docker · веб). Межі каналу й даних: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `RP_ID` `RP_ORIGINS`; `6080` API `5000`; поза винятком localhost passkeys потребують стабільного повного доменного імені через довірений HTTPS; окрема IP-адреса чи звичайний HTTP не підтримуються; `RP_ID` — це ім’я хоста без схеми й порту, а `RP_ORIGINS` — точний повний HTTPS origin; обидва мають відповідати адресі, відкритій у браузері; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `RP_ID` `RP_ORIGINS`; `6080` API `5000`; поза винятком localhost passkeys потребують стабільного повного доменного імені через довірений HTTPS; окрема IP-адреса чи звичайний HTTP не підтримуються; `RP_ID` — це ім’я хоста без схеми й порту, а `RP_ORIGINS` — точний повний HTTPS origin; обидва мають відповідати адресі, відкритій у браузері. Контрольні точки цієї сторінки: FQDN · `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

### DiscVault v26 – стабільний

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `RP_ID` `RP_ORIGINS`
- `6080` API `5000`
- поза винятком localhost passkeys потребують стабільного повного доменного імені через довірений HTTPS; окрема IP-адреса чи звичайний HTTP не підтримуються
- `RP_ID` — це ім’я хоста без схеми й порту, а `RP_ORIGINS` — точний повний HTTPS origin; обидва мають відповідати адресі, відкритій у браузері

### DiscVault v26 – бета

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `RP_ID` `RP_ORIGINS`
- `6080` API `5000`
- поза винятком localhost passkeys потребують стабільного повного доменного імені через довірений HTTPS; окрема IP-адреса чи звичайний HTTP не підтримуються
- `RP_ID` — це ім’я хоста без схеми й порту, а `RP_ORIGINS` — точний повний HTTPS origin; обидва мають відповідати адресі, відкритій у браузері

## Передумови

- FQDN
- TLS
- `RP_ID`

## Процедура

1. **Виберіть:** FQDN · поза винятком localhost passkeys потребують стабільного повного доменного імені через довірений HTTPS; окрема IP-адреса чи звичайний HTTP не підтримуються
2. **Налаштуйте:** `RP_ID`
3. **Налаштуйте:** `RP_ORIGINS` · HTTPS
4. **Налаштуйте:** `http://127.0.0.1:6080`
5. **Перевірте:** `GET /api/next/health`
6. **Запишіть:** виберіть ім’я хоста до реєстрації passkeys; подальша зміна потребує повторної реєстрації, а localhost лишається лише винятком локального налаштування

### Команди й налаштування

#### Обидва канали

```text
RP_ID=discvault.example.com
RP_ORIGINS=https://discvault.example.com
curl --fail https://discvault.example.com/api/next/health
```

## Очікуваний результат

Приймайте результат для **Встановлення: Зворотний проксі та ключі доступу** лише коли підтверджено всі наведені нижче результати: HTTPS · HTTP `2xx`; `RP_ID`; `RP_ID` — це ім’я хоста без схеми й порту, а `RP_ORIGINS` — точний повний HTTPS origin; обидва мають відповідати адресі, відкритій у браузері. Контрольні точки цієї сторінки: FQDN · `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

- HTTPS · HTTP `2xx`
- `RP_ID`
- `RP_ID` — це ім’я хоста без схеми й порту, а `RP_ORIGINS` — точний повний HTTPS origin; обидва мають відповідати адресі, відкритій у браузері

## Безпека й відкочування

:::caution
Безпечна межа відновлення для **Встановлення: Зворотний проксі та ключі доступу** вимагає цих дій до будь-якого запуску чи продовження: виберіть ім’я хоста до реєстрації passkeys; подальша зміна потребує повторної реєстрації, а localhost лишається лише винятком локального налаштування; відновіть разом proxy та значення RP і збережіть спосіб відновлення власника. Контрольні точки цієї сторінки: FQDN · `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

- виберіть ім’я хоста до реєстрації passkeys; подальша зміна потребує повторної реєстрації, а localhost лишається лише винятком локального налаштування
- відновіть разом proxy та значення RP і збережіть спосіб відновлення власника
:::

## Наступний крок

[Встановлення: Перший запуск і перевірка стану](/uk/install/first-start-health/)

## Джерело й стан

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- [`helmerzNL/DiscVault.EU@583f85c55dc2`](https://github.com/helmerzNL/DiscVault.EU/commit/583f85c55dc22b63368f997a3b076a093ca0afa1)
- Джерело й стан: `2026-07-15`
- DiscVault Docs: `0.1.2`
