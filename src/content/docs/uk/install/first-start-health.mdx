---
title: 'Встановлення: Перший запуск і перевірка стану'
description: 'Спеціальні інструкції для завдання: Встановлення: Перший запуск і перевірка стану: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · `6180:5000` · WebAuthn.'
pageId: 'install-first-start-health'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-20'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · веб" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-20" />

## Мета й результат

Виконайте спеціальну процедуру сторінки для **Встановлення: Перший запуск і перевірка стану**. У цій процедурі використовуються `/api/next/health` · `/api/next/auth/status` · `6080:5000` · `6180:5000` · WebAuthn.

## Застосовується до

Ця сторінка стосується лише **Встановлення: Перший запуск і перевірка стану** (Docker · веб). Межі каналу й даних: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `6080` → API `5000`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `/api/next/auth/status`; `6180` → API `5000`. Контрольні точки цієї сторінки: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · `6180:5000` · WebAuthn.

### DiscVault v26 – стабільний

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `6080` → API `5000`

### DiscVault v26 – бета

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `/api/next/auth/status`
- `6180` → API `5000`

## Передумови

- `/api/next/health`
- `/api/next/auth/status`

## Процедура

1. **Перевірте:** `postgres`
2. **Перевірте:** `GET /api/next/health` · HTTP `2xx`
3. **Виберіть:** [Встановлення: Legacy Authentication](/uk/install/legacy-authentication/)
4. **Відкрийте:** `6180:5000`
5. **Перевірте:** `GET /api/next/auth/status` · WebAuthn TOTP

### Команди й налаштування

#### DiscVault v26 – стабільний

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml ps
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml logs --tail=100 next-api next-worker next-mcp postgres
curl --fail http://localhost:6080/api/next/health
curl --fail http://localhost:6080/api/next/auth/status
```

#### DiscVault v26 – бета

```bash
docker compose -p discvault_next_deploy -f /opt/discvault-next/docker-compose.yml ps
docker compose -p discvault_next_deploy -f /opt/discvault-next/docker-compose.yml logs --tail=100 next-api next-worker next-mcp postgres
curl --fail http://localhost:6180/api/next/health
curl --fail http://localhost:6180/api/next/auth/status
```

## Очікуваний результат

Приймайте результат для **Встановлення: Перший запуск і перевірка стану** лише коли підтверджено всі наведені нижче результати: health успішний, налаштований origin відкривається, а перший обліковий запис стає власником. Контрольні точки цієї сторінки: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · `6180:5000` · WebAuthn.

- health успішний, налаштований origin відкривається, а перший обліковий запис стає власником

## Безпека й відкочування

:::caution
Безпечна межа відновлення для **Встановлення: Перший запуск і перевірка стану** вимагає цих дій до будь-якого запуску чи продовження: журнали з часом і прихованими приватними даними; відповідний комплект резервних копій. Контрольні точки цієї сторінки: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · `6180:5000` · WebAuthn.

- журнали з часом і прихованими приватними даними
- відповідний комплект резервних копій
:::

## Наступний крок

[Оновлення й міграція: Резервне копіювання](/uk/update/backup/)

## Джерело й стан

- [`helmerzNL/DiscVault@4352c060ccd6`](https://github.com/helmerzNL/DiscVault/commit/4352c060ccd6fd625a828f6e20c24f111c9ef743)
- [Moving beta source: Legacy Authentication deployment notes](https://github.com/helmerzNL/DiscVault/blob/release/v26-beta/app/deploy/next/README.md)
- [Pinned verification: `next_auth.py`](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/backend/next_auth.py)
- [Pinned verification: `next_legacy_auth.py`](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/backend/next_legacy_auth.py)
- [Pinned verification: Legacy Authentication schema](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/backend/migrations_next/041_legacy_auth.sql)
- [Pinned verification: unified recovery codes](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/backend/migrations_next/042_unified_recovery_codes.sql)
- Джерело й стан: `2026-07-20`
- DiscVault Docs: `0.1.6`
