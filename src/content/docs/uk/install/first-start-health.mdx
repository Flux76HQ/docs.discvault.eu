---
title: 'Встановлення: Перший запуск і перевірка стану'
description: 'Спеціальні інструкції для завдання: Встановлення: Перший запуск і перевірка стану: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.'
pageId: 'install-first-start-health'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · веб" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Мета й результат

Виконайте спеціальну процедуру сторінки для **Встановлення: Перший запуск і перевірка стану**. У цій процедурі використовуються `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

## Застосовується до

Ця сторінка стосується лише **Встановлення: Перший запуск і перевірка стану** (Docker · веб). Межі каналу й даних: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `/api/next/auth/status`; `6080` → API `5000`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `/api/next/auth/status`; `6080` → API `5000`. Контрольні точки цієї сторінки: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

### DiscVault v26 – стабільний

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `/api/next/auth/status`
- `6080` → API `5000`

### DiscVault v26 – бета

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `/api/next/auth/status`
- `6080` → API `5000`

## Передумови

- `/api/next/health`
- `/api/next/auth/status`

## Процедура

1. **Перевірте:** `postgres`
2. **Перевірте:** `GET /api/next/health` · HTTP `2xx`
3. **Відкрийте:** `6080:5000`
4. **Перевірте:** `GET /api/next/auth/status` · WebAuthn

### Команди й налаштування

#### Обидва канали

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml ps
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml logs --tail=100 next-api next-worker next-mcp postgres
curl --fail http://localhost:6080/api/next/health
curl --fail http://localhost:6080/api/next/auth/status
```

## Очікуваний результат

Приймайте результат для **Встановлення: Перший запуск і перевірка стану** лише коли підтверджено всі наведені нижче результати: health успішний, налаштований origin відкривається, а перший обліковий запис стає власником. Контрольні точки цієї сторінки: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

- health успішний, налаштований origin відкривається, а перший обліковий запис стає власником

## Безпека й відкочування

:::caution
Безпечна межа відновлення для **Встановлення: Перший запуск і перевірка стану** вимагає цих дій до будь-якого запуску чи продовження: журнали з часом і прихованими приватними даними; відповідний комплект резервних копій. Контрольні точки цієї сторінки: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

- журнали з часом і прихованими приватними даними
- відповідний комплект резервних копій
:::

## Наступний крок

[Оновлення й міграція: Резервне копіювання](/uk/update/backup/)

## Джерело й стан

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Джерело й стан: `2026-07-14`
- DiscVault Docs: `0.1.0`
