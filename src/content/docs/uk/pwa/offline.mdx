---
title: 'PWA: Робота без мережі'
description: 'Спеціальні інструкції для завдання: PWA: Робота без мережі: `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.'
pageId: 'pwa-offline'
products: ['pwa']
platforms: ['web', 'ios', 'android']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="веб · iOS · Android" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Мета й результат

Виконайте спеціальну процедуру сторінки для **PWA: Робота без мережі**. У цій процедурі використовуються `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.

## Застосовується до

Ця сторінка стосується лише **PWA: Робота без мережі** (веб · iOS · Android). Межі каналу й даних: DiscVault · `ghcr.io/helmerznl/discvault:latest`; кешовані екрани можуть відкритися, але нові дані й запис потребують мережі · `Service Worker` · `CacheStorage`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; кешовані екрани можуть відкритися, але нові дані й запис потребують мережі · `Service Worker` · `CacheStorage`. Контрольні точки цієї сторінки: `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.

### DiscVault v26 – стабільний

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- кешовані екрани можуть відкритися, але нові дані й запис потребують мережі · `Service Worker` · `CacheStorage`

### DiscVault v26 – бета

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- кешовані екрани можуть відкритися, але нові дані й запис потребують мережі · `Service Worker` · `CacheStorage`

## Передумови

- PWA
- `CacheStorage`

## Процедура

1. **Відкрийте:** Бібліотека → Пошук
2. **Перевірте:** режим польоту · `Service Worker`
3. **Запишіть:** `navigator.onLine`
4. **Підключіться знову:** HTTPS



## Очікуваний результат

Приймайте результат для **PWA: Робота без мережі** лише коли підтверджено всі наведені нижче результати: кешовані екрани можуть відкритися, але нові дані й запис потребують мережі; `Service Worker` · `navigator.onLine`. Контрольні точки цієї сторінки: `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.

- кешовані екрани можуть відкритися, але нові дані й запис потребують мережі
- `Service Worker` · `navigator.onLine`

## Безпека й відкочування

:::caution
Безпечна межа відновлення для **PWA: Робота без мережі** вимагає цих дій до будь-якого запуску чи продовження: кешовані екрани можуть відкритися, але нові дані й запис потребують мережі · DiscVault. Контрольні точки цієї сторінки: `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.

- кешовані екрани можуть відкритися, але нові дані й запис потребують мережі · DiscVault
:::

## Наступний крок

[Усунення проблем](/uk/troubleshooting/)

## Джерело й стан

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Джерело й стан: `2026-07-14`
- DiscVault Docs: `0.1.0`
