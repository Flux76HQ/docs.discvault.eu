---
title: 'Адміністрування: Журнали, завдання й безпека'
description: 'Спеціальні інструкції для завдання: Адміністрування: Журнали, завдання й безпека: `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.'
pageId: 'admin-index'
products: ['server']
platforms: ['docker', 'unraid', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid · веб" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Мета й результат

Виконайте спеціальну процедуру сторінки для **Адміністрування: Журнали, завдання й безпека**. У цій процедурі використовуються `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.

## Застосовується до

Ця сторінка стосується лише **Адміністрування: Журнали, завдання й безпека** (Docker · Unraid · веб). Межі каналу й даних: DiscVault · `ghcr.io/helmerznl/discvault:latest`; API MCP PostgreSQL; DiscVault · `ghcr.io/helmerznl/discvault:beta`; API MCP PostgreSQL. Контрольні точки цієї сторінки: `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.

### DiscVault v26 – стабільний

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- API MCP PostgreSQL

### DiscVault v26 – бета

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- API MCP PostgreSQL

## Передумови

- доступ власника
- відповідний комплект резервних копій
- журнали з часом і прихованими приватними даними

## Процедура

1. **Перевірте:** `/api/next/health`
2. **Перевірте:** журнали з часом і прихованими приватними даними
3. **Перевірте:** `next-worker`
4. **Збережіть:** `--tail=200`

### Команди й налаштування

#### Обидва канали

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml logs --tail=200 next-api next-worker next-mcp postgres
curl --fail http://localhost:6080/api/next/jobs
curl --fail http://localhost:6080/api/next/health
```

## Очікуваний результат

Приймайте результат для **Адміністрування: Журнали, завдання й безпека** лише коли підтверджено всі наведені нижче результати: один несправний рівень ізольовано з часом, станом, digest образу й прихованою помилкою; відповідний комплект резервних копій. Контрольні точки цієї сторінки: `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.

- один несправний рівень ізольовано з часом, станом, digest образу й прихованою помилкою
- відповідний комплект резервних копій

## Безпека й відкочування

:::caution
Безпечна межа відновлення для **Адміністрування: Журнали, завдання й безпека** вимагає цих дій до будь-якого запуску чи продовження: не видаляйте спершу volumes, `/data`, дані браузера чи сховище застосунку; збережіть приховані журнали й відновлюйте лише відповідну копію. Контрольні точки цієї сторінки: `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.

- не видаляйте спершу volumes, `/data`, дані браузера чи сховище застосунку; збережіть приховані журнали й відновлюйте лише відповідну копію
:::

## Наступний крок

[Усунення проблем](/uk/troubleshooting/)

## Джерело й стан

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Джерело й стан: `2026-07-14`
- DiscVault Docs: `0.1.0`
