---
title: 'Налаштування: Автентифікація та RBAC'
description: 'Спеціальні інструкції для завдання: Налаштування: Автентифікація та RBAC: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h` · iOS/iPadOS `16+` · Android `9+` · OIDC · Argon2id · TOTP.'
pageId: 'configure-auth-rbac'
products: ['server']
platforms: ['web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault', 'helmerzNL/DiscVault.EU', 'Flux76HQ/App-Guidance']
lastVerified: '2026-09-20'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="веб" version="DiscVault v26" source="helmerzNL/DiscVault · helmerzNL/DiscVault.EU · Flux76HQ/App-Guidance" verified="2026-09-20" />

## Мета й результат

Виконайте спеціальну процедуру сторінки для **Налаштування: Автентифікація та RBAC**. У цій процедурі використовуються `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h` · iOS/iPadOS `16+` · Android `9+` · OIDC · Argon2id · TOTP.

## Застосовується до

Ця сторінка стосується лише **Налаштування: Автентифікація та RBAC** (веб). Межі каналу й даних: DiscVault · `ghcr.io/helmerznl/discvault:latest`; WebAuthn; passkey — це криптографічна пара ключів: приватний ключ залишається на пристрої або в надійному менеджері облікових даних, а DiscVault зберігає лише відкритий ключ; DiscVault · `ghcr.io/helmerznl/discvault:beta`; WebAuthn OIDC; TOTP. Контрольні точки цієї сторінки: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h` · iOS/iPadOS `16+` · Android `9+` · OIDC · Argon2id · TOTP.

### DiscVault v26 – стабільний

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- WebAuthn
- passkey — це криптографічна пара ключів: приватний ключ залишається на пристрої або в надійному менеджері облікових даних, а DiscVault зберігає лише відкритий ключ

### DiscVault v26 – бета

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- WebAuthn OIDC
- TOTP

## Передумови

- доступ власника
- [Встановлення: Зворотний проксі та ключі доступу](/uk/install/reverse-proxy-passkeys/) · HTTPS FQDN
- другий спосіб відновлення

## Процедура

1. **Перевірте:** passkey — це криптографічна пара ключів: приватний ключ залишається на пристрої або в надійному менеджері облікових даних, а DiscVault зберігає лише відкритий ключ
2. **Порівняйте:** passkeys стійкі до фішингу й унікальні для сайту; вхід використовує розблокування пристрою замість багаторазового пароля облікового запису · обмежений у часі код запрошення починає реєстрацію, але не є багаторазовим паролем; звичайний вхід використовує passkey
3. **Перевірте:** Windows потребує сучасного браузера з підтримкою passkeys і Windows Hello щонайменше з PIN-кодом; для вбудованого керування рекомендовано Windows 11 22H2 або новішу
4. **Перевірте:** macOS потребує Ventura 13 або новішу з iCloud Keychain чи інший менеджер облікових даних із підтримкою passkeys
5. **Перевірте:** iPhone та iPad потребують iOS або iPadOS 16+ з iCloud Keychain і двофакторною автентифікацією; Android потребує версію 9+ із провайдером passkeys і блокуванням екрана
6. **Створіть:** зареєструйте кілька passkeys, наприклад на телефоні й комп’ютері, та збережіть незалежний шлях відновлення власника до заміни або втрати пристрою
7. **Відкрийте:** Адміністрування → Безпека → Увімкнути автентифікацію
8. **Створіть:** Адміністрування → Користувачі → Створити запрошення на 48 годин
9. **Налаштуйте:** Адміністрування → Ролі → Базовий / Розширений
10. **Налаштуйте:** в наявній інсталяції увімкніть вхід паролем у Користувачі та ролі й підтвердьте новою passkey-перевіркою власника або адміністратора · власники й адміністратори можуть видавати тимчасові паролі, вимагати зміну, задавати MFA для користувача та керувати реєстрацією passkeys
11. **Порівняйте:** [Налаштування: Вхід через OpenID Connect (OIDC)](/uk/configure/oidc/) · OIDC додає необов’язковий вхід у браузері; локальні passkeys, необов’язкові паролі, коди відновлення, API-токени й MCP bearer-токени зберігають свої ролі
12. **Запишіть:** коди відновлення хешовані, одноразові та спільні з відновленням passkey; резервні копії не містять TOTP-секретів і матеріалів відновлення, тому MFA реєструють знову · вимкнення Legacy Authentication потребує активної passkey власника; власник лише з локальною IP має спершу налаштувати дійсний FQDN і passkey
13. **Перевірте:** `/api/next/auth/rbac`

### Команди й налаштування

#### DiscVault v26 – стабільний

```bash
curl --fail http://localhost:6080/api/next/auth/status
curl --fail http://localhost:6080/api/next/auth/rbac
```

#### DiscVault v26 – бета

```bash
curl --fail http://localhost:6180/api/next/auth/status
curl --fail http://localhost:6180/api/next/auth/rbac
```

## Очікуваний результат

Приймайте результат для **Налаштування: Автентифікація та RBAC** лише коли підтверджено всі наведені нижче результати: вхід власника з passkey працює, реєстрація за запрошенням відповідає налаштуванню, а тестовий користувач має лише призначені дозволи; passkeys стійкі до фішингу й унікальні для сайту; вхід використовує розблокування пристрою замість багаторазового пароля облікового запису; обмежений у часі код запрошення починає реєстрацію, але не є багаторазовим паролем; звичайний вхід використовує passkey. Контрольні точки цієї сторінки: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h` · iOS/iPadOS `16+` · Android `9+` · OIDC · Argon2id · TOTP.

- вхід власника з passkey працює, реєстрація за запрошенням відповідає налаштуванню, а тестовий користувач має лише призначені дозволи
- passkeys стійкі до фішингу й унікальні для сайту; вхід використовує розблокування пристрою замість багаторазового пароля облікового запису
- обмежений у часі код запрошення починає реєстрацію, але не є багаторазовим паролем; звичайний вхід використовує passkey

## Безпека й відкочування

:::caution
Безпечна межа відновлення для **Налаштування: Автентифікація та RBAC** вимагає цих дій до будь-якого запуску чи продовження: зареєструйте кілька passkeys, наприклад на телефоні й комп’ютері, та збережіть незалежний шлях відновлення власника до заміни або втрати пристрою; пароль має щонайменше 15 символів, не може збігатися з іменем користувача чи забороненим поширеним паролем і хешується Argon2id; п’ять помилок за 15 хвилин блокують на 15 хвилин; коди відновлення хешовані, одноразові та спільні з відновленням passkey; резервні копії не містять TOTP-секретів і матеріалів відновлення, тому MFA реєструють знову; вимкнення Legacy Authentication потребує активної passkey власника; власник лише з локальною IP має спершу налаштувати дійсний FQDN і passkey; ідентичність є точною парою issuer і subject; електронна пошта ніколи не прив’язується автоматично, прив’язування потребує активної сесії браузера, а від’єднання — іншого придатного способу входу; збережіть сеанс власника й другий спосіб відновлення; ніколи не видаляйте останній passkey власника. Контрольні точки цієї сторінки: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h` · iOS/iPadOS `16+` · Android `9+` · OIDC · Argon2id · TOTP.

- зареєструйте кілька passkeys, наприклад на телефоні й комп’ютері, та збережіть незалежний шлях відновлення власника до заміни або втрати пристрою
- пароль має щонайменше 15 символів, не може збігатися з іменем користувача чи забороненим поширеним паролем і хешується Argon2id; п’ять помилок за 15 хвилин блокують на 15 хвилин
- коди відновлення хешовані, одноразові та спільні з відновленням passkey; резервні копії не містять TOTP-секретів і матеріалів відновлення, тому MFA реєструють знову
- вимкнення Legacy Authentication потребує активної passkey власника; власник лише з локальною IP має спершу налаштувати дійсний FQDN і passkey
- ідентичність є точною парою issuer і subject; електронна пошта ніколи не прив’язується автоматично, прив’язування потребує активної сесії браузера, а від’єднання — іншого придатного способу входу
- збережіть сеанс власника й другий спосіб відновлення; ніколи не видаляйте останній passkey власника
:::

## Наступний крок

[Налаштування: Вхід через OpenID Connect (OIDC)](/uk/configure/oidc/)

## Джерело й стан

- [`helmerzNL/DiscVault@409da7b2dc25`](https://github.com/helmerzNL/DiscVault/commit/409da7b2dc25bbf621cd0a6682136d565cdf5070)
- [`helmerzNL/DiscVault.EU@583f85c55dc2`](https://github.com/helmerzNL/DiscVault.EU/commit/583f85c55dc22b63368f997a3b076a093ca0afa1)
- [`Flux76HQ/App-Guidance@41bfca3bfa39`](https://github.com/Flux76HQ/App-Guidance/commit/41bfca3bfa39f0b43924190d384e2d96be756020)
- [Moving beta source: OIDC deployment notes](https://github.com/helmerzNL/DiscVault/blob/release/v26-beta/app/deploy/next/README.md)
- [Pinned verification: OIDC environment template](https://github.com/helmerzNL/DiscVault/blob/409da7b2dc25bbf621cd0a6682136d565cdf5070/app/deploy/next/.env.example)
- [Pinned verification: OIDC implementation](https://github.com/helmerzNL/DiscVault/blob/409da7b2dc25bbf621cd0a6682136d565cdf5070/app/backend/next_oidc.py)
- [Authoritative OIDC authentication contract](https://github.com/Flux76HQ/App-Guidance/blob/41bfca3bfa39f0b43924190d384e2d96be756020/projects/discvault/specs/oidc-authentication.md)
- Джерело й стан: `2026-09-20`
- DiscVault Docs: `0.1.14`
