---
title: 'Інтеграції: MCP і REST API'
description: 'Спеціальні інструкції для завдання: Інтеграції: MCP і REST API: `streamable-http` · `/mcp` · `Authorization` · `6090`.'
pageId: 'integrations-mcp-api'
products: ['server']
platforms: ['web', 'docker']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="веб · Docker" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Мета й результат

Виконайте спеціальну процедуру сторінки для **Інтеграції: MCP і REST API**. У цій процедурі використовуються `streamable-http` · `/mcp` · `Authorization` · `6090`.

## Застосовується до

Ця сторінка стосується лише **Інтеграції: MCP і REST API** (веб · Docker). Межі каналу й даних: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `/mcp` `6090` · REST API; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `/mcp` `6090` · REST API. Контрольні точки цієї сторінки: `streamable-http` · `/mcp` · `Authorization` · `6090`.

### DiscVault v26 – стабільний

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `/mcp` `6090` · REST API

### DiscVault v26 – бета

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `/mcp` `6090` · REST API

## Передумови

- API
- HTTPS DiscVault
- MCP HTTP

## Процедура

1. **Створіть:** API
2. **Налаштуйте:** `streamable-http` · `https://discvault.example.com/mcp`
3. **Перевірте:** `Authorization`
4. **Відкличте:** `6090`

### Команди й налаштування

#### Обидва канали

```json
{
  "mcpServers": {
    "discvault": {
      "transport": "streamable-http",
      "url": "https://discvault.example.com/mcp",
      "headers": { "Authorization": "******" }
    }
  }
}
```

```bash
curl --fail https://discvault.example.com/api/next/health
```

## Очікуваний результат

Приймайте результат для **Інтеграції: MCP і REST API** лише коли підтверджено всі наведені нижче результати: DiscVault · `streamable-http` · `/mcp`; облікові дані постачальника, збережені як секрет · `Authorization`. Контрольні точки цієї сторінки: `streamable-http` · `/mcp` · `Authorization` · `6090`.

- DiscVault · `streamable-http` · `/mcp`
- облікові дані постачальника, збережені як секрет · `Authorization`

## Безпека й відкочування

:::caution
Безпечна межа відновлення для **Інтеграції: MCP і REST API** вимагає цих дій до будь-якого запуску чи продовження: вимкніть інтеграцію, відкличте тестові чи розкриті облікові дані, за потреби створіть нові й ніколи не використовуйте старі повторно. Контрольні точки цієї сторінки: `streamable-http` · `/mcp` · `Authorization` · `6090`.

- вимкніть інтеграцію, відкличте тестові чи розкриті облікові дані, за потреби створіть нові й ніколи не використовуйте старі повторно
:::

## Наступний крок

[Інтеграції: Plex і Jellyfin](/uk/integrations/plex-jellyfin/)

## Джерело й стан

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Джерело й стан: `2026-07-14`
- DiscVault Docs: `0.1.0`
