---
title: 'Оновлення й міграція: Міграція до DiscVault 26'
description: 'Спеціальні інструкції для завдання: Оновлення й міграція: Міграція до DiscVault 26: `/migration/readiness` · `ready_for_confirmation` · `/data/discvault.db` · SQLite.'
pageId: 'update-migration'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · веб" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Мета й результат

Виконайте спеціальну процедуру сторінки для **Оновлення й міграція: Міграція до DiscVault 26**. У цій процедурі використовуються `/migration/readiness` · `ready_for_confirmation` · `/data/discvault.db` · SQLite.

## Застосовується до

Ця сторінка стосується лише **Оновлення й міграція: Міграція до DiscVault 26** (Docker · веб). Межі каналу й даних: DiscVault · `ghcr.io/helmerznl/discvault:latest`; DiscVault 26; джерело наявних даних лише для читання; ніколи не поточна runtime-база · SQLite → PostgreSQL; DiscVault · `ghcr.io/helmerznl/discvault:beta`; DiscVault 26; джерело наявних даних лише для читання; ніколи не поточна runtime-база · SQLite → PostgreSQL. Контрольні точки цієї сторінки: `/migration/readiness` · `ready_for_confirmation` · `/data/discvault.db` · SQLite.

### DiscVault v26 – стабільний

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- DiscVault 26
- джерело наявних даних лише для читання; ніколи не поточна runtime-база · SQLite → PostgreSQL

### DiscVault v26 – бета

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- DiscVault 26
- джерело наявних даних лише для читання; ніколи не поточна runtime-база · SQLite → PostgreSQL

## Передумови

- джерело наявних даних лише для читання; ніколи не поточна runtime-база · `/data/discvault.db`
- дамп PostgreSQL і файловий архів з одного вікна без запису
- доступ власника

## Процедура

1. **Створіть резервну копію:** джерело наявних даних лише для читання; ніколи не поточна runtime-база · дамп PostgreSQL і файловий архів з одного вікна без запису
2. **Перевірте:** `/api/next/migration/readiness` · `ready_for_confirmation`
3. **Виконайте:** `POST /api/next/migration/start`
4. **Порівняйте:** готовність підтверджено, міграція не має невдалих записів, а кількість і зразок медіа збігаються

### Команди й налаштування

#### Обидва канали

```bash
curl --fail http://localhost:6080/api/next/migration/readiness
curl --fail http://localhost:6080/api/next/migration/status
curl --fail -X POST http://localhost:6080/api/next/migration/start \
  -H 'Content-Type: application/json' -d '{}'
curl --fail http://localhost:6080/api/next/migration/status
```

## Очікуваний результат

Приймайте результат для **Оновлення й міграція: Міграція до DiscVault 26** лише коли підтверджено всі наведені нижче результати: готовність підтверджено, міграція не має невдалих записів, а кількість і зразок медіа збігаються. Контрольні точки цієї сторінки: `/migration/readiness` · `ready_for_confirmation` · `/data/discvault.db` · SQLite.

- готовність підтверджено, міграція не має невдалих записів, а кількість і зразок медіа збігаються

## Безпека й відкочування

:::caution
Безпечна межа відновлення для **Оновлення й міграція: Міграція до DiscVault 26** вимагає цих дій до будь-якого запуску чи продовження: зупиніть процеси запису DiscVault, відновіть відповідні копії PostgreSQL і `/data` та ніколи не повторюйте імпорт у частково заповнену базу. Контрольні точки цієї сторінки: `/migration/readiness` · `ready_for_confirmation` · `/data/discvault.db` · SQLite.

- зупиніть процеси запису DiscVault, відновіть відповідні копії PostgreSQL і `/data` та ніколи не повторюйте імпорт у частково заповнену базу
:::

## Наступний крок

[Налаштування: Змінні середовища](/uk/configure/environment/)

## Джерело й стан

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Джерело й стан: `2026-07-14`
- DiscVault Docs: `0.1.0`
