---
title: 'Instalacja: Odwrotny serwer proxy i klucze dostępu'
description: 'Instrukcja właściwa dla zadania: Instalacja: Odwrotny serwer proxy i klucze dostępu: FQDN · `RP_ORIGINS` · `RP_ID` · OIDC · `/api/next/auth/oidc/callback` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.'
pageId: 'install-reverse-proxy-passkeys'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault', 'helmerzNL/DiscVault.EU', 'Flux76HQ/App-Guidance']
lastVerified: '2026-09-20'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · web" version="DiscVault v26" source="helmerzNL/DiscVault · helmerzNL/DiscVault.EU · Flux76HQ/App-Guidance" verified="2026-09-20" />

## Cel i rezultat

Wykonaj procedurę właściwą dla strony: **Instalacja: Odwrotny serwer proxy i klucze dostępu**. Ta procedura korzysta z FQDN · `RP_ORIGINS` · `RP_ID` · OIDC · `/api/next/auth/oidc/callback` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

## Dotyczy

Ta strona dotyczy wyłącznie **Instalacja: Odwrotny serwer proxy i klucze dostępu** (Docker · web). Granice kanału i danych: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `RP_ID` `RP_ORIGINS` · `6080`; poza wyjątkiem localhost passkeys wymagają stabilnej pełnej nazwy domenowej przez zaufany HTTPS; sam adres IP ani zwykły HTTP nie są obsługiwane; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `RP_ID` `RP_ORIGINS` · `6180`; OIDC · `/api/next/auth/oidc/callback` · [Konfiguracja: Logowanie przez OpenID Connect (OIDC)](/pl/configure/oidc/); `LEGACY_AUTH_ENABLED=true`. Punkty kontrolne tej strony: FQDN · `RP_ORIGINS` · `RP_ID` · OIDC · `/api/next/auth/oidc/callback` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

### DiscVault v26 – stabilny

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `RP_ID` `RP_ORIGINS` · `6080`
- poza wyjątkiem localhost passkeys wymagają stabilnej pełnej nazwy domenowej przez zaufany HTTPS; sam adres IP ani zwykły HTTP nie są obsługiwane

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `RP_ID` `RP_ORIGINS` · `6180`
- OIDC · `/api/next/auth/oidc/callback` · [Konfiguracja: Logowanie przez OpenID Connect (OIDC)](/pl/configure/oidc/)
- `LEGACY_AUTH_ENABLED=true`

## Wymagania wstępne

- FQDN
- `RP_ORIGINS`
- `RP_ID`

### Awaryjne Legacy Authentication

:::note[Dostępne w wersji beta]
Legacy Authentication z nazwą użytkownika, hasłem i uwierzytelnianiem dwuskładnikowym (2FA) jest dostępne w DiscVault 26 beta, gdy nie można używać passkeys.

Przed włączeniem funkcji lub utworzeniem pierwszego właściciela wykonaj `/install/legacy-authentication/`.
:::

## Procedura

1. **Wybierz:** FQDN · poza wyjątkiem localhost passkeys wymagają stabilnej pełnej nazwy domenowej przez zaufany HTTPS; sam adres IP ani zwykły HTTP nie są obsługiwane
2. **Wybierz:** [Instalacja: Legacy Authentication](/pl/install/legacy-authentication/) · dla bezpośredniego prywatnego lub zwrotnego IP bez FQDN pozostaw `RP_ID` i `RP_ORIGINS` puste
3. **Skonfiguruj:** `RP_ID`
4. **Skonfiguruj:** `RP_ORIGINS` · HTTPS
5. **Skonfiguruj:** [Konfiguracja: Logowanie przez OpenID Connect (OIDC)](/pl/configure/oidc/) · `/api/next/auth/oidc/callback`
6. **Skonfiguruj:** `6080` `6180`
7. **Przetestuj:** `GET /api/next/health`
8. **Zapisz:** wybierz nazwę hosta przed rejestracją passkeys; późniejsza zmiana wymaga ponownej rejestracji, a localhost pozostaje tylko wyjątkiem lokalnej konfiguracji

### Polecenia i ustawienia

#### DiscVault v26 – stabilny

```text
RP_ID=discvault.example.com
RP_NAME=DiscVault
RP_ORIGINS=https://discvault.example.com
curl --fail https://discvault.example.com/api/next/health
```

#### DiscVault v26 – beta

```text
RP_ID=discvault.example.com
RP_NAME=DiscVault
RP_ORIGINS=https://discvault.example.com
LEGACY_AUTH_ENABLED=true
curl --fail http://localhost:6180/api/next/health
```

## Oczekiwany rezultat

Zaakceptuj wynik dla **Instalacja: Odwrotny serwer proxy i klucze dostępu** tylko gdy wszystkie poniższe wyniki są prawdziwe: HTTPS · HTTP `2xx`; `RP_ID`; OIDC · `/api/next/auth/oidc/callback`; `RP_ID` to nazwa hosta bez schematu i portu, a `RP_ORIGINS` to dokładne pełne źródło HTTPS; oba muszą odpowiadać adresowi otwartemu w przeglądarce. Punkty kontrolne tej strony: FQDN · `RP_ORIGINS` · `RP_ID` · OIDC · `/api/next/auth/oidc/callback` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

- HTTPS · HTTP `2xx`
- `RP_ID`
- OIDC · `/api/next/auth/oidc/callback`
- `RP_ID` to nazwa hosta bez schematu i portu, a `RP_ORIGINS` to dokładne pełne źródło HTTPS; oba muszą odpowiadać adresowi otwartemu w przeglądarce

## Bezpieczeństwo i wycofanie

:::caution
Bezpieczna granica odzyskiwania dla **Instalacja: Odwrotny serwer proxy i klucze dostępu** wymaga tych działań przed uruchomieniem lub kontynuacją: wybierz nazwę hosta przed rejestracją passkeys; późniejsza zmiana wymaga ponownej rejestracji, a localhost pozostaje tylko wyjątkiem lokalnej konfiguracji; tylko bezpośrednie prywatne lub zwrotne żądanie z nieprawidłowym lub pustym RP może oferować start bez TOTP; pozostaw TOTP dla nazwy użytkownika, hasła i 2FA; przywróć razem proxy i wartości RP oraz zachowaj metodę odzyskiwania właściciela. Punkty kontrolne tej strony: FQDN · `RP_ORIGINS` · `RP_ID` · OIDC · `/api/next/auth/oidc/callback` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

- wybierz nazwę hosta przed rejestracją passkeys; późniejsza zmiana wymaga ponownej rejestracji, a localhost pozostaje tylko wyjątkiem lokalnej konfiguracji
- tylko bezpośrednie prywatne lub zwrotne żądanie z nieprawidłowym lub pustym RP może oferować start bez TOTP; pozostaw TOTP dla nazwy użytkownika, hasła i 2FA
- przywróć razem proxy i wartości RP oraz zachowaj metodę odzyskiwania właściciela
:::

## Następny krok

[Instalacja: Pierwsze uruchomienie i kontrola stanu](/pl/install/first-start-health/)

## Źródło i stan

- [`helmerzNL/DiscVault@409da7b2dc25`](https://github.com/helmerzNL/DiscVault/commit/409da7b2dc25bbf621cd0a6682136d565cdf5070)
- [`helmerzNL/DiscVault.EU@583f85c55dc2`](https://github.com/helmerzNL/DiscVault.EU/commit/583f85c55dc22b63368f997a3b076a093ca0afa1)
- [`Flux76HQ/App-Guidance@41bfca3bfa39`](https://github.com/Flux76HQ/App-Guidance/commit/41bfca3bfa39f0b43924190d384e2d96be756020)
- [Moving beta source: OIDC deployment notes](https://github.com/helmerzNL/DiscVault/blob/release/v26-beta/app/deploy/next/README.md)
- [Pinned verification: OIDC environment template](https://github.com/helmerzNL/DiscVault/blob/409da7b2dc25bbf621cd0a6682136d565cdf5070/app/deploy/next/.env.example)
- [Pinned verification: OIDC implementation](https://github.com/helmerzNL/DiscVault/blob/409da7b2dc25bbf621cd0a6682136d565cdf5070/app/backend/next_oidc.py)
- [Authoritative OIDC authentication contract](https://github.com/Flux76HQ/App-Guidance/blob/41bfca3bfa39f0b43924190d384e2d96be756020/projects/discvault/specs/oidc-authentication.md)
- Źródło i stan: `2026-09-20`
- DiscVault Docs: `0.1.14`
