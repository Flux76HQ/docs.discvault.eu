---
title: 'Instalacja: Pierwsze uruchomienie i kontrola stanu'
description: 'Instrukcja właściwa dla zadania: Instalacja: Pierwsze uruchomienie i kontrola stanu: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.'
pageId: 'install-first-start-health'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · web" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Cel i rezultat

Wykonaj procedurę właściwą dla strony: **Instalacja: Pierwsze uruchomienie i kontrola stanu**. Ta procedura korzysta z `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

## Dotyczy

Ta strona dotyczy wyłącznie **Instalacja: Pierwsze uruchomienie i kontrola stanu** (Docker · web). Granice kanału i danych: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `/api/next/auth/status`; `6080` → API `5000`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `/api/next/auth/status`; `6080` → API `5000`. Punkty kontrolne tej strony: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

### DiscVault v26 – stabilny

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `/api/next/auth/status`
- `6080` → API `5000`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `/api/next/auth/status`
- `6080` → API `5000`

## Wymagania wstępne

- `/api/next/health`
- `/api/next/auth/status`

## Procedura

1. **Sprawdź:** `postgres`
2. **Zweryfikuj:** `GET /api/next/health` · HTTP `2xx`
3. **Otwórz:** `6080:5000`
4. **Zweryfikuj:** `GET /api/next/auth/status` · WebAuthn

### Polecenia i ustawienia

#### Oba kanały

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml ps
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml logs --tail=100 next-api next-worker next-mcp postgres
curl --fail http://localhost:6080/api/next/health
curl --fail http://localhost:6080/api/next/auth/status
```

## Oczekiwany rezultat

Zaakceptuj wynik dla **Instalacja: Pierwsze uruchomienie i kontrola stanu** tylko gdy wszystkie poniższe wyniki są prawdziwe: kontrola stanu przechodzi, skonfigurowane origin się otwiera, a pierwsze konto jest właścicielem. Punkty kontrolne tej strony: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

- kontrola stanu przechodzi, skonfigurowane origin się otwiera, a pierwsze konto jest właścicielem

## Bezpieczeństwo i wycofanie

:::caution
Bezpieczna granica odzyskiwania dla **Instalacja: Pierwsze uruchomienie i kontrola stanu** wymaga tych działań przed uruchomieniem lub kontynuacją: dzienniki ze znacznikiem czasu i ukrytymi danymi; pasujący zestaw kopii. Punkty kontrolne tej strony: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

- dzienniki ze znacznikiem czasu i ukrytymi danymi
- pasujący zestaw kopii
:::

## Następny krok

[Aktualizacja i migracja: Kopia zapasowa](/pl/update/backup/)

## Źródło i stan

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Źródło i stan: `2026-07-14`
- DiscVault Docs: `0.1.0`
