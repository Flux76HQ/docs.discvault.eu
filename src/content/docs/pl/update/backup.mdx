---
title: 'Aktualizacja i migracja: Kopia zapasowa'
description: 'Instrukcja właściwa dla zadania: Aktualizacja i migracja: Kopia zapasowa: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.'
pageId: 'update-backup'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Cel i rezultat

Wykonaj procedurę właściwą dla strony: **Aktualizacja i migracja: Kopia zapasowa**. Ta procedura korzysta z `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

## Dotyczy

Ta strona dotyczy wyłącznie **Aktualizacja i migracja: Kopia zapasowa** (Docker · Unraid). Granice kanału i danych: DiscVault · `ghcr.io/helmerznl/discvault:latest`; zrzut PostgreSQL i archiwum plików z tego samego okna bez zapisów; PostgreSQL `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; zrzut PostgreSQL i archiwum plików z tego samego okna bez zapisów; PostgreSQL `/data`. Punkty kontrolne tej strony: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

### DiscVault v26 – stabilny

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- zrzut PostgreSQL i archiwum plików z tego samego okna bez zapisów
- PostgreSQL `/data`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- zrzut PostgreSQL i archiwum plików z tego samego okna bez zapisów
- PostgreSQL `/data`

## Wymagania wstępne

- miejsce kopii poza aktywnymi danymi
- zapisywalna pamięć trwała i wolne miejsce na kopię

## Procedura

1. **Zapisz:** `PREVIOUS_IMAGE` · `sha256`
2. **Zatrzymaj:** `next-api` `next-worker` `next-mcp`
3. **Utwórz kopię:** `pg_dump -Fc`
4. **Uruchom:** `next-api` → → MCP

### Polecenia i ustawienia

#### Oba kanały

```bash
set -euo pipefail
cd /opt/discvault
install -d -m 0700 /srv/backups
BACKUP_ID="$(date -u +%Y%m%dT%H%M%SZ)"
API_ID="$(docker compose --env-file .env -p discvault -f compose.yaml ps -q next-api)"
IMAGE_ID="$(docker inspect "$API_ID" --format '{{.Image}}')"
PREVIOUS_IMAGE="$(docker image inspect "$IMAGE_ID" --format '{{index .RepoDigests 0}}')"
test -n "$PREVIOUS_IMAGE"
printf 'BACKUP_ID=%s\nPREVIOUS_IMAGE=%s\n' "$BACKUP_ID" "$PREVIOUS_IMAGE" \
  > "/srv/backups/discvault-$BACKUP_ID.env"
chmod 0600 "/srv/backups/discvault-$BACKUP_ID.env"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_dump -Fc -U "$POSTGRES_USER" "$POSTGRES_DB"' \
  > "/srv/backups/discvault-$BACKUP_ID.dump"
tar --acls --xattrs -C /srv -czf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz" discvault
docker compose --env-file .env -p discvault -f compose.yaml start next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml start next-worker next-mcp
test -s "/srv/backups/discvault-$BACKUP_ID.dump"
gzip -t "/srv/backups/discvault-data-$BACKUP_ID.tgz"
```

## Oczekiwany rezultat

Zaakceptuj wynik dla **Aktualizacja i migracja: Kopia zapasowa** tylko gdy wszystkie poniższe wyniki są prawdziwe: archiwum i zrzut nie są puste, można je odczytać podczas próby i są powiązane z niezmiennym digestem obrazu. Punkty kontrolne tej strony: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- archiwum i zrzut nie są puste, można je odczytać podczas próby i są powiązane z niezmiennym digestem obrazu

## Bezpieczeństwo i wycofanie

:::caution
Bezpieczna granica odzyskiwania dla **Aktualizacja i migracja: Kopia zapasowa** wymaga tych działań przed uruchomieniem lub kontynuacją: jeśli polecenie kopii nie powiedzie się, uruchom ponownie tylko niezmienione usługi i anuluj aktualizację; zrzut PostgreSQL i archiwum plików z tego samego okna bez zapisów. Punkty kontrolne tej strony: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- jeśli polecenie kopii nie powiedzie się, uruchom ponownie tylko niezmienione usługi i anuluj aktualizację
- zrzut PostgreSQL i archiwum plików z tego samego okna bez zapisów
:::

## Następny krok

[Aktualizacja i migracja: Przywracanie](/pl/update/restore/)

## Źródło i stan

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Źródło i stan: `2026-07-14`
- DiscVault Docs: `0.1.0`
