---
title: 'Aktualizacja i migracja: Aktualizacja kontenera'
description: 'Instrukcja właściwa dla zadania: Aktualizacja i migracja: Aktualizacja kontenera: `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.'
pageId: 'update-update'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Cel i rezultat

Wykonaj procedurę właściwą dla strony: **Aktualizacja i migracja: Aktualizacja kontenera**. Ta procedura korzysta z `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.

## Dotyczy

Ta strona dotyczy wyłącznie **Aktualizacja i migracja: Aktualizacja kontenera** (Docker · Unraid). Granice kanału i danych: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `DISCVAULT_IMAGE`; PostgreSQL `/data` · informacje o wydaniu i zapisany niezmienny skrót obrazu · pasujący zestaw kopii; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `DISCVAULT_IMAGE`; PostgreSQL `/data` · informacje o wydaniu i zapisany niezmienny skrót obrazu · pasujący zestaw kopii. Punkty kontrolne tej strony: `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.

### DiscVault v26 – stabilny

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `DISCVAULT_IMAGE`
- PostgreSQL `/data` · informacje o wydaniu i zapisany niezmienny skrót obrazu · pasujący zestaw kopii

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `DISCVAULT_IMAGE`
- PostgreSQL `/data` · informacje o wydaniu i zapisany niezmienny skrót obrazu · pasujący zestaw kopii

## Wymagania wstępne

- pasujący zestaw kopii
- informacje o wydaniu i zapisany niezmienny skrót obrazu

## Procedura

1. **Zweryfikuj:** `PREVIOUS_IMAGE=@sha256:…`
2. **Wybierz:** `DISCVAULT_IMAGE` · `:latest` `:beta`
3. **Uruchom:** `--force-recreate`
4. **Porównaj:** działa zamierzony digest obrazu, stan jest prawidłowy, a logowanie właściciela, liczba rekordów i próbka przechodzą

### Polecenia i ustawienia

#### DiscVault v26 – stabilny

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:latest
```

#### DiscVault v26 – beta

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:beta
```

#### Oba kanały

```bash
set -euo pipefail
test -n "$DISCVAULT_IMAGE"
case "$DISCVAULT_IMAGE" in
  ghcr.io/helmerznl/discvault:latest|ghcr.io/helmerznl/discvault:beta) ;;
  *) printf '%s\n' "$DISCVAULT_IMAGE" >&2; exit 64 ;;
esac
cd /opt/discvault
ENV_FILE=/opt/discvault/.env
ENV_TMP="$(mktemp "$ENV_FILE.update.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk -v image="$DISCVAULT_IMAGE" '
  /^DISCVAULT_IMAGE=/ { print "DISCVAULT_IMAGE=" image; found=1; next }
  { print }
  END { if (!found) exit 42 }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
docker compose --env-file "$ENV_TMP" -p discvault -f compose.yaml config --quiet
docker compose --env-file "$ENV_TMP" -p discvault -f compose.yaml pull next-api next-worker next-mcp
docker compose --env-file "$ENV_TMP" -p discvault -f compose.yaml up -d --force-recreate
docker compose --env-file "$ENV_TMP" -p discvault -f compose.yaml ps
curl --fail http://localhost:6080/api/next/health
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

## Oczekiwany rezultat

Zaakceptuj wynik dla **Aktualizacja i migracja: Aktualizacja kontenera** tylko gdy wszystkie poniższe wyniki są prawdziwe: działa zamierzony digest obrazu, stan jest prawidłowy, a logowanie właściciela, liczba rekordów i próbka przechodzą. Punkty kontrolne tej strony: `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.

- działa zamierzony digest obrazu, stan jest prawidłowy, a logowanie właściciela, liczba rekordów i próbka przechodzą

## Bezpieczeństwo i wycofanie

:::caution
Bezpieczna granica odzyskiwania dla **Aktualizacja i migracja: Aktualizacja kontenera** wymaga tych działań przed uruchomieniem lub kontynuacją: [Aktualizacja i migracja: Wycofanie obrazu](/pl/update/rollback/) · pasujący zestaw kopii; informacje o wydaniu i zapisany niezmienny skrót obrazu. Punkty kontrolne tej strony: `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.

- [Aktualizacja i migracja: Wycofanie obrazu](/pl/update/rollback/) · pasujący zestaw kopii
- informacje o wydaniu i zapisany niezmienny skrót obrazu
:::

## Następny krok

[Aktualizacja i migracja: Wycofanie obrazu](/pl/update/rollback/)

## Źródło i stan

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Źródło i stan: `2026-07-14`
- DiscVault Docs: `0.1.0`
