---
title: 'インストール: Legacy Authentication'
description: 'タスク別の手順: インストール: Legacy Authentication: `LEGACY_AUTH_ENABLED=true` · `RP_ID=` · `RP_ORIGINS=` · TOTP · Argon2id · `15` · `/api/next/auth/status`.'
pageId: 'install-legacy-authentication'
products: ['server']
platforms: ['docker', 'web']
channels: ['beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-20'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="beta" platform="Docker · Web" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-20" />

## 目的と結果

このページ専用の手順を実行します: **インストール: Legacy Authentication**. この手順で扱う項目: `LEGACY_AUTH_ENABLED=true` · `RP_ID=` · `RP_ORIGINS=` · TOTP · Argon2id · `15` · `/api/next/auth/status`.

## 対象

このページの対象は **インストール: Legacy Authentication** (Docker · Web). チャンネルとデータの境界: DiscVault · `ghcr.io/helmerznl/discvault:beta`; TOTP; FQDN HTTPS. このページ固有の確認点: `LEGACY_AUTH_ENABLED=true` · `RP_ID=` · `RP_ORIGINS=` · TOTP · Argon2id · `15` · `/api/next/auth/status`.

### DiscVault v26 ベータ版

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- TOTP
- FQDN HTTPS

## 前提条件

- ユーザー名、パスワード、2 要素認証（2FA）を使用する Legacy Authentication は、パスキーを使用できない場合の代替手段として DiscVault 26 ベータで利用できます。
- 所有者アクセス · ユーザーや認証情報のない新規インスタンスでは、パスワードリスクの確認後にウィザードが最初の所有者をアトミックに作成します
- TOTP · 復旧コードはハッシュ化された使い捨てでパスキー復旧と共通です。バックアップには TOTP シークレットと復旧情報が含まれないため、復元後に MFA を再登録します

### Legacy Authentication のフォールバック

:::note[ベータで利用可能]
無効または空の RP 設定を持つ直接のプライベートまたはループバック要求だけが TOTP なしの初期設定を提示できます。ユーザー名、パスワード、2FA では TOTP を有効に保ちます
:::

## 手順

1. **設定:** `LEGACY_AUTH_ENABLED=true` · 有効な FQDN では `RP_ID` と `RP_ORIGINS` を一致させ、信頼された HTTPS を使用します
2. **設定:** `RP_ID=` · `RP_ORIGINS=` · FQDN のない直接のプライベートまたはループバック IP では `RP_ID` と `RP_ORIGINS` を空にします
3. **選択:** ユーザーや認証情報のない新規インスタンスでは、パスワードリスクの確認後にウィザードが最初の所有者をアトミックに作成します · 既存インストールではユーザーとロールでパスワードログインを有効にし、所有者または管理者の新しいパスキー検証で承認します
4. **作成:** パスワードは 15 文字以上で、ユーザー名や拒否対象の一般的なパスワードは使用できず、Argon2id でハッシュ化されます。15 分間に 5 回失敗すると 15 分間ロックされます · TOTP · 復旧コードはハッシュ化された使い捨てでパスキー復旧と共通です。バックアップには TOTP シークレットと復旧情報が含まれないため、復元後に MFA を再登録します
5. **設定:** 所有者と管理者は一時パスワードの発行、変更の強制、ユーザー別 MFA ポリシー、パスキー登録の制御を行えます
6. **テスト:** ユーザー名、パスワード、2 要素認証（2FA）を使用する Legacy Authentication は、パスキーを使用できない場合の代替手段として DiscVault 26 ベータで利用できます。 · `GET /api/next/auth/status`
7. **記録:** 復旧コードはハッシュ化された使い捨てでパスキー復旧と共通です。バックアップには TOTP シークレットと復旧情報が含まれないため、復元後に MFA を再登録します

### コマンドと設定

#### DiscVault v26 ベータ版

```text
RP_ID=discvault.example.com
RP_NAME=DiscVault
RP_ORIGINS=https://discvault.example.com
LEGACY_AUTH_ENABLED=true
```

```text
RP_ID=
RP_NAME=DiscVault
RP_ORIGINS=
LEGACY_AUTH_ENABLED=true
```

```bash
curl --fail http://localhost:6180/api/next/health
curl --fail http://localhost:6180/api/next/auth/status
```

## 期待される結果

次の結果を受け入れる条件: **インストール: Legacy Authentication** 以下の結果をすべて確認した場合のみ: ユーザー名、パスワード、2 要素認証（2FA）を使用する Legacy Authentication は、パスキーを使用できない場合の代替手段として DiscVault 26 ベータで利用できます。; TOTP · 復旧コードはハッシュ化された使い捨てでパスキー復旧と共通です。バックアップには TOTP シークレットと復旧情報が含まれないため、復元後に MFA を再登録します; パスワードは 15 文字以上で、ユーザー名や拒否対象の一般的なパスワードは使用できず、Argon2id でハッシュ化されます。15 分間に 5 回失敗すると 15 分間ロックされます. このページ固有の確認点: `LEGACY_AUTH_ENABLED=true` · `RP_ID=` · `RP_ORIGINS=` · TOTP · Argon2id · `15` · `/api/next/auth/status`.

- ユーザー名、パスワード、2 要素認証（2FA）を使用する Legacy Authentication は、パスキーを使用できない場合の代替手段として DiscVault 26 ベータで利用できます。
- TOTP · 復旧コードはハッシュ化された使い捨てでパスキー復旧と共通です。バックアップには TOTP シークレットと復旧情報が含まれないため、復元後に MFA を再登録します
- パスワードは 15 文字以上で、ユーザー名や拒否対象の一般的なパスワードは使用できず、Argon2id でハッシュ化されます。15 分間に 5 回失敗すると 15 分間ロックされます

## 安全性とロールバック

:::caution
安全な復旧境界: **インストール: Legacy Authentication** 起動または続行の前に、次の操作が必要です: パスワードは 15 文字以上で、ユーザー名や拒否対象の一般的なパスワードは使用できず、Argon2id でハッシュ化されます。15 分間に 5 回失敗すると 15 分間ロックされます; 復旧コードはハッシュ化された使い捨てでパスキー復旧と共通です。バックアップには TOTP シークレットと復旧情報が含まれないため、復元後に MFA を再登録します; Legacy Authentication の無効化には有効な所有者パスキーが必要です。ローカル IP のみの所有者は無効化前に有効な FQDN と所有者パスキーを設定します. このページ固有の確認点: `LEGACY_AUTH_ENABLED=true` · `RP_ID=` · `RP_ORIGINS=` · TOTP · Argon2id · `15` · `/api/next/auth/status`.

- パスワードは 15 文字以上で、ユーザー名や拒否対象の一般的なパスワードは使用できず、Argon2id でハッシュ化されます。15 分間に 5 回失敗すると 15 分間ロックされます
- 復旧コードはハッシュ化された使い捨てでパスキー復旧と共通です。バックアップには TOTP シークレットと復旧情報が含まれないため、復元後に MFA を再登録します
- Legacy Authentication の無効化には有効な所有者パスキーが必要です。ローカル IP のみの所有者は無効化前に有効な FQDN と所有者パスキーを設定します
:::

## 次の手順

[インストール: 初回起動とヘルスチェック](/ja/install/first-start-health/)

## 情報源と状態

- [`helmerzNL/DiscVault@4352c060ccd6`](https://github.com/helmerzNL/DiscVault/commit/4352c060ccd6fd625a828f6e20c24f111c9ef743)
- [Moving beta source: Legacy Authentication deployment notes](https://github.com/helmerzNL/DiscVault/blob/release/v26-beta/app/deploy/next/README.md)
- [Pinned verification: `next_auth.py`](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/backend/next_auth.py)
- [Pinned verification: `next_legacy_auth.py`](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/backend/next_legacy_auth.py)
- [Pinned verification: Legacy Authentication schema](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/backend/migrations_next/041_legacy_auth.sql)
- [Pinned verification: unified recovery codes](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/backend/migrations_next/042_unified_recovery_codes.sql)
- 情報源と状態: `2026-07-20`
- DiscVault Docs: `0.1.6`
