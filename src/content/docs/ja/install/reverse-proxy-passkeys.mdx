---
title: 'インストール: リバースプロキシとパスキー'
description: 'タスク別の手順: インストール: リバースプロキシとパスキー: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.'
pageId: 'install-reverse-proxy-passkeys'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Web" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## 目的と結果

このページ専用の手順を実行します: **インストール: リバースプロキシとパスキー**. この手順で扱う項目: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

## 対象

このページの対象は **インストール: リバースプロキシとパスキー** (Docker · Web). チャンネルとデータの境界: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `RP_ID` `RP_ORIGINS`; `6080` API `5000`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `RP_ID` `RP_ORIGINS`; `6080` API `5000`. このページ固有の確認点: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

### DiscVault v26 安定版

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `RP_ID` `RP_ORIGINS`
- `6080` API `5000`

### DiscVault v26 ベータ版

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `RP_ID` `RP_ORIGINS`
- `6080` API `5000`

## 前提条件

- DNS
- TLS
- `6080`

## 手順

1. **設定:** `RP_ID`
2. **設定:** `RP_ORIGINS` · HTTPS
3. **設定:** `http://127.0.0.1:6080`
4. **テスト:** `GET /api/next/health`

### コマンドと設定

#### 両方のチャンネル

```text
RP_ID=discvault.example.com
RP_ORIGINS=https://discvault.example.com
curl --fail https://discvault.example.com/api/next/health
```

## 期待される結果

次の結果を受け入れる条件: **インストール: リバースプロキシとパスキー** 以下の結果をすべて確認した場合のみ: HTTPS · HTTP `2xx`; `RP_ID`. このページ固有の確認点: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

- HTTPS · HTTP `2xx`
- `RP_ID`

## 安全性とロールバック

:::caution
安全な復旧境界: **インストール: リバースプロキシとパスキー** 起動または続行の前に、次の操作が必要です: プロキシと RP 値を一緒に復元し、所有者の復旧手段を保持します. このページ固有の確認点: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

- プロキシと RP 値を一緒に復元し、所有者の復旧手段を保持します
:::

## 次の手順

[インストール: 初回起動とヘルスチェック](/ja/install/first-start-health/)

## 情報源と状態

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- 情報源と状態: `2026-07-14`
- DiscVault Docs: `0.1.0`
