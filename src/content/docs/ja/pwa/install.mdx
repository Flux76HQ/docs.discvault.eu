---
title: 'PWA: PWA のインストール'
description: 'タスク別の手順: PWA: PWA のインストール: HTTPS · `display-mode: standalone` · iOS/iPadOS · Android.'
pageId: 'pwa-install'
products: ['pwa']
platforms: ['web', 'ios', 'android']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Web · iOS · Android" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## 目的と結果

このページ専用の手順を実行します: **PWA: PWA のインストール**. この手順で扱う項目: HTTPS · `display-mode: standalone` · iOS/iPadOS · Android.

## 対象

このページの対象は **PWA: PWA のインストール** (Web · iOS · Android). チャンネルとデータの境界: DiscVault · `ghcr.io/helmerznl/discvault:latest`; PWA · `display-mode: standalone`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; PWA · `display-mode: standalone`. このページ固有の確認点: HTTPS · `display-mode: standalone` · iOS/iPadOS · Android.

### DiscVault v26 安定版

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- PWA · `display-mode: standalone`

### DiscVault v26 ベータ版

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- PWA · `display-mode: standalone`

## 前提条件

- HTTPS
- 必要な権限でサインイン済みのアカウント

## 手順

1. **インストール:** アドレスバー → DiscVault をインストール
2. **インストール:** 共有 → ホーム画面に追加 · iOS/iPadOS
3. **インストール:** ブラウザメニュー → アプリをインストール · Android
4. **検証:** Android



## 期待される結果

次の結果を受け入れる条件: **PWA: PWA のインストール** 以下の結果をすべて確認した場合のみ: アドレスバー → DiscVault をインストール; 共有 → ホーム画面に追加; ブラウザメニュー → アプリをインストール; 必要な権限でサインイン済みのアカウント · HTTPS. このページ固有の確認点: HTTPS · `display-mode: standalone` · iOS/iPadOS · Android.

- アドレスバー → DiscVault をインストール
- 共有 → ホーム画面に追加
- ブラウザメニュー → アプリをインストール
- 必要な権限でサインイン済みのアカウント · HTTPS

## 安全性とロールバック

:::caution
安全な復旧境界: **PWA: PWA のインストール** 起動または続行の前に、次の操作が必要です: インストール済み PWA シェルだけを削除し、サーバーライブラリとアカウントデータは変更しません. このページ固有の確認点: HTTPS · `display-mode: standalone` · iOS/iPadOS · Android.

- インストール済み PWA シェルだけを削除し、サーバーライブラリとアカウントデータは変更しません
:::

## 次の手順

[PWA: ライブラリと検索](/ja/pwa/library-search/)

## 情報源と状態

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- 情報源と状態: `2026-07-14`
- DiscVault Docs: `0.1.0`
