---
title: '設定: 認証と RBAC'
description: 'タスク別の手順: 設定: 認証と RBAC: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h`.'
pageId: 'configure-auth-rbac'
products: ['server']
platforms: ['web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Web" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## 目的と結果

このページ専用の手順を実行します: **設定: 認証と RBAC**. この手順で扱う項目: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h`.

## 対象

このページの対象は **設定: 認証と RBAC** (Web). チャンネルとデータの境界: DiscVault · `ghcr.io/helmerznl/discvault:latest`; WebAuthn; DiscVault · `ghcr.io/helmerznl/discvault:beta`; WebAuthn. このページ固有の確認点: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h`.

### DiscVault v26 安定版

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- WebAuthn

### DiscVault v26 ベータ版

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- WebAuthn

## 前提条件

- 所有者アクセス
- HTTPS
- 2 つ目の復旧方法

## 手順

1. **開く:** 管理 → セキュリティ → 認証を有効化
2. **作成:** 管理 → ユーザー → 48 時間の招待を作成
3. **設定:** 管理 → ロール → 基本 / 詳細
4. **テスト:** `48 h`

### コマンドと設定

#### 両方のチャンネル

```bash
curl --fail http://localhost:6080/api/next/auth/status
curl --fail http://localhost:6080/api/next/auth/rbac
```

## 期待される結果

次の結果を受け入れる条件: **設定: 認証と RBAC** 以下の結果をすべて確認した場合のみ: 所有者のパスキーログインが機能し、招待制登録が設定どおりで、テストユーザーには割り当てた権限だけがあります. このページ固有の確認点: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h`.

- 所有者のパスキーログインが機能し、招待制登録が設定どおりで、テストユーザーには割り当てた権限だけがあります

## 安全性とロールバック

:::caution
安全な復旧境界: **設定: 認証と RBAC** 起動または続行の前に、次の操作が必要です: 所有者セッションと 2 つ目の復旧手段を保持し、最後の所有者パスキーは削除しません. このページ固有の確認点: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h`.

- 所有者セッションと 2 つ目の復旧手段を保持し、最後の所有者パスキーは削除しません
:::

## 次の手順

[設定: プラグインとメタデータ](/ja/configure/plugins-metadata/)

## 情報源と状態

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- 情報源と状態: `2026-07-14`
- DiscVault Docs: `0.1.0`
