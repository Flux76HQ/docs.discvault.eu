---
title: 'Kurulum: Ters vekil ve geçiş anahtarları'
description: 'Göreve özel yönergeler: Kurulum: Ters vekil ve geçiş anahtarları: FQDN · `RP_ORIGINS` · `RP_ID` · OIDC · `/api/next/auth/oidc/callback` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.'
pageId: 'install-reverse-proxy-passkeys'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault', 'helmerzNL/DiscVault.EU', 'Flux76HQ/App-Guidance']
lastVerified: '2026-09-20'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · web" version="DiscVault v26" source="helmerzNL/DiscVault · helmerzNL/DiscVault.EU · Flux76HQ/App-Guidance" verified="2026-09-20" />

## Amaç ve sonuç

Sayfaya özel iş akışını tamamlayın: **Kurulum: Ters vekil ve geçiş anahtarları**. Bu prosedür şunları kullanır: FQDN · `RP_ORIGINS` · `RP_ID` · OIDC · `/api/next/auth/oidc/callback` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

## Geçerli olduğu alan

Bu sayfa yalnızca şunlar için geçerlidir: **Kurulum: Ters vekil ve geçiş anahtarları** (Docker · web). Kanal ve veri sınırları: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `RP_ID` `RP_ORIGINS` · `6080`; localhost kurulum istisnası dışında passkey için güvenilir HTTPS üzerinden kararlı tam etki alanı adı gerekir; yalnız IP adresi veya düz HTTP desteklenmez; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `RP_ID` `RP_ORIGINS` · `6180`; OIDC · `/api/next/auth/oidc/callback` · [Yapılandırma: OpenID Connect (OIDC) ile oturum açma](/tr/configure/oidc/); `LEGACY_AUTH_ENABLED=true`. Sayfaya özel denetim noktaları: FQDN · `RP_ORIGINS` · `RP_ID` · OIDC · `/api/next/auth/oidc/callback` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

### DiscVault v26 kararlı

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `RP_ID` `RP_ORIGINS` · `6080`
- localhost kurulum istisnası dışında passkey için güvenilir HTTPS üzerinden kararlı tam etki alanı adı gerekir; yalnız IP adresi veya düz HTTP desteklenmez

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `RP_ID` `RP_ORIGINS` · `6180`
- OIDC · `/api/next/auth/oidc/callback` · [Yapılandırma: OpenID Connect (OIDC) ile oturum açma](/tr/configure/oidc/)
- `LEGACY_AUTH_ENABLED=true`

## Ön koşullar

- FQDN
- `RP_ORIGINS`
- `RP_ID`

### Legacy Authentication geri dönüşü

:::note[Betada kullanılabilir]
Kullanıcı adı, parola ve iki faktörlü kimlik doğrulama (2FA) kullanan Legacy Authentication, passkey kullanılamadığında DiscVault 26 betada sunulur.

Özelliği etkinleştirmeden veya ilk sahibi oluşturmadan önce `/install/legacy-authentication/` adımlarını izleyin.
:::

## Prosedür

1. **Seçin:** FQDN · localhost kurulum istisnası dışında passkey için güvenilir HTTPS üzerinden kararlı tam etki alanı adı gerekir; yalnız IP adresi veya düz HTTP desteklenmez
2. **Seçin:** [Kurulum: Legacy Authentication](/tr/install/legacy-authentication/) · FQDN olmadan doğrudan özel veya geri döngü IP için `RP_ID` ve `RP_ORIGINS` değerlerini boş bırakın
3. **Yapılandırın:** `RP_ID`
4. **Yapılandırın:** `RP_ORIGINS` · HTTPS
5. **Yapılandırın:** [Yapılandırma: OpenID Connect (OIDC) ile oturum açma](/tr/configure/oidc/) · `/api/next/auth/oidc/callback`
6. **Yapılandırın:** `6080` `6180`
7. **Test edin:** `GET /api/next/health`
8. **Kaydedin:** passkey kaydetmeden önce ana bilgisayar adını seçin; daha sonra değiştirmek yeniden kayıt gerektirir ve localhost yalnızca yerel kurulum istisnasıdır

### Komutlar ve ayarlar

#### DiscVault v26 kararlı

```text
RP_ID=discvault.example.com
RP_NAME=DiscVault
RP_ORIGINS=https://discvault.example.com
curl --fail https://discvault.example.com/api/next/health
```

#### DiscVault v26 beta

```text
RP_ID=discvault.example.com
RP_NAME=DiscVault
RP_ORIGINS=https://discvault.example.com
LEGACY_AUTH_ENABLED=true
curl --fail http://localhost:6180/api/next/health
```

## Beklenen sonuç

Şunun sonucunu kabul edin: **Kurulum: Ters vekil ve geçiş anahtarları** yalnızca aşağıdaki sonuçların tümü doğru olduğunda: HTTPS · HTTP `2xx`; `RP_ID`; OIDC · `/api/next/auth/oidc/callback`; `RP_ID` şema veya bağlantı noktası olmadan ana bilgisayar adıdır; `RP_ORIGINS` ise tam HTTPS origin değeridir ve ikisi de tarayıcıda açılan adresle eşleşmelidir. Sayfaya özel denetim noktaları: FQDN · `RP_ORIGINS` · `RP_ID` · OIDC · `/api/next/auth/oidc/callback` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

- HTTPS · HTTP `2xx`
- `RP_ID`
- OIDC · `/api/next/auth/oidc/callback`
- `RP_ID` şema veya bağlantı noktası olmadan ana bilgisayar adıdır; `RP_ORIGINS` ise tam HTTPS origin değeridir ve ikisi de tarayıcıda açılan adresle eşleşmelidir

## Güvenlik ve geri alma

:::caution
Şunun güvenli kurtarma sınırı: **Kurulum: Ters vekil ve geçiş anahtarları** başlatmadan veya devam etmeden önce şu işlemleri gerektirir: passkey kaydetmeden önce ana bilgisayar adını seçin; daha sonra değiştirmek yeniden kayıt gerektirir ve localhost yalnızca yerel kurulum istisnasıdır; yalnızca geçersiz veya boş RP ayarlarına sahip doğrudan özel ya da geri döngü isteği TOTP olmadan başlangıç sunabilir; kullanıcı adı, parola ve 2FA için TOTP etkin kalsın; proxy ve RP değerlerini birlikte geri yükleyin ve bir sahip kurtarma yöntemi bulundurun. Sayfaya özel denetim noktaları: FQDN · `RP_ORIGINS` · `RP_ID` · OIDC · `/api/next/auth/oidc/callback` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

- passkey kaydetmeden önce ana bilgisayar adını seçin; daha sonra değiştirmek yeniden kayıt gerektirir ve localhost yalnızca yerel kurulum istisnasıdır
- yalnızca geçersiz veya boş RP ayarlarına sahip doğrudan özel ya da geri döngü isteği TOTP olmadan başlangıç sunabilir; kullanıcı adı, parola ve 2FA için TOTP etkin kalsın
- proxy ve RP değerlerini birlikte geri yükleyin ve bir sahip kurtarma yöntemi bulundurun
:::

## Sonraki adım

[Kurulum: İlk başlatma ve sağlık kontrolü](/tr/install/first-start-health/)

## Kaynak ve durum

- [`helmerzNL/DiscVault@409da7b2dc25`](https://github.com/helmerzNL/DiscVault/commit/409da7b2dc25bbf621cd0a6682136d565cdf5070)
- [`helmerzNL/DiscVault.EU@583f85c55dc2`](https://github.com/helmerzNL/DiscVault.EU/commit/583f85c55dc22b63368f997a3b076a093ca0afa1)
- [`Flux76HQ/App-Guidance@41bfca3bfa39`](https://github.com/Flux76HQ/App-Guidance/commit/41bfca3bfa39f0b43924190d384e2d96be756020)
- [Moving beta source: OIDC deployment notes](https://github.com/helmerzNL/DiscVault/blob/release/v26-beta/app/deploy/next/README.md)
- [Pinned verification: OIDC environment template](https://github.com/helmerzNL/DiscVault/blob/409da7b2dc25bbf621cd0a6682136d565cdf5070/app/deploy/next/.env.example)
- [Pinned verification: OIDC implementation](https://github.com/helmerzNL/DiscVault/blob/409da7b2dc25bbf621cd0a6682136d565cdf5070/app/backend/next_oidc.py)
- [Authoritative OIDC authentication contract](https://github.com/Flux76HQ/App-Guidance/blob/41bfca3bfa39f0b43924190d384e2d96be756020/projects/discvault/specs/oidc-authentication.md)
- Kaynak ve durum: `2026-09-20`
- DiscVault Docs: `0.1.14`
