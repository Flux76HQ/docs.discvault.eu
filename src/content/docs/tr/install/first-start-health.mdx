---
title: 'Kurulum: İlk başlatma ve sağlık kontrolü'
description: 'Göreve özel yönergeler: Kurulum: İlk başlatma ve sağlık kontrolü: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.'
pageId: 'install-first-start-health'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · web" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Amaç ve sonuç

Sayfaya özel iş akışını tamamlayın: **Kurulum: İlk başlatma ve sağlık kontrolü**. Bu prosedür şunları kullanır: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

## Geçerli olduğu alan

Bu sayfa yalnızca şunlar için geçerlidir: **Kurulum: İlk başlatma ve sağlık kontrolü** (Docker · web). Kanal ve veri sınırları: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `/api/next/auth/status`; `6080` → API `5000`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `/api/next/auth/status`; `6080` → API `5000`. Sayfaya özel denetim noktaları: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

### DiscVault v26 kararlı

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `/api/next/auth/status`
- `6080` → API `5000`

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `/api/next/auth/status`
- `6080` → API `5000`

## Ön koşullar

- `/api/next/health`
- `/api/next/auth/status`

## Prosedür

1. **İnceleyin:** `postgres`
2. **Doğrulayın:** `GET /api/next/health` · HTTP `2xx`
3. **Açın:** `6080:5000`
4. **Doğrulayın:** `GET /api/next/auth/status` · WebAuthn

### Komutlar ve ayarlar

#### Her iki kanal

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml ps
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml logs --tail=100 next-api next-worker next-mcp postgres
curl --fail http://localhost:6080/api/next/health
curl --fail http://localhost:6080/api/next/auth/status
```

## Beklenen sonuç

Şunun sonucunu kabul edin: **Kurulum: İlk başlatma ve sağlık kontrolü** yalnızca aşağıdaki sonuçların tümü doğru olduğunda: sağlık denetimi başarılıdır, yapılandırılmış origin açılır ve ilk hesap sahip olur. Sayfaya özel denetim noktaları: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

- sağlık denetimi başarılıdır, yapılandırılmış origin açılır ve ilk hesap sahip olur

## Güvenlik ve geri alma

:::caution
Şunun güvenli kurtarma sınırı: **Kurulum: İlk başlatma ve sağlık kontrolü** başlatmadan veya devam etmeden önce şu işlemleri gerektirir: özel verileri gizlenmiş zaman damgalı günlükler; eşleşen yedek kümesi. Sayfaya özel denetim noktaları: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

- özel verileri gizlenmiş zaman damgalı günlükler
- eşleşen yedek kümesi
:::

## Sonraki adım

[Güncelleme ve geçiş: Yedekleme](/tr/update/backup/)

## Kaynak ve durum

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Kaynak ve durum: `2026-07-14`
- DiscVault Docs: `0.1.0`
