---
title: 'Yapılandırma: Ortam değişkenleri'
description: 'Göreve özel yönergeler: Yapılandırma: Ortam değişkenleri: `/opt/discvault-next/.env` · `DISCVAULT_NEXT_IMAGE` · `DISCVAULT_NEXT_POSTGRES_DATA` · `chmod 0600` · `openssl rand -base64 48` · `RP_ORIGINS` · `LEGACY_AUTH_ENABLED`.'
pageId: 'configure-environment'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-20'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-20" />

## Amaç ve sonuç

Sayfaya özel iş akışını tamamlayın: **Yapılandırma: Ortam değişkenleri**. Bu prosedür şunları kullanır: `/opt/discvault-next/.env` · `DISCVAULT_NEXT_IMAGE` · `DISCVAULT_NEXT_POSTGRES_DATA` · `chmod 0600` · `openssl rand -base64 48` · `RP_ORIGINS` · `LEGACY_AUTH_ENABLED`.

## Geçerli olduğu alan

Bu sayfa yalnızca şunlar için geçerlidir: **Yapılandırma: Ortam değişkenleri** (Docker · Unraid). Kanal ve veri sınırları: DiscVault · `ghcr.io/helmerznl/discvault:beta`; `.env.example` `release/v26-beta`; `0600` · `JWT_SECRET` `POSTGRES_PASSWORD`. Sayfaya özel denetim noktaları: `/opt/discvault-next/.env` · `DISCVAULT_NEXT_IMAGE` · `DISCVAULT_NEXT_POSTGRES_DATA` · `chmod 0600` · `openssl rand -base64 48` · `RP_ORIGINS` · `LEGACY_AUTH_ENABLED`.

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `.env.example` `release/v26-beta`
- `0600` · `JWT_SECRET` `POSTGRES_PASSWORD`

## Ön koşullar

- özel DiscVault çalışma zamanı dosyası
- OpenSSL
- planlanan yeniden başlatma aralığı

## Prosedür

1. **Oluşturun:** `/opt/discvault-next/.env` · yalnızca sahip dosya izinleri (0600)
2. **Yapılandırın:** `DISCVAULT_NEXT_IMAGE` · `RP_ORIGINS`
3. **Seçin:** geçerli bir FQDN ile `RP_ID` ve `RP_ORIGINS` değerlerini eşleştirin ve güvenilir HTTPS kullanın · FQDN olmadan doğrudan özel veya geri döngü IP için `RP_ID` ve `RP_ORIGINS` değerlerini boş bırakın
4. **Doğrulayın:** `chmod 0600` · `docker compose -p discvault_next_deploy config --quiet`
5. **Başlatın:** normal yapılandırma değişikliklerinde JWT ve PostgreSQL gizlerini değiştirmeyin

### Komutlar ve ayarlar

#### DiscVault v26 beta

```bash
set -euo pipefail
install -d -m 0700 /opt/discvault-next
cd /opt/discvault-next
curl --fail --location --proto '=https' --tlsv1.2 \
  --output docker-compose.yml \
  https://raw.githubusercontent.com/helmerzNL/DiscVault/release/v26-beta/app/deploy/next/docker-compose.yml
curl --fail --location --proto '=https' --tlsv1.2 \
  --output .env.example \
  https://raw.githubusercontent.com/helmerzNL/DiscVault/release/v26-beta/app/deploy/next/.env.example

umask 077
ENV_FILE=/opt/discvault-next/.env
test ! -e "$ENV_FILE"
ENV_TMP="$(mktemp "$ENV_FILE.tmp.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
POSTGRES_PASSWORD="$(openssl rand -hex 32)"
JWT_SECRET="$(openssl rand -base64 48)"
awk \
  -v image='ghcr.io/helmerznl/discvault:beta' \
  -v postgres_password="$POSTGRES_PASSWORD" \
  -v jwt_secret="$JWT_SECRET" \
  -v data_dir='/mnt/user/appdata/discvault' \
  -v postgres_dir='/mnt/user/appdata/discvault-next/postgres' '
  /^DISCVAULT_NEXT_IMAGE=/ { print "DISCVAULT_NEXT_IMAGE=" image; next }
  /^POSTGRES_PASSWORD=/ { print "POSTGRES_PASSWORD=" postgres_password; next }
  /^JWT_SECRET=/ { print "JWT_SECRET=" jwt_secret; next }
  /^DISCVAULT_DATA_DIR=/ { print "DISCVAULT_DATA_DIR=" data_dir; next }
  /^DISCVAULT_NEXT_POSTGRES_DATA=/ {
    print "DISCVAULT_NEXT_POSTGRES_DATA=" postgres_dir
    next
  }
  /^LEGACY_AUTH_ENABLED=/ { print "LEGACY_AUTH_ENABLED=true"; next }
  { print }
' .env.example > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
ln "$ENV_TMP" "$ENV_FILE"
rm -f "$ENV_TMP"
trap - EXIT
```

```bash
set -euo pipefail
cd /opt/discvault-next
FQDN=discvault.example.com
ENV_FILE=/opt/discvault-next/.env
ENV_TMP="$(mktemp "$ENV_FILE.tmp.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk -v rp_id="$FQDN" -v rp_origin="https://$FQDN" '
  /^RP_ID=/ { print "RP_ID=" rp_id; next }
  /^RP_NAME=/ { print "RP_NAME=DiscVault"; next }
  /^RP_ORIGINS=/ { print "RP_ORIGINS=" rp_origin; next }
  /^LEGACY_AUTH_ENABLED=/ { print "LEGACY_AUTH_ENABLED=true"; next }
  { print }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

```bash
set -euo pipefail
cd /opt/discvault-next
ENV_FILE=/opt/discvault-next/.env
ENV_TMP="$(mktemp "$ENV_FILE.tmp.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk '
  /^RP_ID=/ { print "RP_ID="; next }
  /^RP_NAME=/ { print "RP_NAME=DiscVault"; next }
  /^RP_ORIGINS=/ { print "RP_ORIGINS="; next }
  /^LEGACY_AUTH_ENABLED=/ { print "LEGACY_AUTH_ENABLED=true"; next }
  { print }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

```bash
set -euo pipefail
cd /opt/discvault-next
ENV_FILE=/opt/discvault-next/.env
test "$(stat -c '%a' "$ENV_FILE")" = 600
read_env() { sed -n "s/^$1=//p" "$ENV_FILE"; }

test "$(read_env DISCVAULT_NEXT_IMAGE)" = 'ghcr.io/helmerznl/discvault:beta'
! grep -q ':dev' "$ENV_FILE"
test "$(read_env LEGACY_AUTH_ENABLED)" = true
POSTGRES_PASSWORD_VALUE="$(read_env POSTGRES_PASSWORD)"
JWT_SECRET_VALUE="$(read_env JWT_SECRET)"
test "${#POSTGRES_PASSWORD_VALUE}" -ge 32
test "$POSTGRES_PASSWORD_VALUE" != 'change-me-to-a-long-random-password'
test "${#JWT_SECRET_VALUE}" -ge 48
grep -qx 'DISCVAULT_DATA_DIR=/mnt/user/appdata/discvault' "$ENV_FILE"
grep -qx 'DISCVAULT_NEXT_POSTGRES_DATA=/mnt/user/appdata/discvault-next/postgres' "$ENV_FILE"

RP_ID_VALUE="$(read_env RP_ID)"
RP_ORIGINS_VALUE="$(read_env RP_ORIGINS)"
if [ -z "$RP_ID_VALUE" ]; then
  test -z "$RP_ORIGINS_VALUE"
else
  test "$RP_ORIGINS_VALUE" = "https://$RP_ID_VALUE"
fi

docker compose -p discvault_next_deploy config --quiet
docker compose -p discvault_next_deploy pull
docker compose -p discvault_next_deploy up -d
docker compose -p discvault_next_deploy ps
curl --fail http://localhost:6180/api/next/health
```

## Beklenen sonuç

Şunun sonucunu kabul edin: **Yapılandırma: Ortam değişkenleri** yalnızca aşağıdaki sonuçların tümü doğru olduğunda: yalnızca sahip dosya izinleri (0600); özel dosyanın modu 600 olur, Compose doğrulaması başarılıdır ve seçilen kanal sağlıklı hale gelir. Sayfaya özel denetim noktaları: `/opt/discvault-next/.env` · `DISCVAULT_NEXT_IMAGE` · `DISCVAULT_NEXT_POSTGRES_DATA` · `chmod 0600` · `openssl rand -base64 48` · `RP_ORIGINS` · `LEGACY_AUTH_ENABLED`.

- yalnızca sahip dosya izinleri (0600)
- özel dosyanın modu 600 olur, Compose doğrulaması başarılıdır ve seçilen kanal sağlıklı hale gelir

## Güvenlik ve geri alma

:::caution
Şunun güvenli kurtarma sınırı: **Yapılandırma: Ortam değişkenleri** başlatmadan veya devam etmeden önce şu işlemleri gerektirir: belge deposunun ortam dosyası hiçbir zaman DiscVault çalışma zamanı dosyası değildir · `.env.example`; normal yapılandırma değişikliklerinde JWT ve PostgreSQL gizlerini değiştirmeyin · `JWT_SECRET` · `POSTGRES_PASSWORD`; indirilen `.env.example` hâlâ `:dev` kullanır; pull veya başlangıçtan önce çalışma zamanı `.env` dosyası `:beta` yapılmalıdır; hata durumunda hizmetleri durdurulmuş tutun, önceki özel ortam dosyasını geri yükleyip doğrulayın ve gizleri döndürmeden aynı imajı yeniden oluşturun. Sayfaya özel denetim noktaları: `/opt/discvault-next/.env` · `DISCVAULT_NEXT_IMAGE` · `DISCVAULT_NEXT_POSTGRES_DATA` · `chmod 0600` · `openssl rand -base64 48` · `RP_ORIGINS` · `LEGACY_AUTH_ENABLED`.

- belge deposunun ortam dosyası hiçbir zaman DiscVault çalışma zamanı dosyası değildir · `.env.example`
- normal yapılandırma değişikliklerinde JWT ve PostgreSQL gizlerini değiştirmeyin · `JWT_SECRET` · `POSTGRES_PASSWORD`
- indirilen `.env.example` hâlâ `:dev` kullanır; pull veya başlangıçtan önce çalışma zamanı `.env` dosyası `:beta` yapılmalıdır
- hata durumunda hizmetleri durdurulmuş tutun, önceki özel ortam dosyasını geri yükleyip doğrulayın ve gizleri döndürmeden aynı imajı yeniden oluşturun
:::

## Sonraki adım

[Yapılandırma: Kimlik doğrulama ve RBAC](/tr/configure/auth-rbac/)

## Kaynak ve durum

- [`helmerzNL/DiscVault@4352c060ccd6`](https://github.com/helmerzNL/DiscVault/commit/4352c060ccd6fd625a828f6e20c24f111c9ef743)
- [Moving beta source: `docker-compose.yml`](https://github.com/helmerzNL/DiscVault/blob/release/v26-beta/app/deploy/next/docker-compose.yml)
- [Raw beta download: `docker-compose.yml`](https://raw.githubusercontent.com/helmerzNL/DiscVault/release/v26-beta/app/deploy/next/docker-compose.yml)
- [Pinned verification: `docker-compose.yml`](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/deploy/next/docker-compose.yml)
- [Moving beta source: `.env.example`](https://github.com/helmerzNL/DiscVault/blob/release/v26-beta/app/deploy/next/.env.example)
- [Raw beta download: `.env.example`](https://raw.githubusercontent.com/helmerzNL/DiscVault/release/v26-beta/app/deploy/next/.env.example)
- [Pinned verification: `.env.example`](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/deploy/next/.env.example)
- Kaynak ve durum: `2026-07-20`
- DiscVault Docs: `0.1.6`
