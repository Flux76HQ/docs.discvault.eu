---
title: 'Güncelleme ve geçiş: Yedekleme'
description: 'Göreve özel yönergeler: Güncelleme ve geçiş: Yedekleme: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.'
pageId: 'update-backup'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Amaç ve sonuç

Sayfaya özel iş akışını tamamlayın: **Güncelleme ve geçiş: Yedekleme**. Bu prosedür şunları kullanır: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

## Geçerli olduğu alan

Bu sayfa yalnızca şunlar için geçerlidir: **Güncelleme ve geçiş: Yedekleme** (Docker · Unraid). Kanal ve veri sınırları: DiscVault · `ghcr.io/helmerznl/discvault:latest`; aynı yazıcıların durdurulduğu aralıktan PostgreSQL dökümü ve dosya arşivi; PostgreSQL `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; aynı yazıcıların durdurulduğu aralıktan PostgreSQL dökümü ve dosya arşivi; PostgreSQL `/data`. Sayfaya özel denetim noktaları: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

### DiscVault v26 kararlı

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- aynı yazıcıların durdurulduğu aralıktan PostgreSQL dökümü ve dosya arşivi
- PostgreSQL `/data`

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- aynı yazıcıların durdurulduğu aralıktan PostgreSQL dökümü ve dosya arşivi
- PostgreSQL `/data`

## Ön koşullar

- canlı veri yolunun dışında yedek hedefi
- yazılabilir kalıcı depolama ve boş yedek alanı

## Prosedür

1. **Kaydedin:** `PREVIOUS_IMAGE` · `sha256`
2. **Durdurun:** `next-api` `next-worker` `next-mcp`
3. **Yedekleyin:** `pg_dump -Fc`
4. **Başlatın:** `next-api` → → MCP

### Komutlar ve ayarlar

#### Her iki kanal

```bash
set -euo pipefail
cd /opt/discvault
install -d -m 0700 /srv/backups
BACKUP_ID="$(date -u +%Y%m%dT%H%M%SZ)"
API_ID="$(docker compose --env-file .env -p discvault -f compose.yaml ps -q next-api)"
IMAGE_ID="$(docker inspect "$API_ID" --format '{{.Image}}')"
PREVIOUS_IMAGE="$(docker image inspect "$IMAGE_ID" --format '{{index .RepoDigests 0}}')"
test -n "$PREVIOUS_IMAGE"
printf 'BACKUP_ID=%s\nPREVIOUS_IMAGE=%s\n' "$BACKUP_ID" "$PREVIOUS_IMAGE" \
  > "/srv/backups/discvault-$BACKUP_ID.env"
chmod 0600 "/srv/backups/discvault-$BACKUP_ID.env"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_dump -Fc -U "$POSTGRES_USER" "$POSTGRES_DB"' \
  > "/srv/backups/discvault-$BACKUP_ID.dump"
tar --acls --xattrs -C /srv -czf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz" discvault
docker compose --env-file .env -p discvault -f compose.yaml start next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml start next-worker next-mcp
test -s "/srv/backups/discvault-$BACKUP_ID.dump"
gzip -t "/srv/backups/discvault-data-$BACKUP_ID.tgz"
```

## Beklenen sonuç

Şunun sonucunu kabul edin: **Güncelleme ve geçiş: Yedekleme** yalnızca aşağıdaki sonuçların tümü doğru olduğunda: arşiv ve döküm boş değildir, geri yükleme provasında okunur ve değişmez bir imaj özetine bağlıdır. Sayfaya özel denetim noktaları: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- arşiv ve döküm boş değildir, geri yükleme provasında okunur ve değişmez bir imaj özetine bağlıdır

## Güvenlik ve geri alma

:::caution
Şunun güvenli kurtarma sınırı: **Güncelleme ve geçiş: Yedekleme** başlatmadan veya devam etmeden önce şu işlemleri gerektirir: herhangi bir yedek komutu başarısız olursa yalnızca değişmemiş hizmetleri yeniden başlatın ve güncellemeyi iptal edin; aynı yazıcıların durdurulduğu aralıktan PostgreSQL dökümü ve dosya arşivi. Sayfaya özel denetim noktaları: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- herhangi bir yedek komutu başarısız olursa yalnızca değişmemiş hizmetleri yeniden başlatın ve güncellemeyi iptal edin
- aynı yazıcıların durdurulduğu aralıktan PostgreSQL dökümü ve dosya arşivi
:::

## Sonraki adım

[Güncelleme ve geçiş: Geri yükleme](/tr/update/restore/)

## Kaynak ve durum

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Kaynak ve durum: `2026-07-14`
- DiscVault Docs: `0.1.0`
