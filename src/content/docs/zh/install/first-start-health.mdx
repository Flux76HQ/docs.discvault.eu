---
title: '安装: 首次启动和健康检查'
description: '任务专用说明： 安装: 首次启动和健康检查: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.'
pageId: 'install-first-start-health'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · 网页" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## 目标和结果

完成此页面专用的流程： **安装: 首次启动和健康检查**. 此流程使用 `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

## 适用范围

本页面仅适用于 **安装: 首次启动和健康检查** (Docker · 网页). 通道与数据边界为： DiscVault · `ghcr.io/helmerznl/discvault:latest`; `/api/next/auth/status`; `6080` → API `5000`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `/api/next/auth/status`; `6080` → API `5000`. 本页面专用检查点： `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

### DiscVault v26 稳定版

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `/api/next/auth/status`
- `6080` → API `5000`

### DiscVault v26 测试版

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `/api/next/auth/status`
- `6080` → API `5000`

## 前提条件

- `/api/next/health`
- `/api/next/auth/status`

## 操作步骤

1. **检查:** `postgres`
2. **验证:** `GET /api/next/health` · HTTP `2xx`
3. **打开:** `6080:5000`
4. **验证:** `GET /api/next/auth/status` · WebAuthn

### 命令和设置

#### 两个通道

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml ps
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml logs --tail=100 next-api next-worker next-mcp postgres
curl --fail http://localhost:6080/api/next/health
curl --fail http://localhost:6080/api/next/auth/status
```

## 预期结果

仅在以下条件下接受结果： **安装: 首次启动和健康检查** 必须确认以下所有结果： 健康检查成功，配置的来源可打开，且第一个账户成为所有者. 本页面专用检查点： `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

- 健康检查成功，配置的来源可打开，且第一个账户成为所有者

## 安全与回滚

:::caution
安全恢复边界： **安装: 首次启动和健康检查** 在启动或继续任何操作前必须执行： 已隐藏隐私数据且带时间戳的日志; 匹配的备份集. 本页面专用检查点： `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

- 已隐藏隐私数据且带时间戳的日志
- 匹配的备份集
:::

## 下一步

[更新和迁移: 备份](/zh/update/backup/)

## 来源和状态

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- 来源和状态: `2026-07-14`
- DiscVault Docs: `0.1.0`
