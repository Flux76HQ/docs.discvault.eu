---
title: 'Konfiguration: Miljövariabler'
description: 'Uppgiftsspecifik vägledning för Konfiguration: Miljövariabler: `/opt/discvault-next/.env` · `DISCVAULT_NEXT_IMAGE` · `DISCVAULT_NEXT_POSTGRES_DATA` · `chmod 0600` · `openssl rand -base64 48` · `RP_ORIGINS` · `LEGACY_AUTH_ENABLED`.'
pageId: 'configure-environment'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-20'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-20" />

## Mål och resultat

Följ det sidspecifika arbetsflödet för **Konfiguration: Miljövariabler**. Den här proceduren använder `/opt/discvault-next/.env` · `DISCVAULT_NEXT_IMAGE` · `DISCVAULT_NEXT_POSTGRES_DATA` · `chmod 0600` · `openssl rand -base64 48` · `RP_ORIGINS` · `LEGACY_AUTH_ENABLED`.

## Gäller för

Den här sidan gäller endast **Konfiguration: Miljövariabler** (Docker · Unraid). Kanal- och datagränserna är: DiscVault · `ghcr.io/helmerznl/discvault:beta`; `.env.example` `release/v26-beta`; `0600` · `JWT_SECRET` `POSTGRES_PASSWORD`. Sidspecifika kontrollpunkter: `/opt/discvault-next/.env` · `DISCVAULT_NEXT_IMAGE` · `DISCVAULT_NEXT_POSTGRES_DATA` · `chmod 0600` · `openssl rand -base64 48` · `RP_ORIGINS` · `LEGACY_AUTH_ENABLED`.

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `.env.example` `release/v26-beta`
- `0600` · `JWT_SECRET` `POSTGRES_PASSWORD`

## Förutsättningar

- privat DiscVault-körtidsfil
- OpenSSL
- planerat omstartsfönster

## Procedur

1. **Skapa:** `/opt/discvault-next/.env` · filbehörighet endast för ägaren (0600)
2. **Konfigurera:** `DISCVAULT_NEXT_IMAGE` · `RP_ORIGINS`
3. **Välj:** med ett giltigt FQDN ska `RP_ID` och `RP_ORIGINS` stämma och betrodd HTTPS användas · för en direkt privat eller loopback-IP utan FQDN ska `RP_ID` och `RP_ORIGINS` vara tomma
4. **Verifiera:** `chmod 0600` · `docker compose -p discvault_next_deploy config --quiet`
5. **Starta:** behåll JWT- och PostgreSQL-hemligheter vid vanliga konfigurationsändringar

### Kommandon och inställningar

#### DiscVault v26 – beta

```bash
set -euo pipefail
install -d -m 0700 /opt/discvault-next
cd /opt/discvault-next
curl --fail --location --proto '=https' --tlsv1.2 \
  --output docker-compose.yml \
  https://raw.githubusercontent.com/helmerzNL/DiscVault/release/v26-beta/app/deploy/next/docker-compose.yml
curl --fail --location --proto '=https' --tlsv1.2 \
  --output .env.example \
  https://raw.githubusercontent.com/helmerzNL/DiscVault/release/v26-beta/app/deploy/next/.env.example

umask 077
ENV_FILE=/opt/discvault-next/.env
test ! -e "$ENV_FILE"
ENV_TMP="$(mktemp "$ENV_FILE.tmp.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
POSTGRES_PASSWORD="$(openssl rand -hex 32)"
JWT_SECRET="$(openssl rand -base64 48)"
awk \
  -v image='ghcr.io/helmerznl/discvault:beta' \
  -v postgres_password="$POSTGRES_PASSWORD" \
  -v jwt_secret="$JWT_SECRET" \
  -v data_dir='/mnt/user/appdata/discvault' \
  -v postgres_dir='/mnt/user/appdata/discvault-next/postgres' '
  /^DISCVAULT_NEXT_IMAGE=/ { print "DISCVAULT_NEXT_IMAGE=" image; next }
  /^POSTGRES_PASSWORD=/ { print "POSTGRES_PASSWORD=" postgres_password; next }
  /^JWT_SECRET=/ { print "JWT_SECRET=" jwt_secret; next }
  /^DISCVAULT_DATA_DIR=/ { print "DISCVAULT_DATA_DIR=" data_dir; next }
  /^DISCVAULT_NEXT_POSTGRES_DATA=/ {
    print "DISCVAULT_NEXT_POSTGRES_DATA=" postgres_dir
    next
  }
  /^LEGACY_AUTH_ENABLED=/ { print "LEGACY_AUTH_ENABLED=true"; next }
  { print }
' .env.example > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
ln "$ENV_TMP" "$ENV_FILE"
rm -f "$ENV_TMP"
trap - EXIT
```

```bash
set -euo pipefail
cd /opt/discvault-next
FQDN=discvault.example.com
ENV_FILE=/opt/discvault-next/.env
ENV_TMP="$(mktemp "$ENV_FILE.tmp.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk -v rp_id="$FQDN" -v rp_origin="https://$FQDN" '
  /^RP_ID=/ { print "RP_ID=" rp_id; next }
  /^RP_NAME=/ { print "RP_NAME=DiscVault"; next }
  /^RP_ORIGINS=/ { print "RP_ORIGINS=" rp_origin; next }
  /^LEGACY_AUTH_ENABLED=/ { print "LEGACY_AUTH_ENABLED=true"; next }
  { print }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

```bash
set -euo pipefail
cd /opt/discvault-next
ENV_FILE=/opt/discvault-next/.env
ENV_TMP="$(mktemp "$ENV_FILE.tmp.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk '
  /^RP_ID=/ { print "RP_ID="; next }
  /^RP_NAME=/ { print "RP_NAME=DiscVault"; next }
  /^RP_ORIGINS=/ { print "RP_ORIGINS="; next }
  /^LEGACY_AUTH_ENABLED=/ { print "LEGACY_AUTH_ENABLED=true"; next }
  { print }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

```bash
set -euo pipefail
cd /opt/discvault-next
ENV_FILE=/opt/discvault-next/.env
test "$(stat -c '%a' "$ENV_FILE")" = 600
read_env() { sed -n "s/^$1=//p" "$ENV_FILE"; }

test "$(read_env DISCVAULT_NEXT_IMAGE)" = 'ghcr.io/helmerznl/discvault:beta'
! grep -q ':dev' "$ENV_FILE"
test "$(read_env LEGACY_AUTH_ENABLED)" = true
POSTGRES_PASSWORD_VALUE="$(read_env POSTGRES_PASSWORD)"
JWT_SECRET_VALUE="$(read_env JWT_SECRET)"
test "${#POSTGRES_PASSWORD_VALUE}" -ge 32
test "$POSTGRES_PASSWORD_VALUE" != 'change-me-to-a-long-random-password'
test "${#JWT_SECRET_VALUE}" -ge 48
grep -qx 'DISCVAULT_DATA_DIR=/mnt/user/appdata/discvault' "$ENV_FILE"
grep -qx 'DISCVAULT_NEXT_POSTGRES_DATA=/mnt/user/appdata/discvault-next/postgres' "$ENV_FILE"

RP_ID_VALUE="$(read_env RP_ID)"
RP_ORIGINS_VALUE="$(read_env RP_ORIGINS)"
if [ -z "$RP_ID_VALUE" ]; then
  test -z "$RP_ORIGINS_VALUE"
else
  test "$RP_ORIGINS_VALUE" = "https://$RP_ID_VALUE"
fi

docker compose -p discvault_next_deploy config --quiet
docker compose -p discvault_next_deploy pull
docker compose -p discvault_next_deploy up -d
docker compose -p discvault_next_deploy ps
curl --fail http://localhost:6180/api/next/health
```

## Förväntat resultat

Godkänn resultatet för **Konfiguration: Miljövariabler** endast när alla resultat nedan stämmer: filbehörighet endast för ägaren (0600); den privata filen har läge 600, Compose-valideringen lyckas och vald kanal blir frisk. Sidspecifika kontrollpunkter: `/opt/discvault-next/.env` · `DISCVAULT_NEXT_IMAGE` · `DISCVAULT_NEXT_POSTGRES_DATA` · `chmod 0600` · `openssl rand -base64 48` · `RP_ORIGINS` · `LEGACY_AUTH_ENABLED`.

- filbehörighet endast för ägaren (0600)
- den privata filen har läge 600, Compose-valideringen lyckas och vald kanal blir frisk

## Säkerhet och återställning

:::caution
Den säkra återställningsgränsen för **Konfiguration: Miljövariabler** kräver dessa åtgärder innan något startas eller fortsätter: dokumentationslagrets miljöfil är aldrig en DiscVault-körtidsfil · `.env.example`; behåll JWT- och PostgreSQL-hemligheter vid vanliga konfigurationsändringar · `JWT_SECRET` · `POSTGRES_PASSWORD`; hämtad `.env.example` använder fortfarande `:dev`; runtimefilen `.env` måste ändras till `:beta` före pull eller start; vid fel ska tjänsterna förbli stoppade; återställ och validera föregående privata miljöfil och återskapa sedan samma image utan att rotera hemligheter. Sidspecifika kontrollpunkter: `/opt/discvault-next/.env` · `DISCVAULT_NEXT_IMAGE` · `DISCVAULT_NEXT_POSTGRES_DATA` · `chmod 0600` · `openssl rand -base64 48` · `RP_ORIGINS` · `LEGACY_AUTH_ENABLED`.

- dokumentationslagrets miljöfil är aldrig en DiscVault-körtidsfil · `.env.example`
- behåll JWT- och PostgreSQL-hemligheter vid vanliga konfigurationsändringar · `JWT_SECRET` · `POSTGRES_PASSWORD`
- hämtad `.env.example` använder fortfarande `:dev`; runtimefilen `.env` måste ändras till `:beta` före pull eller start
- vid fel ska tjänsterna förbli stoppade; återställ och validera föregående privata miljöfil och återskapa sedan samma image utan att rotera hemligheter
:::

## Nästa steg

[Konfiguration: Autentisering och RBAC](/sv/configure/auth-rbac/)

## Källa och status

- [`helmerzNL/DiscVault@4352c060ccd6`](https://github.com/helmerzNL/DiscVault/commit/4352c060ccd6fd625a828f6e20c24f111c9ef743)
- [Moving beta source: `docker-compose.yml`](https://github.com/helmerzNL/DiscVault/blob/release/v26-beta/app/deploy/next/docker-compose.yml)
- [Raw beta download: `docker-compose.yml`](https://raw.githubusercontent.com/helmerzNL/DiscVault/release/v26-beta/app/deploy/next/docker-compose.yml)
- [Pinned verification: `docker-compose.yml`](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/deploy/next/docker-compose.yml)
- [Moving beta source: `.env.example`](https://github.com/helmerzNL/DiscVault/blob/release/v26-beta/app/deploy/next/.env.example)
- [Raw beta download: `.env.example`](https://raw.githubusercontent.com/helmerzNL/DiscVault/release/v26-beta/app/deploy/next/.env.example)
- [Pinned verification: `.env.example`](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/deploy/next/.env.example)
- Källa och status: `2026-07-20`
- DiscVault Docs: `0.1.6`
