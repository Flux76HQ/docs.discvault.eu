---
title: 'Konfiguration: Miljövariabler'
description: 'Uppgiftsspecifik vägledning för Konfiguration: Miljövariabler: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.'
pageId: 'configure-environment'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Mål och resultat

Följ det sidspecifika arbetsflödet för **Konfiguration: Miljövariabler**. Den här proceduren använder `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

## Gäller för

Den här sidan gäller endast **Konfiguration: Miljövariabler** (Docker · Unraid). Kanal- och datagränserna är: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `.env`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `.env`. Sidspecifika kontrollpunkter: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `.env`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `.env`

## Förutsättningar

- privat DiscVault-körtidsfil
- OpenSSL
- planerat omstartsfönster

## Procedur

1. **Skapa:** `/opt/discvault/.env` · filbehörighet endast för ägaren (0600)
2. **Konfigurera:** `DISCVAULT_IMAGE` · `RP_ORIGINS`
3. **Verifiera:** `chmod 0600` · `docker compose config --quiet`
4. **Starta:** behåll JWT- och PostgreSQL-hemligheter vid vanliga konfigurationsändringar

### Kommandon och inställningar

#### DiscVault v26 – stabil

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:latest
```

#### DiscVault v26 – beta

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:beta
```

#### Båda kanalerna

```bash
set -euo pipefail
test -n "$DISCVAULT_IMAGE"
case "$DISCVAULT_IMAGE" in
  ghcr.io/helmerznl/discvault:latest|ghcr.io/helmerznl/discvault:beta) ;;
  *) printf '%s\n' "$DISCVAULT_IMAGE" >&2; exit 64 ;;
esac
install -d -m 0700 /opt/discvault
install -d -m 0750 /srv/discvault
install -d -o 70 -g 70 -m 0700 /srv/discvault-postgres
cd /opt/discvault
test -f compose.yaml
umask 077
ENV_FILE=/opt/discvault/.env
test ! -e "$ENV_FILE"
ENV_TMP="$(mktemp "$ENV_FILE.tmp.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
POSTGRES_PASSWORD="$(openssl rand -hex 32)"
JWT_SECRET="$(openssl rand -hex 32)"
printf '%s\n' \
  "DISCVAULT_IMAGE=$DISCVAULT_IMAGE" \
  'DISCVAULT_DATA_DIR=/srv/discvault' \
  'DISCVAULT_POSTGRES_DATA=/srv/discvault-postgres' \
  'DISCVAULT_API_PORT=6080' \
  'DISCVAULT_MCP_PORT=6090' \
  'DISCVAULT_NETWORK_NAME=discvault' \
  'DISCVAULT_API_WORKERS=2' \
  'DISCVAULT_API_TIMEOUT=180' \
  'DISCVAULT_WORKER_ID=worker-1' \
  'DISCVAULT_WORKER_POLL_INTERVAL=2' \
  'POSTGRES_DB=discvault' \
  'POSTGRES_USER=discvault' \
  "POSTGRES_PASSWORD=$POSTGRES_PASSWORD" \
  "JWT_SECRET=$JWT_SECRET" \
  'TZ=Europe/Amsterdam' \
  'RP_ID=localhost' \
  'RP_NAME=DiscVault' \
  'RP_ORIGINS=http://localhost:6080' > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
ln "$ENV_TMP" "$ENV_FILE"
rm -f "$ENV_TMP"
trap - EXIT
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml config --quiet
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml pull
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml up -d
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml ps
curl --fail http://localhost:6080/api/next/health
```

## Förväntat resultat

Godkänn resultatet för **Konfiguration: Miljövariabler** endast när alla resultat nedan stämmer: filbehörighet endast för ägaren (0600); den privata filen har läge 600, Compose-valideringen lyckas och vald kanal blir frisk. Sidspecifika kontrollpunkter: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

- filbehörighet endast för ägaren (0600)
- den privata filen har läge 600, Compose-valideringen lyckas och vald kanal blir frisk

## Säkerhet och återställning

:::caution
Den säkra återställningsgränsen för **Konfiguration: Miljövariabler** kräver dessa åtgärder innan något startas eller fortsätter: dokumentationslagrets miljöfil är aldrig en DiscVault-körtidsfil · `.env.example`; behåll JWT- och PostgreSQL-hemligheter vid vanliga konfigurationsändringar · `JWT_SECRET` · `POSTGRES_PASSWORD`; vid fel ska tjänsterna förbli stoppade; återställ och validera föregående privata miljöfil och återskapa sedan samma image utan att rotera hemligheter. Sidspecifika kontrollpunkter: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

- dokumentationslagrets miljöfil är aldrig en DiscVault-körtidsfil · `.env.example`
- behåll JWT- och PostgreSQL-hemligheter vid vanliga konfigurationsändringar · `JWT_SECRET` · `POSTGRES_PASSWORD`
- vid fel ska tjänsterna förbli stoppade; återställ och validera föregående privata miljöfil och återskapa sedan samma image utan att rotera hemligheter
:::

## Nästa steg

[Konfiguration: Autentisering och RBAC](/sv/configure/auth-rbac/)

## Källa och status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Källa och status: `2026-07-14`
- DiscVault Docs: `0.1.0`
