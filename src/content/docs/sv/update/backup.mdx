---
title: 'Uppdatering och migrering: Säkerhetskopia'
description: 'Uppgiftsspecifik vägledning för Uppdatering och migrering: Säkerhetskopia: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.'
pageId: 'update-backup'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Mål och resultat

Följ det sidspecifika arbetsflödet för **Uppdatering och migrering: Säkerhetskopia**. Den här proceduren använder `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

## Gäller för

Den här sidan gäller endast **Uppdatering och migrering: Säkerhetskopia** (Docker · Unraid). Kanal- och datagränserna är: DiscVault · `ghcr.io/helmerznl/discvault:latest`; PostgreSQL-dump och filarkiv från samma stoppade skrivfönster; PostgreSQL `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; PostgreSQL-dump och filarkiv från samma stoppade skrivfönster; PostgreSQL `/data`. Sidspecifika kontrollpunkter: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- PostgreSQL-dump och filarkiv från samma stoppade skrivfönster
- PostgreSQL `/data`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- PostgreSQL-dump och filarkiv från samma stoppade skrivfönster
- PostgreSQL `/data`

## Förutsättningar

- mål utanför den aktiva datasökvägen
- skrivbar beständig lagring och ledigt säkerhetskopieutrymme

## Procedur

1. **Anteckna:** `PREVIOUS_IMAGE` · `sha256`
2. **Stoppa:** `next-api` `next-worker` `next-mcp`
3. **Säkerhetskopiera:** `pg_dump -Fc`
4. **Starta:** `next-api` → → MCP

### Kommandon och inställningar

#### Båda kanalerna

```bash
set -euo pipefail
cd /opt/discvault
install -d -m 0700 /srv/backups
BACKUP_ID="$(date -u +%Y%m%dT%H%M%SZ)"
API_ID="$(docker compose --env-file .env -p discvault -f compose.yaml ps -q next-api)"
IMAGE_ID="$(docker inspect "$API_ID" --format '{{.Image}}')"
PREVIOUS_IMAGE="$(docker image inspect "$IMAGE_ID" --format '{{index .RepoDigests 0}}')"
test -n "$PREVIOUS_IMAGE"
printf 'BACKUP_ID=%s\nPREVIOUS_IMAGE=%s\n' "$BACKUP_ID" "$PREVIOUS_IMAGE" \
  > "/srv/backups/discvault-$BACKUP_ID.env"
chmod 0600 "/srv/backups/discvault-$BACKUP_ID.env"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_dump -Fc -U "$POSTGRES_USER" "$POSTGRES_DB"' \
  > "/srv/backups/discvault-$BACKUP_ID.dump"
tar --acls --xattrs -C /srv -czf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz" discvault
docker compose --env-file .env -p discvault -f compose.yaml start next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml start next-worker next-mcp
test -s "/srv/backups/discvault-$BACKUP_ID.dump"
gzip -t "/srv/backups/discvault-data-$BACKUP_ID.tgz"
```

## Förväntat resultat

Godkänn resultatet för **Uppdatering och migrering: Säkerhetskopia** endast när alla resultat nedan stämmer: arkiv och dump är inte tomma, kan läsas i en återställningsövning och är knutna till en oföränderlig image-digest. Sidspecifika kontrollpunkter: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- arkiv och dump är inte tomma, kan läsas i en återställningsövning och är knutna till en oföränderlig image-digest

## Säkerhet och återställning

:::caution
Den säkra återställningsgränsen för **Uppdatering och migrering: Säkerhetskopia** kräver dessa åtgärder innan något startas eller fortsätter: om ett säkerhetskopieringskommando misslyckas, starta bara oförändrade tjänster och avbryt uppdateringen; PostgreSQL-dump och filarkiv från samma stoppade skrivfönster. Sidspecifika kontrollpunkter: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- om ett säkerhetskopieringskommando misslyckas, starta bara oförändrade tjänster och avbryt uppdateringen
- PostgreSQL-dump och filarkiv från samma stoppade skrivfönster
:::

## Nästa steg

[Uppdatering och migrering: Återställning](/sv/update/restore/)

## Källa och status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Källa och status: `2026-07-14`
- DiscVault Docs: `0.1.0`
