---
title: 'Uppdatering och migrering: Återställning av image'
description: 'Uppgiftsspecifik vägledning för Uppdatering och migrering: Återställning av image: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.'
pageId: 'update-rollback'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Mål och resultat

Följ det sidspecifika arbetsflödet för **Uppdatering och migrering: Återställning av image**. Den här proceduren använder `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

## Gäller för

Den här sidan gäller endast **Uppdatering och migrering: Återställning av image** (Docker · Unraid). Kanal- och datagränserna är: DiscVault · `ghcr.io/helmerznl/discvault:latest`; återställ matchande data innan föregående image startas; PostgreSQL `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; återställ matchande data innan föregående image startas; PostgreSQL `/data`. Sidspecifika kontrollpunkter: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- återställ matchande data innan föregående image startas
- PostgreSQL `/data`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- återställ matchande data innan föregående image startas
- PostgreSQL `/data`

## Förutsättningar

- matchande säkerhetskopieuppsättning
- `PREVIOUS_IMAGE`

## Procedur

1. **Stoppa:** `next-api` `next-worker` `next-mcp`
2. **Återställ:** `/data`
3. **Återställ:** PostgreSQL · `pg_restore --exit-on-error`
4. **Starta:** `PREVIOUS_IMAGE` → API → MCP

### Kommandon och inställningar

#### Båda kanalerna

```bash
set -euo pipefail
BACKUP_ID=20260714T180000Z
cd /opt/discvault
. "/srv/backups/discvault-$BACKUP_ID.env"
test -n "$PREVIOUS_IMAGE"
export DISCVAULT_IMAGE="$PREVIOUS_IMAGE"
docker pull "$DISCVAULT_IMAGE"
ENV_FILE=/opt/discvault/.env
ENV_TMP="$(mktemp "$ENV_FILE.restore.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk -v image="$DISCVAULT_IMAGE" '
  /^DISCVAULT_IMAGE=/ { print "DISCVAULT_IMAGE=" image; found=1; next }
  { print }
  END { if (!found) exit 42 }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
mv /srv/discvault "/srv/discvault.failed-$(date -u +%Y%m%dT%H%M%SZ)"
tar --acls --xattrs -C /srv -xzf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz"
docker compose --env-file .env -p discvault -f compose.yaml up -d postgres
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'dropdb --if-exists --force -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'createdb -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_restore --exit-on-error --no-owner -U "$POSTGRES_USER" -d "$POSTGRES_DB"' \
  < "/srv/backups/discvault-$BACKUP_ID.dump"
docker compose --env-file .env -p discvault -f compose.yaml up -d --force-recreate next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml \
  up -d --force-recreate next-worker next-mcp
docker compose --env-file .env -p discvault -f compose.yaml ps
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

## Förväntat resultat

Godkänn resultatet för **Uppdatering och migrering: Återställning av image** endast när alla resultat nedan stämmer: `PREVIOUS_IMAGE` · `@sha256:`; återställ matchande data innan föregående image startas; biblioteksantal och medieprov matchar vald säkerhetskopia. Sidspecifika kontrollpunkter: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

- `PREVIOUS_IMAGE` · `@sha256:`
- återställ matchande data innan föregående image startas
- biblioteksantal och medieprov matchar vald säkerhetskopia

## Säkerhet och återställning

:::caution
Den säkra återställningsgränsen för **Uppdatering och migrering: Återställning av image** kräver dessa åtgärder innan något startas eller fortsätter: om filextrahering eller pg_restore misslyckas ska container, API, workers och MCP förbli stoppade; försök igen från orörda säkerhetskopior. Sidspecifika kontrollpunkter: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

- om filextrahering eller pg_restore misslyckas ska container, API, workers och MCP förbli stoppade; försök igen från orörda säkerhetskopior
:::

## Nästa steg

[Uppdatering och migrering: Migrering till DiscVault 26](/sv/update/migration/)

## Källa och status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Källa och status: `2026-07-14`
- DiscVault Docs: `0.1.0`
