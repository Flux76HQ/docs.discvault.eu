---
title: 'Installation: Omvänd proxy och åtkomstnycklar'
description: 'Uppgiftsspecifik vägledning för Installation: Omvänd proxy och åtkomstnycklar: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.'
pageId: 'install-reverse-proxy-passkeys'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · webb" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Mål och resultat

Följ det sidspecifika arbetsflödet för **Installation: Omvänd proxy och åtkomstnycklar**. Den här proceduren använder `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

## Gäller för

Den här sidan gäller endast **Installation: Omvänd proxy och åtkomstnycklar** (Docker · webb). Kanal- och datagränserna är: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `RP_ID` `RP_ORIGINS`; `6080` API `5000`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `RP_ID` `RP_ORIGINS`; `6080` API `5000`. Sidspecifika kontrollpunkter: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `RP_ID` `RP_ORIGINS`
- `6080` API `5000`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `RP_ID` `RP_ORIGINS`
- `6080` API `5000`

## Förutsättningar

- DNS
- TLS
- `6080`

## Procedur

1. **Konfigurera:** `RP_ID`
2. **Konfigurera:** `RP_ORIGINS` · HTTPS
3. **Konfigurera:** `http://127.0.0.1:6080`
4. **Testa:** `GET /api/next/health`

### Kommandon och inställningar

#### Båda kanalerna

```text
RP_ID=discvault.example.com
RP_ORIGINS=https://discvault.example.com
curl --fail https://discvault.example.com/api/next/health
```

## Förväntat resultat

Godkänn resultatet för **Installation: Omvänd proxy och åtkomstnycklar** endast när alla resultat nedan stämmer: HTTPS · HTTP `2xx`; `RP_ID`. Sidspecifika kontrollpunkter: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

- HTTPS · HTTP `2xx`
- `RP_ID`

## Säkerhet och återställning

:::caution
Den säkra återställningsgränsen för **Installation: Omvänd proxy och åtkomstnycklar** kräver dessa åtgärder innan något startas eller fortsätter: återställ proxy- och RP-värden tillsammans och behåll en återställningsmetod för ägaren. Sidspecifika kontrollpunkter: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

- återställ proxy- och RP-värden tillsammans och behåll en återställningsmetod för ägaren
:::

## Nästa steg

[Installation: Första start och hälsokontroll](/sv/install/first-start-health/)

## Källa och status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Källa och status: `2026-07-14`
- DiscVault Docs: `0.1.0`
