---
title: 'Installation: Persistente Daten und PostgreSQL'
description: 'Aufgabenspezifische Anleitung für Installation: Persistente Daten und PostgreSQL: `DISCVAULT_POSTGRES_DATA` · `DISCVAULT_DATA_DIR` · `postgres` · `next-api`.'
pageId: 'install-storage-postgresql'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Ziel und Ergebnis

Führe den seitenspezifischen Ablauf aus für **Installation: Persistente Daten und PostgreSQL**. Dieses Verfahren arbeitet mit `DISCVAULT_POSTGRES_DATA` · `DISCVAULT_DATA_DIR` · `postgres` · `next-api`.

## Gültig für

Diese Seite gilt ausschließlich für **Installation: Persistente Daten und PostgreSQL** (Docker · Unraid). Kanal- und Datengrenzen sind: DiscVault · `ghcr.io/helmerznl/discvault:latest`; PostgreSQL; `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; PostgreSQL; `/data`. Seitenspezifische Kontrollpunkte: `DISCVAULT_POSTGRES_DATA` · `DISCVAULT_DATA_DIR` · `postgres` · `next-api`.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- PostgreSQL
- `/data`

### DiscVault v26 – Beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- PostgreSQL
- `/data`

## Voraussetzungen

- `DISCVAULT_POSTGRES_DATA`
- Sicherungsziel außerhalb des Live-Datenpfads
- PostgreSQL `17`

## Vorgehen

1. **Prüfen:** `DISCVAULT_POSTGRES_DATA` · `/var/lib/postgresql/data`
2. **Prüfen:** `DISCVAULT_DATA_DIR` · `/data`
3. **Verifizieren:** `postgres` · `healthy` · `next-api`
4. **Festhalten:** PostgreSQL-Dump und Dateisystemarchiv aus demselben schreibfreien Fenster

### Befehle und Einstellungen

#### Beide Kanäle

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml ps
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml exec -T postgres sh -c 'pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"'
docker inspect discvault-next-api-1 --format '{{range .Mounts}}{{.Source}} -> {{.Destination}}{{println}}{{end}}'
```

## Erwartetes Ergebnis

Akzeptiere das Ergebnis für **Installation: Persistente Daten und PostgreSQL** nur wenn alle folgenden Ergebnisse erfüllt sind: PostgreSQL · `healthy`; `/data`; passender Sicherungssatz. Seitenspezifische Kontrollpunkte: `DISCVAULT_POSTGRES_DATA` · `DISCVAULT_DATA_DIR` · `postgres` · `next-api`.

- PostgreSQL · `healthy`
- `/data`
- passender Sicherungssatz

## Sicherheit und Rollback

:::caution
Die sichere Wiederherstellungsgrenze für **Installation: Persistente Daten und PostgreSQL** verlangt vor jedem Start oder Fortsetzen diese Maßnahmen: PostgreSQL und `/data` sind getrennte Speicher und benötigen einen passenden Sicherungssatz; Sicherungsziel außerhalb des Live-Datenpfads. Seitenspezifische Kontrollpunkte: `DISCVAULT_POSTGRES_DATA` · `DISCVAULT_DATA_DIR` · `postgres` · `next-api`.

- PostgreSQL und `/data` sind getrennte Speicher und benötigen einen passenden Sicherungssatz
- Sicherungsziel außerhalb des Live-Datenpfads
:::

## Nächster Schritt

[Installation: Reverse-Proxy und Passkeys](/de/install/reverse-proxy-passkeys/)

## Quelle und Status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Quelle und Status: `2026-07-14`
- DiscVault Docs: `0.1.0`
