---
title: 'Installation: Reverse-Proxy und Passkeys'
description: 'Aufgabenspezifische Anleitung für Installation: Reverse-Proxy und Passkeys: FQDN · `RP_ORIGINS` · `RP_ID` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.'
pageId: 'install-reverse-proxy-passkeys'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault', 'helmerzNL/DiscVault.EU']
lastVerified: '2026-07-20'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Web" version="DiscVault v26" source="helmerzNL/DiscVault · helmerzNL/DiscVault.EU" verified="2026-07-20" />

## Ziel und Ergebnis

Führe den seitenspezifischen Ablauf aus für **Installation: Reverse-Proxy und Passkeys**. Dieses Verfahren arbeitet mit FQDN · `RP_ORIGINS` · `RP_ID` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

## Gültig für

Diese Seite gilt ausschließlich für **Installation: Reverse-Proxy und Passkeys** (Docker · Web). Kanal- und Datengrenzen sind: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `RP_ID` `RP_ORIGINS` · `6080`; außerhalb der localhost-Ausnahme benötigen Passkeys einen stabilen vollqualifizierten Domainnamen über vertrauenswürdiges HTTPS; eine reine IP-Adresse oder unverschlüsseltes HTTP wird nicht unterstützt; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `RP_ID` `RP_ORIGINS` · `6180`; `LEGACY_AUTH_ENABLED=true`. Seitenspezifische Kontrollpunkte: FQDN · `RP_ORIGINS` · `RP_ID` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `RP_ID` `RP_ORIGINS` · `6080`
- außerhalb der localhost-Ausnahme benötigen Passkeys einen stabilen vollqualifizierten Domainnamen über vertrauenswürdiges HTTPS; eine reine IP-Adresse oder unverschlüsseltes HTTP wird nicht unterstützt

### DiscVault v26 – Beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `RP_ID` `RP_ORIGINS` · `6180`
- `LEGACY_AUTH_ENABLED=true`

## Voraussetzungen

- FQDN
- `RP_ORIGINS`
- `RP_ID`

### Legacy Authentication als Fallback

:::note[In Beta verfügbar]
Legacy Authentication mit Benutzername, Passwort und Zwei-Faktor-Authentifizierung (2FA) ist in DiscVault 26 Beta als Fallback verfügbar, wenn Passkeys nicht verwendet werden können.

Vor Aktivierung der Funktion oder Erstellung des ersten Owners `/install/legacy-authentication/` befolgen.
:::

## Vorgehen

1. **Auswählen:** FQDN · außerhalb der localhost-Ausnahme benötigen Passkeys einen stabilen vollqualifizierten Domainnamen über vertrauenswürdiges HTTPS; eine reine IP-Adresse oder unverschlüsseltes HTTP wird nicht unterstützt
2. **Auswählen:** [Installation: Legacy Authentication](/de/install/legacy-authentication/) · bei direkter privater oder Loopback-IP ohne FQDN `RP_ID` und `RP_ORIGINS` leer lassen
3. **Konfigurieren:** `RP_ID`
4. **Konfigurieren:** `RP_ORIGINS` · HTTPS
5. **Konfigurieren:** `6080` `6180`
6. **Testen:** `GET /api/next/health`
7. **Festhalten:** Hostname vor der Passkey-Registrierung wählen; eine spätere Änderung erfordert eine erneute Registrierung, localhost bleibt nur eine lokale Setup-Ausnahme

### Befehle und Einstellungen

#### DiscVault v26 – stabil

```text
RP_ID=discvault.example.com
RP_NAME=DiscVault
RP_ORIGINS=https://discvault.example.com
curl --fail https://discvault.example.com/api/next/health
```

#### DiscVault v26 – Beta

```text
RP_ID=discvault.example.com
RP_NAME=DiscVault
RP_ORIGINS=https://discvault.example.com
LEGACY_AUTH_ENABLED=true
curl --fail http://localhost:6180/api/next/health
```

## Erwartetes Ergebnis

Akzeptiere das Ergebnis für **Installation: Reverse-Proxy und Passkeys** nur wenn alle folgenden Ergebnisse erfüllt sind: HTTPS · HTTP `2xx`; `RP_ID`; `RP_ID` ist der Hostname ohne Schema oder Port, `RP_ORIGINS` die exakte vollständige HTTPS-Origin; beide müssen zur im Browser geöffneten Adresse passen. Seitenspezifische Kontrollpunkte: FQDN · `RP_ORIGINS` · `RP_ID` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

- HTTPS · HTTP `2xx`
- `RP_ID`
- `RP_ID` ist der Hostname ohne Schema oder Port, `RP_ORIGINS` die exakte vollständige HTTPS-Origin; beide müssen zur im Browser geöffneten Adresse passen

## Sicherheit und Rollback

:::caution
Die sichere Wiederherstellungsgrenze für **Installation: Reverse-Proxy und Passkeys** verlangt vor jedem Start oder Fortsetzen diese Maßnahmen: Hostname vor der Passkey-Registrierung wählen; eine spätere Änderung erfordert eine erneute Registrierung, localhost bleibt nur eine lokale Setup-Ausnahme; nur eine direkte private oder Loopback-Anfrage mit ungültigen oder leeren Passkey-RP-Werten darf Bootstrap ohne TOTP anbieten; für Benutzername, Passwort und 2FA TOTP aktiviert lassen; Proxy- und RP-Werte gemeinsam wiederherstellen und eine Owner-Wiederherstellung behalten. Seitenspezifische Kontrollpunkte: FQDN · `RP_ORIGINS` · `RP_ID` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

- Hostname vor der Passkey-Registrierung wählen; eine spätere Änderung erfordert eine erneute Registrierung, localhost bleibt nur eine lokale Setup-Ausnahme
- nur eine direkte private oder Loopback-Anfrage mit ungültigen oder leeren Passkey-RP-Werten darf Bootstrap ohne TOTP anbieten; für Benutzername, Passwort und 2FA TOTP aktiviert lassen
- Proxy- und RP-Werte gemeinsam wiederherstellen und eine Owner-Wiederherstellung behalten
:::

## Nächster Schritt

[Installation: Erster Start und Zustandsprüfung](/de/install/first-start-health/)

## Quelle und Status

- [`helmerzNL/DiscVault@4352c060ccd6`](https://github.com/helmerzNL/DiscVault/commit/4352c060ccd6fd625a828f6e20c24f111c9ef743)
- [`helmerzNL/DiscVault.EU@583f85c55dc2`](https://github.com/helmerzNL/DiscVault.EU/commit/583f85c55dc22b63368f997a3b076a093ca0afa1)
- [Moving beta source: Legacy Authentication deployment notes](https://github.com/helmerzNL/DiscVault/blob/release/v26-beta/app/deploy/next/README.md)
- [Pinned verification: `next_auth.py`](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/backend/next_auth.py)
- [Pinned verification: `next_legacy_auth.py`](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/backend/next_legacy_auth.py)
- [Pinned verification: Legacy Authentication schema](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/backend/migrations_next/041_legacy_auth.sql)
- [Pinned verification: unified recovery codes](https://github.com/helmerzNL/DiscVault/blob/4352c060ccd6fd625a828f6e20c24f111c9ef743/app/backend/migrations_next/042_unified_recovery_codes.sql)
- Quelle und Status: `2026-07-20`
- DiscVault Docs: `0.1.6`
