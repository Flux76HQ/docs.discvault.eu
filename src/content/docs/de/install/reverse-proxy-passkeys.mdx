---
title: 'Installation: Reverse-Proxy und Passkeys'
description: 'Aufgabenspezifische Anleitung für Installation: Reverse-Proxy und Passkeys: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.'
pageId: 'install-reverse-proxy-passkeys'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Web" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Ziel und Ergebnis

Führe den seitenspezifischen Ablauf aus für **Installation: Reverse-Proxy und Passkeys**. Dieses Verfahren arbeitet mit `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

## Gültig für

Diese Seite gilt ausschließlich für **Installation: Reverse-Proxy und Passkeys** (Docker · Web). Kanal- und Datengrenzen sind: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `RP_ID` `RP_ORIGINS`; `6080` API `5000`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `RP_ID` `RP_ORIGINS`; `6080` API `5000`. Seitenspezifische Kontrollpunkte: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `RP_ID` `RP_ORIGINS`
- `6080` API `5000`

### DiscVault v26 – Beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `RP_ID` `RP_ORIGINS`
- `6080` API `5000`

## Voraussetzungen

- DNS
- TLS
- `6080`

## Vorgehen

1. **Konfigurieren:** `RP_ID`
2. **Konfigurieren:** `RP_ORIGINS` · HTTPS
3. **Konfigurieren:** `http://127.0.0.1:6080`
4. **Testen:** `GET /api/next/health`

### Befehle und Einstellungen

#### Beide Kanäle

```text
RP_ID=discvault.example.com
RP_ORIGINS=https://discvault.example.com
curl --fail https://discvault.example.com/api/next/health
```

## Erwartetes Ergebnis

Akzeptiere das Ergebnis für **Installation: Reverse-Proxy und Passkeys** nur wenn alle folgenden Ergebnisse erfüllt sind: HTTPS · HTTP `2xx`; `RP_ID`. Seitenspezifische Kontrollpunkte: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

- HTTPS · HTTP `2xx`
- `RP_ID`

## Sicherheit und Rollback

:::caution
Die sichere Wiederherstellungsgrenze für **Installation: Reverse-Proxy und Passkeys** verlangt vor jedem Start oder Fortsetzen diese Maßnahmen: Proxy- und RP-Werte gemeinsam wiederherstellen und eine Owner-Wiederherstellung behalten. Seitenspezifische Kontrollpunkte: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

- Proxy- und RP-Werte gemeinsam wiederherstellen und eine Owner-Wiederherstellung behalten
:::

## Nächster Schritt

[Installation: Erster Start und Zustandsprüfung](/de/install/first-start-health/)

## Quelle und Status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Quelle und Status: `2026-07-14`
- DiscVault Docs: `0.1.0`
