---
title: 'Update und Migration: Wiederherstellung'
description: 'Aufgabenspezifische Anleitung für Update und Migration: Wiederherstellung: `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.'
pageId: 'update-restore'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Ziel und Ergebnis

Führe den seitenspezifischen Ablauf aus für **Update und Migration: Wiederherstellung**. Dieses Verfahren arbeitet mit `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.

## Gültig für

Diese Seite gilt ausschließlich für **Update und Migration: Wiederherstellung** (Docker · Unraid). Kanal- und Datengrenzen sind: DiscVault · `ghcr.io/helmerznl/discvault:latest`; PostgreSQL-Dump und Dateisystemarchiv aus demselben schreibfreien Fenster; passende Daten vor dem Start des vorherigen Images wiederherstellen; DiscVault · `ghcr.io/helmerznl/discvault:beta`; PostgreSQL-Dump und Dateisystemarchiv aus demselben schreibfreien Fenster; passende Daten vor dem Start des vorherigen Images wiederherstellen. Seitenspezifische Kontrollpunkte: `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- PostgreSQL-Dump und Dateisystemarchiv aus demselben schreibfreien Fenster
- passende Daten vor dem Start des vorherigen Images wiederherstellen

### DiscVault v26 – Beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- PostgreSQL-Dump und Dateisystemarchiv aus demselben schreibfreien Fenster
- passende Daten vor dem Start des vorherigen Images wiederherstellen

## Voraussetzungen

- passender Sicherungssatz
- Wartungsfenster
- `PREVIOUS_IMAGE` · private DiscVault-Laufzeitdatei

## Vorgehen

1. **Stoppen:** `next-api` `next-worker` `next-mcp`
2. **Wiederherstellen:** `/data`
3. **Wiederherstellen:** PostgreSQL · `pg_restore --exit-on-error`
4. **Starten:** `next-api` → `GET /api/next/health` → `next-worker` `next-mcp`

### Befehle und Einstellungen

#### Beide Kanäle

```bash
set -euo pipefail
BACKUP_ID=20260714T180000Z
cd /opt/discvault
. "/srv/backups/discvault-$BACKUP_ID.env"
test -n "$PREVIOUS_IMAGE"
export DISCVAULT_IMAGE="$PREVIOUS_IMAGE"
docker pull "$DISCVAULT_IMAGE"
ENV_FILE=/opt/discvault/.env
ENV_TMP="$(mktemp "$ENV_FILE.restore.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk -v image="$DISCVAULT_IMAGE" '
  /^DISCVAULT_IMAGE=/ { print "DISCVAULT_IMAGE=" image; found=1; next }
  { print }
  END { if (!found) exit 42 }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
mv /srv/discvault "/srv/discvault.failed-$(date -u +%Y%m%dT%H%M%SZ)"
tar --acls --xattrs -C /srv -xzf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz"
docker compose --env-file .env -p discvault -f compose.yaml up -d postgres
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'dropdb --if-exists --force -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'createdb -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_restore --exit-on-error --no-owner -U "$POSTGRES_USER" -d "$POSTGRES_DB"' \
  < "/srv/backups/discvault-$BACKUP_ID.dump"
docker compose --env-file .env -p discvault -f compose.yaml up -d --force-recreate next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml \
  up -d --force-recreate next-worker next-mcp
docker compose --env-file .env -p discvault -f compose.yaml ps
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

## Erwartetes Ergebnis

Akzeptiere das Ergebnis für **Update und Migration: Wiederherstellung** nur wenn alle folgenden Ergebnisse erfüllt sind: Bibliothekszahlen und Medienprobe entsprechen der gewählten Sicherung; `:latest` `:beta`. Seitenspezifische Kontrollpunkte: `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.

- Bibliothekszahlen und Medienprobe entsprechen der gewählten Sicherung
- `:latest` `:beta`

## Sicherheit und Rollback

:::caution
Die sichere Wiederherstellungsgrenze für **Update und Migration: Wiederherstellung** verlangt vor jedem Start oder Fortsetzen diese Maßnahmen: wenn Dateiwiederherstellung oder pg_restore fehlschlägt, bleiben Container, API, Worker und MCP gestoppt; erneut aus unveränderten Sicherungskopien versuchen. Seitenspezifische Kontrollpunkte: `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.

- wenn Dateiwiederherstellung oder pg_restore fehlschlägt, bleiben Container, API, Worker und MCP gestoppt; erneut aus unveränderten Sicherungskopien versuchen
:::

## Nächster Schritt

[Update und Migration: Container-Update](/de/update/update/)

## Quelle und Status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Quelle und Status: `2026-07-14`
- DiscVault Docs: `0.1.0`
