---
title: 'Oppdatering og migrering: Tilbakerulling av image'
description: 'Oppgavespesifikk veiledning for Oppdatering og migrering: Tilbakerulling av image: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.'
pageId: 'update-rollback'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Mål og resultat

Fullfør den sidespesifikke arbeidsflyten for **Oppdatering og migrering: Tilbakerulling av image**. Denne fremgangsmåten bruker `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

## Gjelder for

Denne siden gjelder bare **Oppdatering og migrering: Tilbakerulling av image** (Docker · Unraid). Kanal- og datagrensene er: DiscVault · `ghcr.io/helmerznl/discvault:latest`; gjenopprett tilhørende data før forrige image startes; PostgreSQL `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; gjenopprett tilhørende data før forrige image startes; PostgreSQL `/data`. Sidespesifikke kontrollpunkter: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- gjenopprett tilhørende data før forrige image startes
- PostgreSQL `/data`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- gjenopprett tilhørende data før forrige image startes
- PostgreSQL `/data`

## Forutsetninger

- tilhørende sikkerhetskopisett
- `PREVIOUS_IMAGE`

## Fremgangsmåte

1. **Stopp:** `next-api` `next-worker` `next-mcp`
2. **Gjenopprett:** `/data`
3. **Gjenopprett:** PostgreSQL · `pg_restore --exit-on-error`
4. **Start:** `PREVIOUS_IMAGE` → API → MCP

### Kommandoer og innstillinger

#### Begge kanaler

```bash
set -euo pipefail
BACKUP_ID=20260714T180000Z
cd /opt/discvault
. "/srv/backups/discvault-$BACKUP_ID.env"
test -n "$PREVIOUS_IMAGE"
export DISCVAULT_IMAGE="$PREVIOUS_IMAGE"
docker pull "$DISCVAULT_IMAGE"
ENV_FILE=/opt/discvault/.env
ENV_TMP="$(mktemp "$ENV_FILE.restore.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk -v image="$DISCVAULT_IMAGE" '
  /^DISCVAULT_IMAGE=/ { print "DISCVAULT_IMAGE=" image; found=1; next }
  { print }
  END { if (!found) exit 42 }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
mv /srv/discvault "/srv/discvault.failed-$(date -u +%Y%m%dT%H%M%SZ)"
tar --acls --xattrs -C /srv -xzf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz"
docker compose --env-file .env -p discvault -f compose.yaml up -d postgres
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'dropdb --if-exists --force -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'createdb -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_restore --exit-on-error --no-owner -U "$POSTGRES_USER" -d "$POSTGRES_DB"' \
  < "/srv/backups/discvault-$BACKUP_ID.dump"
docker compose --env-file .env -p discvault -f compose.yaml up -d --force-recreate next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml \
  up -d --force-recreate next-worker next-mcp
docker compose --env-file .env -p discvault -f compose.yaml ps
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

## Forventet resultat

Godta resultatet for **Oppdatering og migrering: Tilbakerulling av image** bare når alle resultatene nedenfor er oppfylt: `PREVIOUS_IMAGE` · `@sha256:`; gjenopprett tilhørende data før forrige image startes; bibliotektall og medieutvalg samsvarer med valgt sikkerhetskopi. Sidespesifikke kontrollpunkter: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

- `PREVIOUS_IMAGE` · `@sha256:`
- gjenopprett tilhørende data før forrige image startes
- bibliotektall og medieutvalg samsvarer med valgt sikkerhetskopi

## Sikkerhet og tilbakerulling

:::caution
Den sikre gjenopprettingsgrensen for **Oppdatering og migrering: Tilbakerulling av image** krever disse handlingene før noe startes eller fortsetter: hvis filutpakking eller pg_restore mislykkes, skal container, API, arbeidere og MCP forbli stoppet; prøv igjen fra urørte sikkerhetskopier. Sidespesifikke kontrollpunkter: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

- hvis filutpakking eller pg_restore mislykkes, skal container, API, arbeidere og MCP forbli stoppet; prøv igjen fra urørte sikkerhetskopier
:::

## Neste trinn

[Oppdatering og migrering: Migrering til DiscVault 26](/no/update/migration/)

## Kilde og status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Kilde og status: `2026-07-14`
- DiscVault Docs: `0.1.0`
