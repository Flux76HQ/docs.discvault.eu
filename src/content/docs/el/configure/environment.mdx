---
title: 'Ρύθμιση: Μεταβλητές περιβάλλοντος'
description: 'Οδηγίες ειδικά για την εργασία: Ρύθμιση: Μεταβλητές περιβάλλοντος: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.'
pageId: 'configure-environment'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Στόχος και αποτέλεσμα

Ολοκληρώστε τη ροή εργασίας της σελίδας για **Ρύθμιση: Μεταβλητές περιβάλλοντος**. Η διαδικασία χρησιμοποιεί `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

## Ισχύει για

Αυτή η σελίδα ισχύει μόνο για **Ρύθμιση: Μεταβλητές περιβάλλοντος** (Docker · Unraid). Τα όρια καναλιού και δεδομένων είναι: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `.env`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `.env`. Σημεία ελέγχου της σελίδας: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

### DiscVault v26 – σταθερό

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `.env`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `.env`

## Προϋποθέσεις

- ιδιωτικό αρχείο περιβάλλοντος εκτέλεσης DiscVault
- OpenSSL
- προγραμματισμένο παράθυρο επανεκκίνησης

## Διαδικασία

1. **Δημιουργήστε:** `/opt/discvault/.env` · δικαιώματα αρχείου μόνο για τον κάτοχο (0600)
2. **Ρυθμίστε:** `DISCVAULT_IMAGE` · `RP_ORIGINS`
3. **Επαληθεύστε:** `chmod 0600` · `docker compose config --quiet`
4. **Εκκινήστε:** διατηρήστε αμετάβλητα τα μυστικά JWT και PostgreSQL στις συνήθεις αλλαγές

### Εντολές και ρυθμίσεις

#### DiscVault v26 – σταθερό

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:latest
```

#### DiscVault v26 – beta

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:beta
```

#### Και τα δύο κανάλια

```bash
set -euo pipefail
test -n "$DISCVAULT_IMAGE"
case "$DISCVAULT_IMAGE" in
  ghcr.io/helmerznl/discvault:latest|ghcr.io/helmerznl/discvault:beta) ;;
  *) printf '%s\n' "$DISCVAULT_IMAGE" >&2; exit 64 ;;
esac
install -d -m 0700 /opt/discvault
install -d -m 0750 /srv/discvault
install -d -o 70 -g 70 -m 0700 /srv/discvault-postgres
cd /opt/discvault
test -f compose.yaml
umask 077
ENV_FILE=/opt/discvault/.env
test ! -e "$ENV_FILE"
ENV_TMP="$(mktemp "$ENV_FILE.tmp.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
POSTGRES_PASSWORD="$(openssl rand -hex 32)"
JWT_SECRET="$(openssl rand -hex 32)"
printf '%s\n' \
  "DISCVAULT_IMAGE=$DISCVAULT_IMAGE" \
  'DISCVAULT_DATA_DIR=/srv/discvault' \
  'DISCVAULT_POSTGRES_DATA=/srv/discvault-postgres' \
  'DISCVAULT_API_PORT=6080' \
  'DISCVAULT_MCP_PORT=6090' \
  'DISCVAULT_NETWORK_NAME=discvault' \
  'DISCVAULT_API_WORKERS=2' \
  'DISCVAULT_API_TIMEOUT=180' \
  'DISCVAULT_WORKER_ID=worker-1' \
  'DISCVAULT_WORKER_POLL_INTERVAL=2' \
  'POSTGRES_DB=discvault' \
  'POSTGRES_USER=discvault' \
  "POSTGRES_PASSWORD=$POSTGRES_PASSWORD" \
  "JWT_SECRET=$JWT_SECRET" \
  'TZ=Europe/Amsterdam' \
  'RP_ID=localhost' \
  'RP_NAME=DiscVault' \
  'RP_ORIGINS=http://localhost:6080' > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
ln "$ENV_TMP" "$ENV_FILE"
rm -f "$ENV_TMP"
trap - EXIT
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml config --quiet
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml pull
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml up -d
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml ps
curl --fail http://localhost:6080/api/next/health
```

## Αναμενόμενο αποτέλεσμα

Αποδεχθείτε το αποτέλεσμα για **Ρύθμιση: Μεταβλητές περιβάλλοντος** μόνο όταν ισχύουν όλα τα παρακάτω αποτελέσματα: δικαιώματα αρχείου μόνο για τον κάτοχο (0600); το ιδιωτικό αρχείο έχει λειτουργία 600, η επικύρωση Compose πετυχαίνει και το επιλεγμένο κανάλι γίνεται υγιές. Σημεία ελέγχου της σελίδας: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

- δικαιώματα αρχείου μόνο για τον κάτοχο (0600)
- το ιδιωτικό αρχείο έχει λειτουργία 600, η επικύρωση Compose πετυχαίνει και το επιλεγμένο κανάλι γίνεται υγιές

## Ασφάλεια και επαναφορά

:::caution
Το ασφαλές όριο ανάκτησης για **Ρύθμιση: Μεταβλητές περιβάλλοντος** απαιτεί αυτές τις ενέργειες πριν από κάθε εκκίνηση ή συνέχεια: το αρχείο περιβάλλοντος του αποθετηρίου τεκμηρίωσης δεν είναι ποτέ αρχείο εκτέλεσης DiscVault · `.env.example`; διατηρήστε αμετάβλητα τα μυστικά JWT και PostgreSQL στις συνήθεις αλλαγές · `JWT_SECRET` · `POSTGRES_PASSWORD`; σε αποτυχία, κρατήστε τις υπηρεσίες σταματημένες, επαναφέρετε και επικυρώστε το προηγούμενο ιδιωτικό αρχείο περιβάλλοντος και έπειτα αναδημιουργήστε την ίδια εικόνα χωρίς αλλαγή μυστικών. Σημεία ελέγχου της σελίδας: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

- το αρχείο περιβάλλοντος του αποθετηρίου τεκμηρίωσης δεν είναι ποτέ αρχείο εκτέλεσης DiscVault · `.env.example`
- διατηρήστε αμετάβλητα τα μυστικά JWT και PostgreSQL στις συνήθεις αλλαγές · `JWT_SECRET` · `POSTGRES_PASSWORD`
- σε αποτυχία, κρατήστε τις υπηρεσίες σταματημένες, επαναφέρετε και επικυρώστε το προηγούμενο ιδιωτικό αρχείο περιβάλλοντος και έπειτα αναδημιουργήστε την ίδια εικόνα χωρίς αλλαγή μυστικών
:::

## Επόμενο βήμα

[Ρύθμιση: Ταυτοποίηση και RBAC](/el/configure/auth-rbac/)

## Πηγή και κατάσταση

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Πηγή και κατάσταση: `2026-07-14`
- DiscVault Docs: `0.1.0`
