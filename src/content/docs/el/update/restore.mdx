---
title: 'Ενημέρωση και μετεγκατάσταση: Επαναφορά'
description: 'Οδηγίες ειδικά για την εργασία: Ενημέρωση και μετεγκατάσταση: Επαναφορά: `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.'
pageId: 'update-restore'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Στόχος και αποτέλεσμα

Ολοκληρώστε τη ροή εργασίας της σελίδας για **Ενημέρωση και μετεγκατάσταση: Επαναφορά**. Η διαδικασία χρησιμοποιεί `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.

## Ισχύει για

Αυτή η σελίδα ισχύει μόνο για **Ενημέρωση και μετεγκατάσταση: Επαναφορά** (Docker · Unraid). Τα όρια καναλιού και δεδομένων είναι: DiscVault · `ghcr.io/helmerznl/discvault:latest`; dump PostgreSQL και αρχείο συστήματος από το ίδιο παράθυρο χωρίς εγγραφές; επαναφέρετε τα αντίστοιχα δεδομένα πριν εκκινήσετε την προηγούμενη εικόνα; DiscVault · `ghcr.io/helmerznl/discvault:beta`; dump PostgreSQL και αρχείο συστήματος από το ίδιο παράθυρο χωρίς εγγραφές; επαναφέρετε τα αντίστοιχα δεδομένα πριν εκκινήσετε την προηγούμενη εικόνα. Σημεία ελέγχου της σελίδας: `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.

### DiscVault v26 – σταθερό

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- dump PostgreSQL και αρχείο συστήματος από το ίδιο παράθυρο χωρίς εγγραφές
- επαναφέρετε τα αντίστοιχα δεδομένα πριν εκκινήσετε την προηγούμενη εικόνα

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- dump PostgreSQL και αρχείο συστήματος από το ίδιο παράθυρο χωρίς εγγραφές
- επαναφέρετε τα αντίστοιχα δεδομένα πριν εκκινήσετε την προηγούμενη εικόνα

## Προϋποθέσεις

- αντίστοιχο σύνολο αντιγράφων
- παράθυρο συντήρησης
- `PREVIOUS_IMAGE` · ιδιωτικό αρχείο περιβάλλοντος εκτέλεσης DiscVault

## Διαδικασία

1. **Διακόψτε:** `next-api` `next-worker` `next-mcp`
2. **Επαναφέρετε:** `/data`
3. **Επαναφέρετε:** PostgreSQL · `pg_restore --exit-on-error`
4. **Εκκινήστε:** `next-api` → `GET /api/next/health` → `next-worker` `next-mcp`

### Εντολές και ρυθμίσεις

#### Και τα δύο κανάλια

```bash
set -euo pipefail
BACKUP_ID=20260714T180000Z
cd /opt/discvault
. "/srv/backups/discvault-$BACKUP_ID.env"
test -n "$PREVIOUS_IMAGE"
export DISCVAULT_IMAGE="$PREVIOUS_IMAGE"
docker pull "$DISCVAULT_IMAGE"
ENV_FILE=/opt/discvault/.env
ENV_TMP="$(mktemp "$ENV_FILE.restore.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk -v image="$DISCVAULT_IMAGE" '
  /^DISCVAULT_IMAGE=/ { print "DISCVAULT_IMAGE=" image; found=1; next }
  { print }
  END { if (!found) exit 42 }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
mv /srv/discvault "/srv/discvault.failed-$(date -u +%Y%m%dT%H%M%SZ)"
tar --acls --xattrs -C /srv -xzf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz"
docker compose --env-file .env -p discvault -f compose.yaml up -d postgres
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'dropdb --if-exists --force -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'createdb -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_restore --exit-on-error --no-owner -U "$POSTGRES_USER" -d "$POSTGRES_DB"' \
  < "/srv/backups/discvault-$BACKUP_ID.dump"
docker compose --env-file .env -p discvault -f compose.yaml up -d --force-recreate next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml \
  up -d --force-recreate next-worker next-mcp
docker compose --env-file .env -p discvault -f compose.yaml ps
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

## Αναμενόμενο αποτέλεσμα

Αποδεχθείτε το αποτέλεσμα για **Ενημέρωση και μετεγκατάσταση: Επαναφορά** μόνο όταν ισχύουν όλα τα παρακάτω αποτελέσματα: οι μετρήσεις βιβλιοθήκης και το δείγμα πολυμέσων ταιριάζουν με το επιλεγμένο αντίγραφο; `:latest` `:beta`. Σημεία ελέγχου της σελίδας: `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.

- οι μετρήσεις βιβλιοθήκης και το δείγμα πολυμέσων ταιριάζουν με το επιλεγμένο αντίγραφο
- `:latest` `:beta`

## Ασφάλεια και επαναφορά

:::caution
Το ασφαλές όριο ανάκτησης για **Ενημέρωση και μετεγκατάσταση: Επαναφορά** απαιτεί αυτές τις ενέργειες πριν από κάθε εκκίνηση ή συνέχεια: αν αποτύχει η εξαγωγή αρχείων ή το pg_restore, κρατήστε περιέκτη, API, workers και MCP σταματημένα και δοκιμάστε ξανά από ανέπαφα αντίγραφα ασφαλείας. Σημεία ελέγχου της σελίδας: `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.

- αν αποτύχει η εξαγωγή αρχείων ή το pg_restore, κρατήστε περιέκτη, API, workers και MCP σταματημένα και δοκιμάστε ξανά από ανέπαφα αντίγραφα ασφαλείας
:::

## Επόμενο βήμα

[Ενημέρωση και μετεγκατάσταση: Ενημέρωση περιέκτη](/el/update/update/)

## Πηγή και κατάσταση

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Πηγή και κατάσταση: `2026-07-14`
- DiscVault Docs: `0.1.0`
