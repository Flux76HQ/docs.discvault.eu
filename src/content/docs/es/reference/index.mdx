---
title: 'Referencia'
description: 'Instrucciones específicas para Referencia. :latest. :beta. :dev. 6080:5000. DISCVAULT_OIDC_ISSUER.'
pageId: 'reference-index'
products: ['server', 'pwa', 'ios', 'android']
platforms: ['all']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-09-20'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="todas las plataformas" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-09-20" />

## Objetivo y resultado

Completa el flujo específico de la página para **Referencia**. Este procedimiento utiliza `:latest` · `:beta` · `:dev` · `6080:5000` · `DISCVAULT_OIDC_ISSUER`.

## Aplicable a

Esta página se aplica solo a **Referencia** (todas las plataformas). Sus límites de canal y datos son: DiscVault · `ghcr.io/helmerznl/discvault:latest`; PostgreSQL `/data`; API `6080:5000` MCP `6090:6090`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; PostgreSQL `/data`; API `6080:5000` MCP `6090:6090`. Puntos de control de esta página: `:latest` · `:beta` · `:dev` · `6080:5000` · `DISCVAULT_OIDC_ISSUER`.

### DiscVault v26 estable

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- PostgreSQL `/data`
- API `6080:5000` MCP `6090:6090`

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- PostgreSQL `/data`
- API `6080:5000` MCP `6090:6090`

## Requisitos previos

- canal de publicación elegido

## Procedimiento

1. **Inspecciona:** `:latest` `:beta`
2. **Registra:** `:dev` es solo para ingeniería y no es un canal de publicación; DiscVault v26 estable usa `:latest` y beta usa `:beta` · `:dev`
3. **Registra:** `/api/next/health`
4. **Registra:** [Configuración: Inicio de sesión con OpenID Connect (OIDC)](/es/configure/oidc/) · `DISCVAULT_OIDC_ISSUER`
5. **Registra:** PostgreSQL `/data`



## Resultado esperado

Acepta el resultado de **Referencia** solo cuando se cumplan todos estos resultados: canal de publicación elegido · `@sha256:`; `:latest` · `:beta` · `:dev`. Puntos de control de esta página: `:latest` · `:beta` · `:dev` · `6080:5000` · `DISCVAULT_OIDC_ISSUER`.

- canal de publicación elegido · `@sha256:`
- `:latest` · `:beta` · `:dev`

## Seguridad y reversión

:::caution
El límite de recuperación segura de **Referencia** exige estas acciones antes de iniciar o continuar: la referencia es de solo lectura; usa el procedimiento específico antes de ejecutar órdenes o cambiar datos; `:dev` es solo para ingeniería y no es un canal de publicación; DiscVault v26 estable usa `:latest` y beta usa `:beta` · `:dev`. Puntos de control de esta página: `:latest` · `:beta` · `:dev` · `6080:5000` · `DISCVAULT_OIDC_ISSUER`.

- la referencia es de solo lectura; usa el procedimiento específico antes de ejecutar órdenes o cambiar datos
- `:dev` es solo para ingeniería y no es un canal de publicación; DiscVault v26 estable usa `:latest` y beta usa `:beta` · `:dev`
:::

## Siguiente paso

[Inicio: Resumen](/es/start/)

## Fuente y estado

- [`helmerzNL/DiscVault@409da7b2dc25`](https://github.com/helmerzNL/DiscVault/commit/409da7b2dc25bbf621cd0a6682136d565cdf5070)
- Fuente y estado: `2026-09-20`
- DiscVault Docs: `0.1.14`
