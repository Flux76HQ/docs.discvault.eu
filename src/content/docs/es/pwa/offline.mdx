---
title: 'PWA: Comportamiento sin conexión'
description: 'Instrucciones específicas para PWA: Comportamiento sin conexión: `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.'
pageId: 'pwa-offline'
products: ['pwa']
platforms: ['web', 'ios', 'android']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="web · iOS · Android" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Objetivo y resultado

Completa el flujo específico de la página para **PWA: Comportamiento sin conexión**. Este procedimiento utiliza `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.

## Aplicable a

Esta página se aplica solo a **PWA: Comportamiento sin conexión** (web · iOS · Android). Sus límites de canal y datos son: DiscVault · `ghcr.io/helmerznl/discvault:latest`; las pantallas en caché pueden abrirse, pero los datos nuevos y las escrituras requieren red · `Service Worker` · `CacheStorage`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; las pantallas en caché pueden abrirse, pero los datos nuevos y las escrituras requieren red · `Service Worker` · `CacheStorage`. Puntos de control de esta página: `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.

### DiscVault v26 estable

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- las pantallas en caché pueden abrirse, pero los datos nuevos y las escrituras requieren red · `Service Worker` · `CacheStorage`

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- las pantallas en caché pueden abrirse, pero los datos nuevos y las escrituras requieren red · `Service Worker` · `CacheStorage`

## Requisitos previos

- PWA
- `CacheStorage`

## Procedimiento

1. **Abre:** Biblioteca → Buscar
2. **Prueba:** modo avión · `Service Worker`
3. **Registra:** `navigator.onLine`
4. **Vuelve a conectar:** HTTPS



## Resultado esperado

Acepta el resultado de **PWA: Comportamiento sin conexión** solo cuando se cumplan todos estos resultados: las pantallas en caché pueden abrirse, pero los datos nuevos y las escrituras requieren red; `Service Worker` · `navigator.onLine`. Puntos de control de esta página: `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.

- las pantallas en caché pueden abrirse, pero los datos nuevos y las escrituras requieren red
- `Service Worker` · `navigator.onLine`

## Seguridad y reversión

:::caution
El límite de recuperación segura de **PWA: Comportamiento sin conexión** exige estas acciones antes de iniciar o continuar: las pantallas en caché pueden abrirse, pero los datos nuevos y las escrituras requieren red · DiscVault. Puntos de control de esta página: `Service Worker` · `CacheStorage` · `navigator.onLine` · HTTPS.

- las pantallas en caché pueden abrirse, pero los datos nuevos y las escrituras requieren red · DiscVault
:::

## Siguiente paso

[Solución de problemas](/es/troubleshooting/)

## Fuente y estado

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Fuente y estado: `2026-07-14`
- DiscVault Docs: `0.1.0`
