---
title: 'Administración: Registros, tareas y seguridad'
description: 'Instrucciones específicas para Administración: Registros, tareas y seguridad: `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.'
pageId: 'admin-index'
products: ['server']
platforms: ['docker', 'unraid', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid · web" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Objetivo y resultado

Completa el flujo específico de la página para **Administración: Registros, tareas y seguridad**. Este procedimiento utiliza `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.

## Aplicable a

Esta página se aplica solo a **Administración: Registros, tareas y seguridad** (Docker · Unraid · web). Sus límites de canal y datos son: DiscVault · `ghcr.io/helmerznl/discvault:latest`; API MCP PostgreSQL; DiscVault · `ghcr.io/helmerznl/discvault:beta`; API MCP PostgreSQL. Puntos de control de esta página: `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.

### DiscVault v26 estable

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- API MCP PostgreSQL

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- API MCP PostgreSQL

## Requisitos previos

- acceso de propietario
- juego de copias coincidente
- registros con hora y datos privados ocultos

## Procedimiento

1. **Inspecciona:** `/api/next/health`
2. **Inspecciona:** registros con hora y datos privados ocultos
3. **Verifica:** `next-worker`
4. **Conserva:** `--tail=200`

### Comandos y ajustes

#### Ambos canales

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml logs --tail=200 next-api next-worker next-mcp postgres
curl --fail http://localhost:6080/api/next/jobs
curl --fail http://localhost:6080/api/next/health
```

## Resultado esperado

Acepta el resultado de **Administración: Registros, tareas y seguridad** solo cuando se cumplan todos estos resultados: una capa defectuosa queda aislada con hora, estado, digest de imagen y error oculto; juego de copias coincidente. Puntos de control de esta página: `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.

- una capa defectuosa queda aislada con hora, estado, digest de imagen y error oculto
- juego de copias coincidente

## Seguridad y reversión

:::caution
El límite de recuperación segura de **Administración: Registros, tareas y seguridad** exige estas acciones antes de iniciar o continuar: no borres primero volúmenes, `/data`, credenciales del navegador ni almacenamiento nativo; conserva registros ocultos y restaura solo una copia coincidente. Puntos de control de esta página: `docker compose` · `/api/next/jobs` · `next-worker` · `--tail=200`.

- no borres primero volúmenes, `/data`, credenciales del navegador ni almacenamiento nativo; conserva registros ocultos y restaura solo una copia coincidente
:::

## Siguiente paso

[Solución de problemas](/es/troubleshooting/)

## Fuente y estado

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Fuente y estado: `2026-07-14`
- DiscVault Docs: `0.1.0`
