---
title: 'Actualización y migración: Reversión de imagen'
description: 'Instrucciones específicas para Actualización y migración: Reversión de imagen: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.'
pageId: 'update-rollback'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Objetivo y resultado

Completa el flujo específico de la página para **Actualización y migración: Reversión de imagen**. Este procedimiento utiliza `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

## Aplicable a

Esta página se aplica solo a **Actualización y migración: Reversión de imagen** (Docker · Unraid). Sus límites de canal y datos son: DiscVault · `ghcr.io/helmerznl/discvault:latest`; restaura los datos coincidentes antes de iniciar la imagen anterior; PostgreSQL `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; restaura los datos coincidentes antes de iniciar la imagen anterior; PostgreSQL `/data`. Puntos de control de esta página: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

### DiscVault v26 estable

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- restaura los datos coincidentes antes de iniciar la imagen anterior
- PostgreSQL `/data`

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- restaura los datos coincidentes antes de iniciar la imagen anterior
- PostgreSQL `/data`

## Requisitos previos

- juego de copias coincidente
- `PREVIOUS_IMAGE`

## Procedimiento

1. **Detén:** `next-api` `next-worker` `next-mcp`
2. **Restaura:** `/data`
3. **Restaura:** PostgreSQL · `pg_restore --exit-on-error`
4. **Inicia:** `PREVIOUS_IMAGE` → API → MCP

### Comandos y ajustes

#### Ambos canales

```bash
set -euo pipefail
BACKUP_ID=20260714T180000Z
cd /opt/discvault
. "/srv/backups/discvault-$BACKUP_ID.env"
test -n "$PREVIOUS_IMAGE"
export DISCVAULT_IMAGE="$PREVIOUS_IMAGE"
docker pull "$DISCVAULT_IMAGE"
ENV_FILE=/opt/discvault/.env
ENV_TMP="$(mktemp "$ENV_FILE.restore.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk -v image="$DISCVAULT_IMAGE" '
  /^DISCVAULT_IMAGE=/ { print "DISCVAULT_IMAGE=" image; found=1; next }
  { print }
  END { if (!found) exit 42 }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
mv /srv/discvault "/srv/discvault.failed-$(date -u +%Y%m%dT%H%M%SZ)"
tar --acls --xattrs -C /srv -xzf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz"
docker compose --env-file .env -p discvault -f compose.yaml up -d postgres
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'dropdb --if-exists --force -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'createdb -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_restore --exit-on-error --no-owner -U "$POSTGRES_USER" -d "$POSTGRES_DB"' \
  < "/srv/backups/discvault-$BACKUP_ID.dump"
docker compose --env-file .env -p discvault -f compose.yaml up -d --force-recreate next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml \
  up -d --force-recreate next-worker next-mcp
docker compose --env-file .env -p discvault -f compose.yaml ps
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

## Resultado esperado

Acepta el resultado de **Actualización y migración: Reversión de imagen** solo cuando se cumplan todos estos resultados: `PREVIOUS_IMAGE` · `@sha256:`; restaura los datos coincidentes antes de iniciar la imagen anterior; los recuentos y la muestra multimedia coinciden con la copia elegida. Puntos de control de esta página: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

- `PREVIOUS_IMAGE` · `@sha256:`
- restaura los datos coincidentes antes de iniciar la imagen anterior
- los recuentos y la muestra multimedia coinciden con la copia elegida

## Seguridad y reversión

:::caution
El límite de recuperación segura de **Actualización y migración: Reversión de imagen** exige estas acciones antes de iniciar o continuar: si falla la extracción de archivos o pg_restore, mantén detenidos el contenedor, la API, los workers y MCP; reintenta desde copias de respaldo intactas. Puntos de control de esta página: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

- si falla la extracción de archivos o pg_restore, mantén detenidos el contenedor, la API, los workers y MCP; reintenta desde copias de respaldo intactas
:::

## Siguiente paso

[Actualización y migración: Migración a DiscVault 26](/es/update/migration/)

## Fuente y estado

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Fuente y estado: `2026-07-14`
- DiscVault Docs: `0.1.0`
