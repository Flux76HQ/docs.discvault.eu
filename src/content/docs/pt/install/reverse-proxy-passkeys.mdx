---
title: 'Instalação: Proxy inverso e chaves de acesso'
description: 'Instruções específicas para Instalação: Proxy inverso e chaves de acesso. FQDN. RP_ORIGINS. RP_ID. OIDC. /api/next/auth/oidc/callback. LEGACY_AUTH_ENABLED.'
pageId: 'install-reverse-proxy-passkeys'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault', 'helmerzNL/DiscVault.EU', 'Flux76HQ/App-Guidance']
lastVerified: '2026-09-20'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · web" version="DiscVault v26" source="helmerzNL/DiscVault · helmerzNL/DiscVault.EU · Flux76HQ/App-Guidance" verified="2026-09-20" />

## Objetivo e resultado

Conclua o procedimento específico da página para **Instalação: Proxy inverso e chaves de acesso**. Este procedimento utiliza FQDN · `RP_ORIGINS` · `RP_ID` · OIDC · `/api/next/auth/oidc/callback` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

## Aplicável a

Esta página aplica-se apenas a **Instalação: Proxy inverso e chaves de acesso** (Docker · web). Os limites de canal e dados são: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `RP_ID` `RP_ORIGINS` · `6080`; fora da exceção localhost, as passkeys requerem um nome de domínio completo e estável por HTTPS fidedigno; um endereço IP isolado ou HTTP simples não são suportados; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `RP_ID` `RP_ORIGINS` · `6180`; OIDC · `/api/next/auth/oidc/callback` · [Configuração: Início de sessão com OpenID Connect (OIDC)](/pt/configure/oidc/); `LEGACY_AUTH_ENABLED=true`. Pontos de controlo desta página: FQDN · `RP_ORIGINS` · `RP_ID` · OIDC · `/api/next/auth/oidc/callback` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

### DiscVault v26 estável

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `RP_ID` `RP_ORIGINS` · `6080`
- fora da exceção localhost, as passkeys requerem um nome de domínio completo e estável por HTTPS fidedigno; um endereço IP isolado ou HTTP simples não são suportados

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `RP_ID` `RP_ORIGINS` · `6180`
- OIDC · `/api/next/auth/oidc/callback` · [Configuração: Início de sessão com OpenID Connect (OIDC)](/pt/configure/oidc/)
- `LEGACY_AUTH_ENABLED=true`

## Pré-requisitos

- FQDN
- `RP_ORIGINS`
- `RP_ID`

### Alternativa Legacy Authentication

:::note[Disponível em beta]
Legacy Authentication com nome de utilizador, palavra-passe e autenticação de dois fatores (2FA) está disponível no DiscVault 26 beta quando não é possível usar passkeys.

Siga `/install/legacy-authentication/` antes de ativar a funcionalidade ou criar o primeiro proprietário.
:::

## Procedimento

1. **Escolher:** FQDN · fora da exceção localhost, as passkeys requerem um nome de domínio completo e estável por HTTPS fidedigno; um endereço IP isolado ou HTTP simples não são suportados
2. **Escolher:** [Instalação: Legacy Authentication](/pt/install/legacy-authentication/) · para um IP privado ou loopback direto sem FQDN deixe `RP_ID` e `RP_ORIGINS` vazios
3. **Configurar:** `RP_ID`
4. **Configurar:** `RP_ORIGINS` · HTTPS
5. **Configurar:** [Configuração: Início de sessão com OpenID Connect (OIDC)](/pt/configure/oidc/) · `/api/next/auth/oidc/callback`
6. **Configurar:** `6080` `6180`
7. **Testar:** `GET /api/next/health`
8. **Registar:** escolha o nome do anfitrião antes de registar passkeys; alterá-lo depois exige novo registo, enquanto localhost continua apenas uma exceção local

### Comandos e definições

#### DiscVault v26 estável

```text
RP_ID=discvault.example.com
RP_NAME=DiscVault
RP_ORIGINS=https://discvault.example.com
curl --fail https://discvault.example.com/api/next/health
```

#### DiscVault v26 beta

```text
RP_ID=discvault.example.com
RP_NAME=DiscVault
RP_ORIGINS=https://discvault.example.com
LEGACY_AUTH_ENABLED=true
curl --fail http://localhost:6180/api/next/health
```

## Resultado esperado

Aceite o resultado de **Instalação: Proxy inverso e chaves de acesso** apenas quando todos os resultados abaixo forem confirmados: HTTPS · HTTP `2xx`; `RP_ID`; OIDC · `/api/next/auth/oidc/callback`; `RP_ID` é o nome do anfitrião sem esquema ou porta, enquanto `RP_ORIGINS` é a origem HTTPS completa exata; ambos devem corresponder ao endereço aberto no navegador. Pontos de controlo desta página: FQDN · `RP_ORIGINS` · `RP_ID` · OIDC · `/api/next/auth/oidc/callback` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

- HTTPS · HTTP `2xx`
- `RP_ID`
- OIDC · `/api/next/auth/oidc/callback`
- `RP_ID` é o nome do anfitrião sem esquema ou porta, enquanto `RP_ORIGINS` é a origem HTTPS completa exata; ambos devem corresponder ao endereço aberto no navegador

## Segurança e reversão

:::caution
O limite de recuperação segura de **Instalação: Proxy inverso e chaves de acesso** exige estas ações antes de iniciar ou continuar: escolha o nome do anfitrião antes de registar passkeys; alterá-lo depois exige novo registo, enquanto localhost continua apenas uma exceção local; apenas um pedido privado ou loopback direto com RP inválido ou vazio pode oferecer arranque sem TOTP; mantenha TOTP para utilizador, palavra-passe e 2FA; restaure em conjunto o proxy e os valores RP e mantenha um método de recuperação do proprietário. Pontos de controlo desta página: FQDN · `RP_ORIGINS` · `RP_ID` · OIDC · `/api/next/auth/oidc/callback` · `LEGACY_AUTH_ENABLED` · `/install/legacy-authentication/` · `/api/next/health`.

- escolha o nome do anfitrião antes de registar passkeys; alterá-lo depois exige novo registo, enquanto localhost continua apenas uma exceção local
- apenas um pedido privado ou loopback direto com RP inválido ou vazio pode oferecer arranque sem TOTP; mantenha TOTP para utilizador, palavra-passe e 2FA
- restaure em conjunto o proxy e os valores RP e mantenha um método de recuperação do proprietário
:::

## Próximo passo

[Instalação: Primeiro arranque e verificação de estado](/pt/install/first-start-health/)

## Fonte e estado

- [`helmerzNL/DiscVault@409da7b2dc25`](https://github.com/helmerzNL/DiscVault/commit/409da7b2dc25bbf621cd0a6682136d565cdf5070)
- [`helmerzNL/DiscVault.EU@583f85c55dc2`](https://github.com/helmerzNL/DiscVault.EU/commit/583f85c55dc22b63368f997a3b076a093ca0afa1)
- [`Flux76HQ/App-Guidance@41bfca3bfa39`](https://github.com/Flux76HQ/App-Guidance/commit/41bfca3bfa39f0b43924190d384e2d96be756020)
- [Moving beta source: OIDC deployment notes](https://github.com/helmerzNL/DiscVault/blob/release/v26-beta/app/deploy/next/README.md)
- [Pinned verification: OIDC environment template](https://github.com/helmerzNL/DiscVault/blob/409da7b2dc25bbf621cd0a6682136d565cdf5070/app/deploy/next/.env.example)
- [Pinned verification: OIDC implementation](https://github.com/helmerzNL/DiscVault/blob/409da7b2dc25bbf621cd0a6682136d565cdf5070/app/backend/next_oidc.py)
- [Authoritative OIDC authentication contract](https://github.com/Flux76HQ/App-Guidance/blob/41bfca3bfa39f0b43924190d384e2d96be756020/projects/discvault/specs/oidc-authentication.md)
- Fonte e estado: `2026-09-20`
- DiscVault Docs: `0.1.14`
