---
title: 'Configuração: Variáveis de ambiente'
description: 'Instruções específicas para Configuração: Variáveis de ambiente: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.'
pageId: 'configure-environment'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Objetivo e resultado

Conclua o procedimento específico da página para **Configuração: Variáveis de ambiente**. Este procedimento utiliza `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

## Aplicável a

Esta página aplica-se apenas a **Configuração: Variáveis de ambiente** (Docker · Unraid). Os limites de canal e dados são: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `.env`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `.env`. Pontos de controlo desta página: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

### DiscVault v26 estável

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `.env`

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `.env`

## Pré-requisitos

- ficheiro privado de execução do DiscVault
- OpenSSL
- janela de reinício planeada

## Procedimento

1. **Criar:** `/opt/discvault/.env` · permissões de ficheiro apenas para o proprietário (0600)
2. **Configurar:** `DISCVAULT_IMAGE` · `RP_ORIGINS`
3. **Verificar:** `chmod 0600` · `docker compose config --quiet`
4. **Iniciar:** mantenha os segredos JWT e PostgreSQL em alterações normais

### Comandos e definições

#### DiscVault v26 estável

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:latest
```

#### DiscVault v26 beta

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:beta
```

#### Ambos os canais

```bash
set -euo pipefail
test -n "$DISCVAULT_IMAGE"
case "$DISCVAULT_IMAGE" in
  ghcr.io/helmerznl/discvault:latest|ghcr.io/helmerznl/discvault:beta) ;;
  *) printf '%s\n' "$DISCVAULT_IMAGE" >&2; exit 64 ;;
esac
install -d -m 0700 /opt/discvault
install -d -m 0750 /srv/discvault
install -d -o 70 -g 70 -m 0700 /srv/discvault-postgres
cd /opt/discvault
test -f compose.yaml
umask 077
ENV_FILE=/opt/discvault/.env
test ! -e "$ENV_FILE"
ENV_TMP="$(mktemp "$ENV_FILE.tmp.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
POSTGRES_PASSWORD="$(openssl rand -hex 32)"
JWT_SECRET="$(openssl rand -hex 32)"
printf '%s\n' \
  "DISCVAULT_IMAGE=$DISCVAULT_IMAGE" \
  'DISCVAULT_DATA_DIR=/srv/discvault' \
  'DISCVAULT_POSTGRES_DATA=/srv/discvault-postgres' \
  'DISCVAULT_API_PORT=6080' \
  'DISCVAULT_MCP_PORT=6090' \
  'DISCVAULT_NETWORK_NAME=discvault' \
  'DISCVAULT_API_WORKERS=2' \
  'DISCVAULT_API_TIMEOUT=180' \
  'DISCVAULT_WORKER_ID=worker-1' \
  'DISCVAULT_WORKER_POLL_INTERVAL=2' \
  'POSTGRES_DB=discvault' \
  'POSTGRES_USER=discvault' \
  "POSTGRES_PASSWORD=$POSTGRES_PASSWORD" \
  "JWT_SECRET=$JWT_SECRET" \
  'TZ=Europe/Amsterdam' \
  'RP_ID=localhost' \
  'RP_NAME=DiscVault' \
  'RP_ORIGINS=http://localhost:6080' > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
ln "$ENV_TMP" "$ENV_FILE"
rm -f "$ENV_TMP"
trap - EXIT
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml config --quiet
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml pull
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml up -d
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml ps
curl --fail http://localhost:6080/api/next/health
```

## Resultado esperado

Aceite o resultado de **Configuração: Variáveis de ambiente** apenas quando todos os resultados abaixo forem confirmados: permissões de ficheiro apenas para o proprietário (0600); o ficheiro privado tem modo 600, a validação do Compose é concluída e o canal escolhido fica saudável. Pontos de controlo desta página: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

- permissões de ficheiro apenas para o proprietário (0600)
- o ficheiro privado tem modo 600, a validação do Compose é concluída e o canal escolhido fica saudável

## Segurança e reversão

:::caution
O limite de recuperação segura de **Configuração: Variáveis de ambiente** exige estas ações antes de iniciar ou continuar: o ficheiro de ambiente do repositório de documentação nunca é um ficheiro de execução do DiscVault · `.env.example`; mantenha os segredos JWT e PostgreSQL em alterações normais · `JWT_SECRET` · `POSTGRES_PASSWORD`; em caso de falha, mantenha os serviços parados, restaure e valide o ficheiro de ambiente privado anterior e recrie a mesma imagem sem rodar segredos. Pontos de controlo desta página: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

- o ficheiro de ambiente do repositório de documentação nunca é um ficheiro de execução do DiscVault · `.env.example`
- mantenha os segredos JWT e PostgreSQL em alterações normais · `JWT_SECRET` · `POSTGRES_PASSWORD`
- em caso de falha, mantenha os serviços parados, restaure e valide o ficheiro de ambiente privado anterior e recrie a mesma imagem sem rodar segredos
:::

## Próximo passo

[Configuração: Autenticação e RBAC](/pt/configure/auth-rbac/)

## Fonte e estado

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Fonte e estado: `2026-07-14`
- DiscVault Docs: `0.1.0`
