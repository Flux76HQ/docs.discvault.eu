---
title: 'Atualização e migração: Atualização do contentor'
description: 'Instruções específicas para Atualização e migração: Atualização do contentor: `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.'
pageId: 'update-update'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Objetivo e resultado

Conclua o procedimento específico da página para **Atualização e migração: Atualização do contentor**. Este procedimento utiliza `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.

## Aplicável a

Esta página aplica-se apenas a **Atualização e migração: Atualização do contentor** (Docker · Unraid). Os limites de canal e dados são: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `DISCVAULT_IMAGE`; PostgreSQL `/data` · notas de versão e digest imutável registado · conjunto de cópias correspondente; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `DISCVAULT_IMAGE`; PostgreSQL `/data` · notas de versão e digest imutável registado · conjunto de cópias correspondente. Pontos de controlo desta página: `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.

### DiscVault v26 estável

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `DISCVAULT_IMAGE`
- PostgreSQL `/data` · notas de versão e digest imutável registado · conjunto de cópias correspondente

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `DISCVAULT_IMAGE`
- PostgreSQL `/data` · notas de versão e digest imutável registado · conjunto de cópias correspondente

## Pré-requisitos

- conjunto de cópias correspondente
- notas de versão e digest imutável registado

## Procedimento

1. **Verificar:** `PREVIOUS_IMAGE=@sha256:…`
2. **Escolher:** `DISCVAULT_IMAGE` · `:latest` `:beta`
3. **Executar:** `--force-recreate`
4. **Comparar:** o digest pretendido está em execução, a saúde é bem-sucedida e o login, a contagem e uma amostra passam

### Comandos e definições

#### DiscVault v26 estável

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:latest
```

#### DiscVault v26 beta

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:beta
```

#### Ambos os canais

```bash
set -euo pipefail
test -n "$DISCVAULT_IMAGE"
case "$DISCVAULT_IMAGE" in
  ghcr.io/helmerznl/discvault:latest|ghcr.io/helmerznl/discvault:beta) ;;
  *) printf '%s\n' "$DISCVAULT_IMAGE" >&2; exit 64 ;;
esac
cd /opt/discvault
ENV_FILE=/opt/discvault/.env
ENV_TMP="$(mktemp "$ENV_FILE.update.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk -v image="$DISCVAULT_IMAGE" '
  /^DISCVAULT_IMAGE=/ { print "DISCVAULT_IMAGE=" image; found=1; next }
  { print }
  END { if (!found) exit 42 }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
docker compose --env-file "$ENV_TMP" -p discvault -f compose.yaml config --quiet
docker compose --env-file "$ENV_TMP" -p discvault -f compose.yaml pull next-api next-worker next-mcp
docker compose --env-file "$ENV_TMP" -p discvault -f compose.yaml up -d --force-recreate
docker compose --env-file "$ENV_TMP" -p discvault -f compose.yaml ps
curl --fail http://localhost:6080/api/next/health
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

## Resultado esperado

Aceite o resultado de **Atualização e migração: Atualização do contentor** apenas quando todos os resultados abaixo forem confirmados: o digest pretendido está em execução, a saúde é bem-sucedida e o login, a contagem e uma amostra passam. Pontos de controlo desta página: `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.

- o digest pretendido está em execução, a saúde é bem-sucedida e o login, a contagem e uma amostra passam

## Segurança e reversão

:::caution
O limite de recuperação segura de **Atualização e migração: Atualização do contentor** exige estas ações antes de iniciar ou continuar: [Atualização e migração: Reversão da imagem](/pt/update/rollback/) · conjunto de cópias correspondente; notas de versão e digest imutável registado. Pontos de controlo desta página: `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.

- [Atualização e migração: Reversão da imagem](/pt/update/rollback/) · conjunto de cópias correspondente
- notas de versão e digest imutável registado
:::

## Próximo passo

[Atualização e migração: Reversão da imagem](/pt/update/rollback/)

## Fonte e estado

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Fonte e estado: `2026-07-14`
- DiscVault Docs: `0.1.0`
