---
title: 'Atualização e migração: Restauro'
description: 'Instruções específicas para Atualização e migração: Restauro: `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.'
pageId: 'update-restore'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Objetivo e resultado

Conclua o procedimento específico da página para **Atualização e migração: Restauro**. Este procedimento utiliza `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.

## Aplicável a

Esta página aplica-se apenas a **Atualização e migração: Restauro** (Docker · Unraid). Os limites de canal e dados são: DiscVault · `ghcr.io/helmerznl/discvault:latest`; dump PostgreSQL e arquivo de ficheiros da mesma janela sem escritas; restaure os dados correspondentes antes de iniciar a imagem anterior; DiscVault · `ghcr.io/helmerznl/discvault:beta`; dump PostgreSQL e arquivo de ficheiros da mesma janela sem escritas; restaure os dados correspondentes antes de iniciar a imagem anterior. Pontos de controlo desta página: `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.

### DiscVault v26 estável

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- dump PostgreSQL e arquivo de ficheiros da mesma janela sem escritas
- restaure os dados correspondentes antes de iniciar a imagem anterior

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- dump PostgreSQL e arquivo de ficheiros da mesma janela sem escritas
- restaure os dados correspondentes antes de iniciar a imagem anterior

## Pré-requisitos

- conjunto de cópias correspondente
- janela de manutenção
- `PREVIOUS_IMAGE` · ficheiro privado de execução do DiscVault

## Procedimento

1. **Parar:** `next-api` `next-worker` `next-mcp`
2. **Restaurar:** `/data`
3. **Restaurar:** PostgreSQL · `pg_restore --exit-on-error`
4. **Iniciar:** `next-api` → `GET /api/next/health` → `next-worker` `next-mcp`

### Comandos e definições

#### Ambos os canais

```bash
set -euo pipefail
BACKUP_ID=20260714T180000Z
cd /opt/discvault
. "/srv/backups/discvault-$BACKUP_ID.env"
test -n "$PREVIOUS_IMAGE"
export DISCVAULT_IMAGE="$PREVIOUS_IMAGE"
docker pull "$DISCVAULT_IMAGE"
ENV_FILE=/opt/discvault/.env
ENV_TMP="$(mktemp "$ENV_FILE.restore.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk -v image="$DISCVAULT_IMAGE" '
  /^DISCVAULT_IMAGE=/ { print "DISCVAULT_IMAGE=" image; found=1; next }
  { print }
  END { if (!found) exit 42 }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
mv /srv/discvault "/srv/discvault.failed-$(date -u +%Y%m%dT%H%M%SZ)"
tar --acls --xattrs -C /srv -xzf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz"
docker compose --env-file .env -p discvault -f compose.yaml up -d postgres
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'dropdb --if-exists --force -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'createdb -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_restore --exit-on-error --no-owner -U "$POSTGRES_USER" -d "$POSTGRES_DB"' \
  < "/srv/backups/discvault-$BACKUP_ID.dump"
docker compose --env-file .env -p discvault -f compose.yaml up -d --force-recreate next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml \
  up -d --force-recreate next-worker next-mcp
docker compose --env-file .env -p discvault -f compose.yaml ps
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

## Resultado esperado

Aceite o resultado de **Atualização e migração: Restauro** apenas quando todos os resultados abaixo forem confirmados: as contagens e a amostra multimédia correspondem à cópia selecionada; `:latest` `:beta`. Pontos de controlo desta página: `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.

- as contagens e a amostra multimédia correspondem à cópia selecionada
- `:latest` `:beta`

## Segurança e reversão

:::caution
O limite de recuperação segura de **Atualização e migração: Restauro** exige estas ações antes de iniciar ou continuar: se a extração dos ficheiros ou pg_restore falhar, mantenha o contentor, a API, os workers e o MCP parados; repita a partir de cópias intactas. Pontos de controlo desta página: `pg_restore --exit-on-error` · `discvault.failed-` · `next-worker` · `next-api`.

- se a extração dos ficheiros ou pg_restore falhar, mantenha o contentor, a API, os workers e o MCP parados; repita a partir de cópias intactas
:::

## Próximo passo

[Atualização e migração: Atualização do contentor](/pt/update/update/)

## Fonte e estado

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Fonte e estado: `2026-07-14`
- DiscVault Docs: `0.1.0`
