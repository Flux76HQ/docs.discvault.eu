---
title: 'Install: Reverse proxy and passkeys'
description: 'Use a stable FQDN over trusted HTTPS and keep passkey relying-party values aligned with the exact public origin.'
pageId: 'install-reverse-proxy-passkeys'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault', 'helmerzNL/DiscVault.EU']
lastVerified: '2026-07-15'
---

import PageMeta from '../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · web" version="DiscVault v26" source="helmerzNL/DiscVault · helmerzNL/DiscVault.EU" verified="2026-07-15" />

## Goal and result

Use a stable FQDN over trusted HTTPS and keep passkey relying-party values aligned with the exact public origin.

## Applies to

DiscVault v26 stable and beta both use `RP_ID`, `RP_ORIGINS`, and `/api/next/health`. Outside the localhost setup exception, passkeys require a stable fully qualified domain name over trusted HTTPS; a bare IP address or plain HTTP is unsupported. `RP_ID` is the hostname without scheme or port, and `RP_ORIGINS` is the exact full HTTPS origin opened in the browser. The proxy connects to host port 6080, which maps to API container port 5000.

### DiscVault v26 stable

- DiscVault v26 stable · `ghcr.io/helmerznl/discvault:latest`
- same `RP_ID` + `RP_ORIGINS` configuration
- proxy host port `6080`; API listens on container port `5000`
- outside the localhost setup exception, passkeys require a stable fully qualified domain name over trusted HTTPS; a bare IP address or plain HTTP is unsupported
- `RP_ID` is the hostname without scheme or port, while `RP_ORIGINS` is the exact full HTTPS origin; both must match the address opened in the browser

### DiscVault v26 beta

- DiscVault v26 beta · `ghcr.io/helmerznl/discvault:beta`
- same `RP_ID` + `RP_ORIGINS` configuration
- proxy host port `6080`; API listens on container port `5000`
- outside the localhost setup exception, passkeys require a stable fully qualified domain name over trusted HTTPS; a bare IP address or plain HTTP is unsupported
- `RP_ID` is the hostname without scheme or port, while `RP_ORIGINS` is the exact full HTTPS origin; both must match the address opened in the browser

## Prerequisites

- public FQDN
- trusted TLS certificate
- reverse proxy

### Legacy Authentication fallback

:::note[Planned]
Legacy Authentication with a username, password, and two-factor authentication (2FA) is being developed as a fallback for installations without a valid FQDN, where passkeys cannot be used.

The setup steps and configuration options will be added here when this authentication method is available. Until then, do not treat Legacy Authentication as an available installation route.
:::

## Procedure

1. **Choose:** stable FQDN · outside the localhost setup exception, passkeys require a stable fully qualified domain name over trusted HTTPS; a bare IP address or plain HTTP is unsupported
2. **Configure:** `RP_ID` · hostname only
3. **Configure:** `RP_ORIGINS` · exact public HTTPS origin
4. **Configure:** proxy upstream · `http://127.0.0.1:6080`
5. **Test:** `GET /api/next/health` · passkey registration · same origin
6. **Record:** choose the hostname before registering passkeys; changing it later requires passkey registration again, while localhost remains only a local setup exception

### Commands and settings

#### Both channels

```text
RP_ID=discvault.example.com
RP_ORIGINS=https://discvault.example.com
curl --fail https://discvault.example.com/api/next/health
```

## Expected result

The public health URL succeeds and a passkey can be registered and used from the same FQDN and exact HTTPS origin.

- HTTPS health · HTTP `2xx`
- passkey · `RP_ID` + public origin
- `RP_ID` is the hostname without scheme or port, while `RP_ORIGINS` is the exact full HTTPS origin; both must match the address opened in the browser

## Safety and rollback

:::caution
Restore the prior proxy configuration and RP values together. Choose the hostname before registration: changing it later requires passkey registration again, so keep an owner recovery path.

- choose the hostname before registering passkeys; changing it later requires passkey registration again, while localhost remains only a local setup exception
- restore proxy and RP values together and keep an owner recovery method
:::

## Next step

[Install: First start and health check](/install/first-start-health/)

## Source and status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- [`helmerzNL/DiscVault.EU@583f85c55dc2`](https://github.com/helmerzNL/DiscVault.EU/commit/583f85c55dc22b63368f997a3b076a093ca0afa1)
- Verified: `2026-07-15`
- DiscVault Docs: `0.1.5`
