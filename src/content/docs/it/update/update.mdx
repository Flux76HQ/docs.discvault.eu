---
title: 'Aggiornamento e migrazione: Aggiornamento del contenitore'
description: 'Istruzioni specifiche per Aggiornamento e migrazione: Aggiornamento del contenitore: `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.'
pageId: 'update-update'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Obiettivo e risultato

Completa la procedura specifica della pagina per **Aggiornamento e migrazione: Aggiornamento del contenitore**. Questa procedura utilizza `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.

## Si applica a

Questa pagina si applica solo a **Aggiornamento e migrazione: Aggiornamento del contenitore** (Docker · Unraid). I limiti di canale e dati sono: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `DISCVAULT_IMAGE`; PostgreSQL `/data` · note di rilascio e digest immutabile registrato · set di backup corrispondente; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `DISCVAULT_IMAGE`; PostgreSQL `/data` · note di rilascio e digest immutabile registrato · set di backup corrispondente. Punti di controllo della pagina: `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.

### DiscVault v26 stabile

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `DISCVAULT_IMAGE`
- PostgreSQL `/data` · note di rilascio e digest immutabile registrato · set di backup corrispondente

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `DISCVAULT_IMAGE`
- PostgreSQL `/data` · note di rilascio e digest immutabile registrato · set di backup corrispondente

## Prerequisiti

- set di backup corrispondente
- note di rilascio e digest immutabile registrato

## Procedura

1. **Verifica:** `PREVIOUS_IMAGE=@sha256:…`
2. **Scegli:** `DISCVAULT_IMAGE` · `:latest` `:beta`
3. **Esegui:** `--force-recreate`
4. **Confronta:** è in esecuzione il digest previsto, lo stato è integro e accesso proprietario, conteggio e campione multimediale riescono

### Comandi e impostazioni

#### DiscVault v26 stabile

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:latest
```

#### DiscVault v26 beta

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:beta
```

#### Entrambi i canali

```bash
set -euo pipefail
test -n "$DISCVAULT_IMAGE"
case "$DISCVAULT_IMAGE" in
  ghcr.io/helmerznl/discvault:latest|ghcr.io/helmerznl/discvault:beta) ;;
  *) printf '%s\n' "$DISCVAULT_IMAGE" >&2; exit 64 ;;
esac
cd /opt/discvault
ENV_FILE=/opt/discvault/.env
ENV_TMP="$(mktemp "$ENV_FILE.update.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk -v image="$DISCVAULT_IMAGE" '
  /^DISCVAULT_IMAGE=/ { print "DISCVAULT_IMAGE=" image; found=1; next }
  { print }
  END { if (!found) exit 42 }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
docker compose --env-file "$ENV_TMP" -p discvault -f compose.yaml config --quiet
docker compose --env-file "$ENV_TMP" -p discvault -f compose.yaml pull next-api next-worker next-mcp
docker compose --env-file "$ENV_TMP" -p discvault -f compose.yaml up -d --force-recreate
docker compose --env-file "$ENV_TMP" -p discvault -f compose.yaml ps
curl --fail http://localhost:6080/api/next/health
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

## Risultato previsto

Accetta il risultato per **Aggiornamento e migrazione: Aggiornamento del contenitore** solo quando tutti i risultati seguenti sono veri: è in esecuzione il digest previsto, lo stato è integro e accesso proprietario, conteggio e campione multimediale riescono. Punti di controllo della pagina: `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.

- è in esecuzione il digest previsto, lo stato è integro e accesso proprietario, conteggio e campione multimediale riescono

## Sicurezza e ripristino

:::caution
Il limite di ripristino sicuro per **Aggiornamento e migrazione: Aggiornamento del contenitore** richiede queste azioni prima di avviare o continuare: [Aggiornamento e migrazione: Ripristino dell’immagine](/it/update/rollback/) · set di backup corrispondente; note di rilascio e digest immutabile registrato. Punti di controllo della pagina: `:latest` · `:beta` · `DISCVAULT_IMAGE` · `--force-recreate`.

- [Aggiornamento e migrazione: Ripristino dell’immagine](/it/update/rollback/) · set di backup corrispondente
- note di rilascio e digest immutabile registrato
:::

## Passaggio successivo

[Aggiornamento e migrazione: Ripristino dell’immagine](/it/update/rollback/)

## Fonte e stato

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Fonte e stato: `2026-07-14`
- DiscVault Docs: `0.1.0`
