---
title: 'Configurazione: Variabili di ambiente'
description: 'Istruzioni specifiche per Configurazione: Variabili di ambiente: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.'
pageId: 'configure-environment'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Obiettivo e risultato

Completa la procedura specifica della pagina per **Configurazione: Variabili di ambiente**. Questa procedura utilizza `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

## Si applica a

Questa pagina si applica solo a **Configurazione: Variabili di ambiente** (Docker · Unraid). I limiti di canale e dati sono: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `.env`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `.env`. Punti di controllo della pagina: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

### DiscVault v26 stabile

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `.env`

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `.env`

## Prerequisiti

- file runtime privato di DiscVault
- OpenSSL
- finestra di riavvio pianificata

## Procedura

1. **Crea:** `/opt/discvault/.env` · permessi file solo proprietario (0600)
2. **Configura:** `DISCVAULT_IMAGE` · `RP_ORIGINS`
3. **Verifica:** `chmod 0600` · `docker compose config --quiet`
4. **Avvia:** mantieni invariati i segreti JWT e PostgreSQL nelle modifiche ordinarie

### Comandi e impostazioni

#### DiscVault v26 stabile

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:latest
```

#### DiscVault v26 beta

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:beta
```

#### Entrambi i canali

```bash
set -euo pipefail
test -n "$DISCVAULT_IMAGE"
case "$DISCVAULT_IMAGE" in
  ghcr.io/helmerznl/discvault:latest|ghcr.io/helmerznl/discvault:beta) ;;
  *) printf '%s\n' "$DISCVAULT_IMAGE" >&2; exit 64 ;;
esac
install -d -m 0700 /opt/discvault
install -d -m 0750 /srv/discvault
install -d -o 70 -g 70 -m 0700 /srv/discvault-postgres
cd /opt/discvault
test -f compose.yaml
umask 077
ENV_FILE=/opt/discvault/.env
test ! -e "$ENV_FILE"
ENV_TMP="$(mktemp "$ENV_FILE.tmp.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
POSTGRES_PASSWORD="$(openssl rand -hex 32)"
JWT_SECRET="$(openssl rand -hex 32)"
printf '%s\n' \
  "DISCVAULT_IMAGE=$DISCVAULT_IMAGE" \
  'DISCVAULT_DATA_DIR=/srv/discvault' \
  'DISCVAULT_POSTGRES_DATA=/srv/discvault-postgres' \
  'DISCVAULT_API_PORT=6080' \
  'DISCVAULT_MCP_PORT=6090' \
  'DISCVAULT_NETWORK_NAME=discvault' \
  'DISCVAULT_API_WORKERS=2' \
  'DISCVAULT_API_TIMEOUT=180' \
  'DISCVAULT_WORKER_ID=worker-1' \
  'DISCVAULT_WORKER_POLL_INTERVAL=2' \
  'POSTGRES_DB=discvault' \
  'POSTGRES_USER=discvault' \
  "POSTGRES_PASSWORD=$POSTGRES_PASSWORD" \
  "JWT_SECRET=$JWT_SECRET" \
  'TZ=Europe/Amsterdam' \
  'RP_ID=localhost' \
  'RP_NAME=DiscVault' \
  'RP_ORIGINS=http://localhost:6080' > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
ln "$ENV_TMP" "$ENV_FILE"
rm -f "$ENV_TMP"
trap - EXIT
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml config --quiet
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml pull
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml up -d
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml ps
curl --fail http://localhost:6080/api/next/health
```

## Risultato previsto

Accetta il risultato per **Configurazione: Variabili di ambiente** solo quando tutti i risultati seguenti sono veri: permessi file solo proprietario (0600); il file privato ha modalità 600, la convalida Compose riesce e il canale scelto risulta integro. Punti di controllo della pagina: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

- permessi file solo proprietario (0600)
- il file privato ha modalità 600, la convalida Compose riesce e il canale scelto risulta integro

## Sicurezza e ripristino

:::caution
Il limite di ripristino sicuro per **Configurazione: Variabili di ambiente** richiede queste azioni prima di avviare o continuare: il file di ambiente del repository della documentazione non è mai un file runtime DiscVault · `.env.example`; mantieni invariati i segreti JWT e PostgreSQL nelle modifiche ordinarie · `JWT_SECRET` · `POSTGRES_PASSWORD`; in caso di errore, lascia i servizi arrestati, ripristina e convalida il file di ambiente privato precedente, quindi ricrea la stessa immagine senza ruotare i segreti. Punti di controllo della pagina: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

- il file di ambiente del repository della documentazione non è mai un file runtime DiscVault · `.env.example`
- mantieni invariati i segreti JWT e PostgreSQL nelle modifiche ordinarie · `JWT_SECRET` · `POSTGRES_PASSWORD`
- in caso di errore, lascia i servizi arrestati, ripristina e convalida il file di ambiente privato precedente, quindi ricrea la stessa immagine senza ruotare i segreti
:::

## Passaggio successivo

[Configurazione: Autenticazione e RBAC](/it/configure/auth-rbac/)

## Fonte e stato

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Fonte e stato: `2026-07-14`
- DiscVault Docs: `0.1.0`
