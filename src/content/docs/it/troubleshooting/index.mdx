---
title: 'Risoluzione dei problemi'
description: 'Istruzioni specifiche per Risoluzione dei problemi. docker compose. /api/next/health. postgres. RP_ID. OIDC. auth.oidc_failed.'
pageId: 'troubleshooting-index'
products: ['server', 'pwa', 'ios', 'android']
platforms: ['all']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault', 'helmerzNL/DiscVaultApp', 'Flux76HQ/DiscVault-AndroidApp']
lastVerified: '2026-09-20'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="tutte le piattaforme" version="DiscVault v26" source="helmerzNL/DiscVault · helmerzNL/DiscVaultApp · Flux76HQ/DiscVault-AndroidApp" verified="2026-09-20" />

## Obiettivo e risultato

Completa la procedura specifica della pagina per **Risoluzione dei problemi**. Questa procedura utilizza `docker compose` · `/api/next/health` · `postgres` · `RP_ID` · OIDC · `auth.oidc_failed`.

## Si applica a

Questa pagina si applica solo a **Risoluzione dei problemi** (tutte le piattaforme). I limiti di canale e dati sono: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `/api/next/health`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `/api/next/health`. Punti di controllo della pagina: `docker compose` · `/api/next/health` · `postgres` · `RP_ID` · OIDC · `auth.oidc_failed`.

### DiscVault v26 stabile

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `/api/next/health`

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `/api/next/health`

## Prerequisiti

- canale di rilascio scelto
- log con data e dati privati oscurati
- `postgres`

## Procedura

1. **Registra:** `docker compose`
2. **Isola:** container → proxy inverso → browser/app → provider → dati
3. **Controlla:** [Configurazione: Accesso con OpenID Connect (OIDC)](/it/configure/oidc/) · `auth.oidc_failed`
4. **Controlla:** log con data e dati privati oscurati
5. **Conserva:** PostgreSQL `/data`

### Comandi e impostazioni

#### Entrambi i canali

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml ps
curl --fail http://localhost:6080/api/next/health
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml logs --tail=200 next-api next-worker next-mcp postgres
```

## Risultato previsto

Accetta il risultato per **Risoluzione dei problemi** solo quando tutti i risultati seguenti sono veri: un livello guasto è isolato con data, stato, digest immagine ed errore oscurato. Punti di controllo della pagina: `docker compose` · `/api/next/health` · `postgres` · `RP_ID` · OIDC · `auth.oidc_failed`.

- un livello guasto è isolato con data, stato, digest immagine ed errore oscurato

## Sicurezza e ripristino

:::caution
Il limite di ripristino sicuro per **Risoluzione dei problemi** richiede queste azioni prima di avviare o continuare: non eliminare prima volumi, `/data`, credenziali browser o archivio nativo; conserva log oscurati e ripristina solo un backup corrispondente. Punti di controllo della pagina: `docker compose` · `/api/next/health` · `postgres` · `RP_ID` · OIDC · `auth.oidc_failed`.

- non eliminare prima volumi, `/data`, credenziali browser o archivio nativo; conserva log oscurati e ripristina solo un backup corrispondente
:::

## Passaggio successivo

[Riferimenti](/it/reference/)

## Fonte e stato

- [`helmerzNL/DiscVault@409da7b2dc25`](https://github.com/helmerzNL/DiscVault/commit/409da7b2dc25bbf621cd0a6682136d565cdf5070)
- [`helmerzNL/DiscVaultApp@9d15ab2a339c`](https://github.com/helmerzNL/DiscVaultApp/commit/9d15ab2a339cefdb164ec0ab29ad69ddabc511c1)
- [`Flux76HQ/DiscVault-AndroidApp@a13ef4bdbb23`](https://github.com/Flux76HQ/DiscVault-AndroidApp/commit/a13ef4bdbb23b6a7e52e01cdf532fa222c1b67de)
- Fonte e stato: `2026-09-20`
- DiscVault Docs: `0.1.14`
