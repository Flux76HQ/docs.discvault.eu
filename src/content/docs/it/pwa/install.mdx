---
title: 'PWA: Installare la PWA'
description: 'Istruzioni specifiche per PWA: Installare la PWA: HTTPS · `display-mode: standalone` · iOS/iPadOS · Android.'
pageId: 'pwa-install'
products: ['pwa']
platforms: ['web', 'ios', 'android']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="web · iOS · Android" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Obiettivo e risultato

Completa la procedura specifica della pagina per **PWA: Installare la PWA**. Questa procedura utilizza HTTPS · `display-mode: standalone` · iOS/iPadOS · Android.

## Si applica a

Questa pagina si applica solo a **PWA: Installare la PWA** (web · iOS · Android). I limiti di canale e dati sono: DiscVault · `ghcr.io/helmerznl/discvault:latest`; PWA · `display-mode: standalone`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; PWA · `display-mode: standalone`. Punti di controllo della pagina: HTTPS · `display-mode: standalone` · iOS/iPadOS · Android.

### DiscVault v26 stabile

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- PWA · `display-mode: standalone`

### DiscVault v26 beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- PWA · `display-mode: standalone`

## Prerequisiti

- HTTPS
- account connesso con il permesso richiesto

## Procedura

1. **Installa:** Barra degli indirizzi → Installa DiscVault
2. **Installa:** Condividi → Aggiungi alla schermata Home · iOS/iPadOS
3. **Installa:** Menu del browser → Installa app · Android
4. **Verifica:** Android



## Risultato previsto

Accetta il risultato per **PWA: Installare la PWA** solo quando tutti i risultati seguenti sono veri: Barra degli indirizzi → Installa DiscVault; Condividi → Aggiungi alla schermata Home; Menu del browser → Installa app; account connesso con il permesso richiesto · HTTPS. Punti di controllo della pagina: HTTPS · `display-mode: standalone` · iOS/iPadOS · Android.

- Barra degli indirizzi → Installa DiscVault
- Condividi → Aggiungi alla schermata Home
- Menu del browser → Installa app
- account connesso con il permesso richiesto · HTTPS

## Sicurezza e ripristino

:::caution
Il limite di ripristino sicuro per **PWA: Installare la PWA** richiede queste azioni prima di avviare o continuare: rimuovi solo la shell PWA installata; libreria server e dati account restano invariati. Punti di controllo della pagina: HTTPS · `display-mode: standalone` · iOS/iPadOS · Android.

- rimuovi solo la shell PWA installata; libreria server e dati account restano invariati
:::

## Passaggio successivo

[PWA: Libreria e ricerca](/it/pwa/library-search/)

## Fonte e stato

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Fonte e stato: `2026-07-14`
- DiscVault Docs: `0.1.0`
