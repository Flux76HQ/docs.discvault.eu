---
title: 'Päivitys ja siirto: Varmuuskopiointi'
description: 'Tehtäväkohtaiset ohjeet: Päivitys ja siirto: Varmuuskopiointi: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.'
pageId: 'update-backup'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Tavoite ja tulos

Suorita sivukohtainen työnkulku: **Päivitys ja siirto: Varmuuskopiointi**. Tässä menettelyssä käytetään `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

## Koskee

Tämä sivu koskee vain kohdetta **Päivitys ja siirto: Varmuuskopiointi** (Docker · Unraid). Kanava- ja tietorajat ovat: DiscVault · `ghcr.io/helmerznl/discvault:latest`; PostgreSQL-vedos ja tiedostoarkisto samasta kirjoittajat pysäyttävästä ikkunasta; PostgreSQL `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; PostgreSQL-vedos ja tiedostoarkisto samasta kirjoittajat pysäyttävästä ikkunasta; PostgreSQL `/data`. Sivukohtaiset tarkistuspisteet: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

### DiscVault v26 – vakaa

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- PostgreSQL-vedos ja tiedostoarkisto samasta kirjoittajat pysäyttävästä ikkunasta
- PostgreSQL `/data`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- PostgreSQL-vedos ja tiedostoarkisto samasta kirjoittajat pysäyttävästä ikkunasta
- PostgreSQL `/data`

## Edellytykset

- varmuuskopio elävän datan ulkopuolelle
- kirjoitettava pysyvä tallennus ja vapaa varmuuskopiotila

## Toimenpiteet

1. **Kirjaa:** `PREVIOUS_IMAGE` · `sha256`
2. **Pysäytä:** `next-api` `next-worker` `next-mcp`
3. **Varmuuskopioi:** `pg_dump -Fc`
4. **Käynnistä:** `next-api` → → MCP

### Komennot ja asetukset

#### Molemmat kanavat

```bash
set -euo pipefail
cd /opt/discvault
install -d -m 0700 /srv/backups
BACKUP_ID="$(date -u +%Y%m%dT%H%M%SZ)"
API_ID="$(docker compose --env-file .env -p discvault -f compose.yaml ps -q next-api)"
IMAGE_ID="$(docker inspect "$API_ID" --format '{{.Image}}')"
PREVIOUS_IMAGE="$(docker image inspect "$IMAGE_ID" --format '{{index .RepoDigests 0}}')"
test -n "$PREVIOUS_IMAGE"
printf 'BACKUP_ID=%s\nPREVIOUS_IMAGE=%s\n' "$BACKUP_ID" "$PREVIOUS_IMAGE" \
  > "/srv/backups/discvault-$BACKUP_ID.env"
chmod 0600 "/srv/backups/discvault-$BACKUP_ID.env"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_dump -Fc -U "$POSTGRES_USER" "$POSTGRES_DB"' \
  > "/srv/backups/discvault-$BACKUP_ID.dump"
tar --acls --xattrs -C /srv -czf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz" discvault
docker compose --env-file .env -p discvault -f compose.yaml start next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml start next-worker next-mcp
test -s "/srv/backups/discvault-$BACKUP_ID.dump"
gzip -t "/srv/backups/discvault-data-$BACKUP_ID.tgz"
```

## Odotettu tulos

Hyväksy tulos kohteelle **Päivitys ja siirto: Varmuuskopiointi** vain kun kaikki seuraavat tulokset toteutuvat: arkisto ja vedos eivät ole tyhjiä, ne voidaan lukea palautusharjoituksessa ja ne liittyvät muuttumattomaan image-tiivisteeseen. Sivukohtaiset tarkistuspisteet: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- arkisto ja vedos eivät ole tyhjiä, ne voidaan lukea palautusharjoituksessa ja ne liittyvät muuttumattomaan image-tiivisteeseen

## Turvallisuus ja palautus

:::caution
Turvallinen palautusraja kohteelle **Päivitys ja siirto: Varmuuskopiointi** edellyttää näitä toimia ennen käynnistystä tai jatkamista: jos varmuuskopiointikomento epäonnistuu, käynnistä vain muuttumattomat palvelut ja peru päivitys; PostgreSQL-vedos ja tiedostoarkisto samasta kirjoittajat pysäyttävästä ikkunasta. Sivukohtaiset tarkistuspisteet: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- jos varmuuskopiointikomento epäonnistuu, käynnistä vain muuttumattomat palvelut ja peru päivitys
- PostgreSQL-vedos ja tiedostoarkisto samasta kirjoittajat pysäyttävästä ikkunasta
:::

## Seuraava vaihe

[Päivitys ja siirto: Palautus](/fi/update/restore/)

## Lähde ja tila

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Lähde ja tila: `2026-07-14`
- DiscVault Docs: `0.1.0`
