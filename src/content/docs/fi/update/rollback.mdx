---
title: 'Päivitys ja siirto: Levykuvan palautus'
description: 'Tehtäväkohtaiset ohjeet: Päivitys ja siirto: Levykuvan palautus: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.'
pageId: 'update-rollback'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Tavoite ja tulos

Suorita sivukohtainen työnkulku: **Päivitys ja siirto: Levykuvan palautus**. Tässä menettelyssä käytetään `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

## Koskee

Tämä sivu koskee vain kohdetta **Päivitys ja siirto: Levykuvan palautus** (Docker · Unraid). Kanava- ja tietorajat ovat: DiscVault · `ghcr.io/helmerznl/discvault:latest`; palauta vastaavat tiedot ennen edellisen imagen käynnistämistä; PostgreSQL `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; palauta vastaavat tiedot ennen edellisen imagen käynnistämistä; PostgreSQL `/data`. Sivukohtaiset tarkistuspisteet: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

### DiscVault v26 – vakaa

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- palauta vastaavat tiedot ennen edellisen imagen käynnistämistä
- PostgreSQL `/data`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- palauta vastaavat tiedot ennen edellisen imagen käynnistämistä
- PostgreSQL `/data`

## Edellytykset

- vastaava varmuuskopiosarja
- `PREVIOUS_IMAGE`

## Toimenpiteet

1. **Pysäytä:** `next-api` `next-worker` `next-mcp`
2. **Palauta:** `/data`
3. **Palauta:** PostgreSQL · `pg_restore --exit-on-error`
4. **Käynnistä:** `PREVIOUS_IMAGE` → API → MCP

### Komennot ja asetukset

#### Molemmat kanavat

```bash
set -euo pipefail
BACKUP_ID=20260714T180000Z
cd /opt/discvault
. "/srv/backups/discvault-$BACKUP_ID.env"
test -n "$PREVIOUS_IMAGE"
export DISCVAULT_IMAGE="$PREVIOUS_IMAGE"
docker pull "$DISCVAULT_IMAGE"
ENV_FILE=/opt/discvault/.env
ENV_TMP="$(mktemp "$ENV_FILE.restore.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk -v image="$DISCVAULT_IMAGE" '
  /^DISCVAULT_IMAGE=/ { print "DISCVAULT_IMAGE=" image; found=1; next }
  { print }
  END { if (!found) exit 42 }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
mv /srv/discvault "/srv/discvault.failed-$(date -u +%Y%m%dT%H%M%SZ)"
tar --acls --xattrs -C /srv -xzf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz"
docker compose --env-file .env -p discvault -f compose.yaml up -d postgres
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'dropdb --if-exists --force -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'createdb -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_restore --exit-on-error --no-owner -U "$POSTGRES_USER" -d "$POSTGRES_DB"' \
  < "/srv/backups/discvault-$BACKUP_ID.dump"
docker compose --env-file .env -p discvault -f compose.yaml up -d --force-recreate next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml \
  up -d --force-recreate next-worker next-mcp
docker compose --env-file .env -p discvault -f compose.yaml ps
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

## Odotettu tulos

Hyväksy tulos kohteelle **Päivitys ja siirto: Levykuvan palautus** vain kun kaikki seuraavat tulokset toteutuvat: `PREVIOUS_IMAGE` · `@sha256:`; palauta vastaavat tiedot ennen edellisen imagen käynnistämistä; kirjastomäärät ja mediatesti vastaavat valittua varmuuskopiota. Sivukohtaiset tarkistuspisteet: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

- `PREVIOUS_IMAGE` · `@sha256:`
- palauta vastaavat tiedot ennen edellisen imagen käynnistämistä
- kirjastomäärät ja mediatesti vastaavat valittua varmuuskopiota

## Turvallisuus ja palautus

:::caution
Turvallinen palautusraja kohteelle **Päivitys ja siirto: Levykuvan palautus** edellyttää näitä toimia ennen käynnistystä tai jatkamista: jos tiedostojen purku tai pg_restore epäonnistuu, pidä säilö, API, workerit ja MCP pysäytettyinä ja yritä uudelleen koskemattomista varmuuskopioista. Sivukohtaiset tarkistuspisteet: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

- jos tiedostojen purku tai pg_restore epäonnistuu, pidä säilö, API, workerit ja MCP pysäytettyinä ja yritä uudelleen koskemattomista varmuuskopioista
:::

## Seuraava vaihe

[Päivitys ja siirto: Siirtyminen DiscVault 26:een](/fi/update/migration/)

## Lähde ja tila

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Lähde ja tila: `2026-07-14`
- DiscVault Docs: `0.1.0`
