---
title: 'Asennus: Käänteinen välityspalvelin ja pääsyavaimet'
description: 'Tehtäväkohtaiset ohjeet: Asennus: Käänteinen välityspalvelin ja pääsyavaimet: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.'
pageId: 'install-reverse-proxy-passkeys'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · verkko" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Tavoite ja tulos

Suorita sivukohtainen työnkulku: **Asennus: Käänteinen välityspalvelin ja pääsyavaimet**. Tässä menettelyssä käytetään `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

## Koskee

Tämä sivu koskee vain kohdetta **Asennus: Käänteinen välityspalvelin ja pääsyavaimet** (Docker · verkko). Kanava- ja tietorajat ovat: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `RP_ID` `RP_ORIGINS`; `6080` API `5000`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `RP_ID` `RP_ORIGINS`; `6080` API `5000`. Sivukohtaiset tarkistuspisteet: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

### DiscVault v26 – vakaa

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `RP_ID` `RP_ORIGINS`
- `6080` API `5000`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `RP_ID` `RP_ORIGINS`
- `6080` API `5000`

## Edellytykset

- DNS
- TLS
- `6080`

## Toimenpiteet

1. **Määritä:** `RP_ID`
2. **Määritä:** `RP_ORIGINS` · HTTPS
3. **Määritä:** `http://127.0.0.1:6080`
4. **Testaa:** `GET /api/next/health`

### Komennot ja asetukset

#### Molemmat kanavat

```text
RP_ID=discvault.example.com
RP_ORIGINS=https://discvault.example.com
curl --fail https://discvault.example.com/api/next/health
```

## Odotettu tulos

Hyväksy tulos kohteelle **Asennus: Käänteinen välityspalvelin ja pääsyavaimet** vain kun kaikki seuraavat tulokset toteutuvat: HTTPS · HTTP `2xx`; `RP_ID`. Sivukohtaiset tarkistuspisteet: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

- HTTPS · HTTP `2xx`
- `RP_ID`

## Turvallisuus ja palautus

:::caution
Turvallinen palautusraja kohteelle **Asennus: Käänteinen välityspalvelin ja pääsyavaimet** edellyttää näitä toimia ennen käynnistystä tai jatkamista: palauta välityspalvelin- ja RP-arvot yhdessä ja säilytä omistajan palautustapa. Sivukohtaiset tarkistuspisteet: `RP_ORIGINS` · `RP_ID` · `6080` · `/api/next/health`.

- palauta välityspalvelin- ja RP-arvot yhdessä ja säilytä omistajan palautustapa
:::

## Seuraava vaihe

[Asennus: Ensimmäinen käynnistys ja kuntotarkistus](/fi/install/first-start-health/)

## Lähde ja tila

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Lähde ja tila: `2026-07-14`
- DiscVault Docs: `0.1.0`
