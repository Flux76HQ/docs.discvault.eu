---
title: 'Installeren: Eerste start en statuscontrole'
description: 'Taakspecifieke instructies voor Installeren: Eerste start en statuscontrole: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.'
pageId: 'install-first-start-health'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · web" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Doel en resultaat

Voer de paginaspecifieke werkwijze uit voor **Installeren: Eerste start en statuscontrole**. Deze werkwijze gebruikt `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

## Van toepassing op

Deze pagina is uitsluitend van toepassing op **Installeren: Eerste start en statuscontrole** (Docker · web). De kanaal- en gegevensgrenzen zijn: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `/api/next/auth/status`; `6080` → API `5000`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `/api/next/auth/status`; `6080` → API `5000`. Paginaspecifieke controlepunten: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

### DiscVault v26 stabiel

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `/api/next/auth/status`
- `6080` → API `5000`

### DiscVault v26 bèta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `/api/next/auth/status`
- `6080` → API `5000`

## Vereisten

- `/api/next/health`
- `/api/next/auth/status`

## Procedure

1. **Controleren:** `postgres`
2. **Verifiëren:** `GET /api/next/health` · HTTP `2xx`
3. **Openen:** `6080:5000`
4. **Verifiëren:** `GET /api/next/auth/status` · WebAuthn

### Opdrachten en instellingen

#### Beide kanalen

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml ps
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml logs --tail=100 next-api next-worker next-mcp postgres
curl --fail http://localhost:6080/api/next/health
curl --fail http://localhost:6080/api/next/auth/status
```

## Verwacht resultaat

Accepteer het resultaat voor **Installeren: Eerste start en statuscontrole** alleen wanneer alle onderstaande resultaten kloppen: health slaagt, de ingestelde origin opent en het eerste account is eigenaar. Paginaspecifieke controlepunten: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

- health slaagt, de ingestelde origin opent en het eerste account is eigenaar

## Veiligheid en terugdraaien

:::caution
De veilige herstelgrens voor **Installeren: Eerste start en statuscontrole** vereist deze acties voordat iets start of doorgaat: logboeken met tijdstip en afgeschermde privégegevens; bijpassende back-upset. Paginaspecifieke controlepunten: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

- logboeken met tijdstip en afgeschermde privégegevens
- bijpassende back-upset
:::

## Volgende stap

[Bijwerken en migreren: Back-up](/nl/update/backup/)

## Bron en status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Bron en status: `2026-07-14`
- DiscVault Docs: `0.1.0`
