---
title: 'Problemen oplossen'
description: 'Taakspecifieke instructies voor Problemen oplossen: `docker compose` · `/api/next/health` · `postgres` · `RP_ID`.'
pageId: 'troubleshooting-index'
products: ['server', 'pwa', 'ios', 'android']
platforms: ['all']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault', 'helmerzNL/DiscVaultApp', 'Flux76HQ/DiscVault-AndroidApp']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="alle platforms" version="DiscVault v26" source="helmerzNL/DiscVault · helmerzNL/DiscVaultApp · Flux76HQ/DiscVault-AndroidApp" verified="2026-07-14" />

## Doel en resultaat

Voer de paginaspecifieke werkwijze uit voor **Problemen oplossen**. Deze werkwijze gebruikt `docker compose` · `/api/next/health` · `postgres` · `RP_ID`.

## Van toepassing op

Deze pagina is uitsluitend van toepassing op **Problemen oplossen** (alle platforms). De kanaal- en gegevensgrenzen zijn: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `/api/next/health`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `/api/next/health`. Paginaspecifieke controlepunten: `docker compose` · `/api/next/health` · `postgres` · `RP_ID`.

### DiscVault v26 stabiel

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `/api/next/health`

### DiscVault v26 bèta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `/api/next/health`

## Vereisten

- gekozen releasekanaal
- logboeken met tijdstip en afgeschermde privégegevens
- `postgres`

## Procedure

1. **Vastleggen:** `docker compose`
2. **Isoleren:** container → reverse proxy → browser/app → provider → gegevens
3. **Controleren:** logboeken met tijdstip en afgeschermde privégegevens
4. **Bewaren:** PostgreSQL `/data`

### Opdrachten en instellingen

#### Beide kanalen

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml ps
curl --fail http://localhost:6080/api/next/health
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml logs --tail=200 next-api next-worker next-mcp postgres
```

## Verwacht resultaat

Accepteer het resultaat voor **Problemen oplossen** alleen wanneer alle onderstaande resultaten kloppen: één falende laag is geïsoleerd met tijdstip, status, image-digest en geredigeerde fout. Paginaspecifieke controlepunten: `docker compose` · `/api/next/health` · `postgres` · `RP_ID`.

- één falende laag is geïsoleerd met tijdstip, status, image-digest en geredigeerde fout

## Veiligheid en terugdraaien

:::caution
De veilige herstelgrens voor **Problemen oplossen** vereist deze acties voordat iets start of doorgaat: verwijder niet eerst volumes, `/data`, browserreferenties of native appopslag; bewaar geredigeerde logs en herstel alleen een bijpassende back-up. Paginaspecifieke controlepunten: `docker compose` · `/api/next/health` · `postgres` · `RP_ID`.

- verwijder niet eerst volumes, `/data`, browserreferenties of native appopslag; bewaar geredigeerde logs en herstel alleen een bijpassende back-up
:::

## Volgende stap

[Referentie](/nl/reference/)

## Bron en status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- [`helmerzNL/DiscVaultApp@9d15ab2a339c`](https://github.com/helmerzNL/DiscVaultApp/commit/9d15ab2a339cefdb164ec0ab29ad69ddabc511c1)
- [`Flux76HQ/DiscVault-AndroidApp@a13ef4bdbb23`](https://github.com/Flux76HQ/DiscVault-AndroidApp/commit/a13ef4bdbb23b6a7e52e01cdf532fa222c1b67de)
- Bron en status: `2026-07-14`
- DiscVault Docs: `0.1.0`
