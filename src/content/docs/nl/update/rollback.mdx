---
title: 'Bijwerken en migreren: Image terugdraaien'
description: 'Taakspecifieke instructies voor Bijwerken en migreren: Image terugdraaien: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.'
pageId: 'update-rollback'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Doel en resultaat

Voer de paginaspecifieke werkwijze uit voor **Bijwerken en migreren: Image terugdraaien**. Deze werkwijze gebruikt `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

## Van toepassing op

Deze pagina is uitsluitend van toepassing op **Bijwerken en migreren: Image terugdraaien** (Docker · Unraid). De kanaal- en gegevensgrenzen zijn: DiscVault · `ghcr.io/helmerznl/discvault:latest`; herstel de bijpassende gegevens voordat de vorige image wordt gestart; PostgreSQL `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; herstel de bijpassende gegevens voordat de vorige image wordt gestart; PostgreSQL `/data`. Paginaspecifieke controlepunten: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

### DiscVault v26 stabiel

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- herstel de bijpassende gegevens voordat de vorige image wordt gestart
- PostgreSQL `/data`

### DiscVault v26 bèta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- herstel de bijpassende gegevens voordat de vorige image wordt gestart
- PostgreSQL `/data`

## Vereisten

- bijpassende back-upset
- `PREVIOUS_IMAGE`

## Procedure

1. **Stoppen:** `next-api` `next-worker` `next-mcp`
2. **Herstellen:** `/data`
3. **Herstellen:** PostgreSQL · `pg_restore --exit-on-error`
4. **Starten:** `PREVIOUS_IMAGE` → API → MCP

### Opdrachten en instellingen

#### Beide kanalen

```bash
set -euo pipefail
BACKUP_ID=20260714T180000Z
cd /opt/discvault
. "/srv/backups/discvault-$BACKUP_ID.env"
test -n "$PREVIOUS_IMAGE"
export DISCVAULT_IMAGE="$PREVIOUS_IMAGE"
docker pull "$DISCVAULT_IMAGE"
ENV_FILE=/opt/discvault/.env
ENV_TMP="$(mktemp "$ENV_FILE.restore.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
awk -v image="$DISCVAULT_IMAGE" '
  /^DISCVAULT_IMAGE=/ { print "DISCVAULT_IMAGE=" image; found=1; next }
  { print }
  END { if (!found) exit 42 }
' "$ENV_FILE" > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
mv /srv/discvault "/srv/discvault.failed-$(date -u +%Y%m%dT%H%M%SZ)"
tar --acls --xattrs -C /srv -xzf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz"
docker compose --env-file .env -p discvault -f compose.yaml up -d postgres
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'dropdb --if-exists --force -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'createdb -U "$POSTGRES_USER" "$POSTGRES_DB"'
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_restore --exit-on-error --no-owner -U "$POSTGRES_USER" -d "$POSTGRES_DB"' \
  < "/srv/backups/discvault-$BACKUP_ID.dump"
docker compose --env-file .env -p discvault -f compose.yaml up -d --force-recreate next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml \
  up -d --force-recreate next-worker next-mcp
docker compose --env-file .env -p discvault -f compose.yaml ps
mv "$ENV_TMP" "$ENV_FILE"
trap - EXIT
```

## Verwacht resultaat

Accepteer het resultaat voor **Bijwerken en migreren: Image terugdraaien** alleen wanneer alle onderstaande resultaten kloppen: `PREVIOUS_IMAGE` · `@sha256:`; herstel de bijpassende gegevens voordat de vorige image wordt gestart; bibliotheekaantallen en mediavoorbeeld komen overeen met de gekozen back-up. Paginaspecifieke controlepunten: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

- `PREVIOUS_IMAGE` · `@sha256:`
- herstel de bijpassende gegevens voordat de vorige image wordt gestart
- bibliotheekaantallen en mediavoorbeeld komen overeen met de gekozen back-up

## Veiligheid en terugdraaien

:::caution
De veilige herstelgrens voor **Bijwerken en migreren: Image terugdraaien** vereist deze acties voordat iets start of doorgaat: houd bij een mislukte bestandsextractie of pg_restore de container, API, workers en MCP gestopt; probeer opnieuw vanaf onaangeroerde back-upkopieën. Paginaspecifieke controlepunten: `PREVIOUS_IMAGE` · `pg_restore --exit-on-error` · `discvault.failed-` · `DISCVAULT_IMAGE`.

- houd bij een mislukte bestandsextractie of pg_restore de container, API, workers en MCP gestopt; probeer opnieuw vanaf onaangeroerde back-upkopieën
:::

## Volgende stap

[Bijwerken en migreren: Migratie naar DiscVault 26](/nl/update/migration/)

## Bron en status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Bron en status: `2026-07-14`
- DiscVault Docs: `0.1.0`
