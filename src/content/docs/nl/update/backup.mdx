---
title: 'Bijwerken en migreren: Back-up'
description: 'Taakspecifieke instructies voor Bijwerken en migreren: Back-up: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.'
pageId: 'update-backup'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Doel en resultaat

Voer de paginaspecifieke werkwijze uit voor **Bijwerken en migreren: Back-up**. Deze werkwijze gebruikt `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

## Van toepassing op

Deze pagina is uitsluitend van toepassing op **Bijwerken en migreren: Back-up** (Docker · Unraid). De kanaal- en gegevensgrenzen zijn: DiscVault · `ghcr.io/helmerznl/discvault:latest`; PostgreSQL-dump en bestandsarchief uit hetzelfde schrijversstopvenster; PostgreSQL `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; PostgreSQL-dump en bestandsarchief uit hetzelfde schrijversstopvenster; PostgreSQL `/data`. Paginaspecifieke controlepunten: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

### DiscVault v26 stabiel

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- PostgreSQL-dump en bestandsarchief uit hetzelfde schrijversstopvenster
- PostgreSQL `/data`

### DiscVault v26 bèta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- PostgreSQL-dump en bestandsarchief uit hetzelfde schrijversstopvenster
- PostgreSQL `/data`

## Vereisten

- back-updoel buiten het actieve gegevenspad
- schrijfbare persistente opslag en vrije back-upruimte

## Procedure

1. **Vastleggen:** `PREVIOUS_IMAGE` · `sha256`
2. **Stoppen:** `next-api` `next-worker` `next-mcp`
3. **Back-up maken:** `pg_dump -Fc`
4. **Starten:** `next-api` → → MCP

### Opdrachten en instellingen

#### Beide kanalen

```bash
set -euo pipefail
cd /opt/discvault
install -d -m 0700 /srv/backups
BACKUP_ID="$(date -u +%Y%m%dT%H%M%SZ)"
API_ID="$(docker compose --env-file .env -p discvault -f compose.yaml ps -q next-api)"
IMAGE_ID="$(docker inspect "$API_ID" --format '{{.Image}}')"
PREVIOUS_IMAGE="$(docker image inspect "$IMAGE_ID" --format '{{index .RepoDigests 0}}')"
test -n "$PREVIOUS_IMAGE"
printf 'BACKUP_ID=%s\nPREVIOUS_IMAGE=%s\n' "$BACKUP_ID" "$PREVIOUS_IMAGE" \
  > "/srv/backups/discvault-$BACKUP_ID.env"
chmod 0600 "/srv/backups/discvault-$BACKUP_ID.env"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_dump -Fc -U "$POSTGRES_USER" "$POSTGRES_DB"' \
  > "/srv/backups/discvault-$BACKUP_ID.dump"
tar --acls --xattrs -C /srv -czf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz" discvault
docker compose --env-file .env -p discvault -f compose.yaml start next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml start next-worker next-mcp
test -s "/srv/backups/discvault-$BACKUP_ID.dump"
gzip -t "/srv/backups/discvault-data-$BACKUP_ID.tgz"
```

## Verwacht resultaat

Accepteer het resultaat voor **Bijwerken en migreren: Back-up** alleen wanneer alle onderstaande resultaten kloppen: archief en dump zijn niet leeg, leesbaar in een hersteloefening en gekoppeld aan een onveranderlijke image-digest. Paginaspecifieke controlepunten: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- archief en dump zijn niet leeg, leesbaar in een hersteloefening en gekoppeld aan een onveranderlijke image-digest

## Veiligheid en terugdraaien

:::caution
De veilige herstelgrens voor **Bijwerken en migreren: Back-up** vereist deze acties voordat iets start of doorgaat: als een back-upopdracht mislukt, start je alleen de ongewijzigde services opnieuw en annuleer je de update; PostgreSQL-dump en bestandsarchief uit hetzelfde schrijversstopvenster. Paginaspecifieke controlepunten: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- als een back-upopdracht mislukt, start je alleen de ongewijzigde services opnieuw en annuleer je de update
- PostgreSQL-dump en bestandsarchief uit hetzelfde schrijversstopvenster
:::

## Volgende stap

[Bijwerken en migreren: Herstellen](/nl/update/restore/)

## Bron en status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Bron en status: `2026-07-14`
- DiscVault Docs: `0.1.0`
