---
title: 'Configureren: Omgevingsvariabelen'
description: 'Taakspecifieke instructies voor Configureren: Omgevingsvariabelen: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.'
pageId: 'configure-environment'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Doel en resultaat

Voer de paginaspecifieke werkwijze uit voor **Configureren: Omgevingsvariabelen**. Deze werkwijze gebruikt `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

## Van toepassing op

Deze pagina is uitsluitend van toepassing op **Configureren: Omgevingsvariabelen** (Docker · Unraid). De kanaal- en gegevensgrenzen zijn: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `.env`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `.env`. Paginaspecifieke controlepunten: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

### DiscVault v26 stabiel

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `.env`

### DiscVault v26 bèta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `.env`

## Vereisten

- privé DiscVault-runtimebestand
- OpenSSL
- gepland herstartvenster

## Procedure

1. **Maken:** `/opt/discvault/.env` · bestandsrechten alleen voor eigenaar (0600)
2. **Configureren:** `DISCVAULT_IMAGE` · `RP_ORIGINS`
3. **Verifiëren:** `chmod 0600` · `docker compose config --quiet`
4. **Starten:** houd JWT- en PostgreSQL-geheimen ongewijzigd bij gewone configuratiewijzigingen

### Opdrachten en instellingen

#### DiscVault v26 stabiel

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:latest
```

#### DiscVault v26 bèta

```bash
export DISCVAULT_IMAGE=ghcr.io/helmerznl/discvault:beta
```

#### Beide kanalen

```bash
set -euo pipefail
test -n "$DISCVAULT_IMAGE"
case "$DISCVAULT_IMAGE" in
  ghcr.io/helmerznl/discvault:latest|ghcr.io/helmerznl/discvault:beta) ;;
  *) printf '%s\n' "$DISCVAULT_IMAGE" >&2; exit 64 ;;
esac
install -d -m 0700 /opt/discvault
install -d -m 0750 /srv/discvault
install -d -o 70 -g 70 -m 0700 /srv/discvault-postgres
cd /opt/discvault
test -f compose.yaml
umask 077
ENV_FILE=/opt/discvault/.env
test ! -e "$ENV_FILE"
ENV_TMP="$(mktemp "$ENV_FILE.tmp.XXXXXX")"
trap 'rm -f "$ENV_TMP"' EXIT
POSTGRES_PASSWORD="$(openssl rand -hex 32)"
JWT_SECRET="$(openssl rand -hex 32)"
printf '%s\n' \
  "DISCVAULT_IMAGE=$DISCVAULT_IMAGE" \
  'DISCVAULT_DATA_DIR=/srv/discvault' \
  'DISCVAULT_POSTGRES_DATA=/srv/discvault-postgres' \
  'DISCVAULT_API_PORT=6080' \
  'DISCVAULT_MCP_PORT=6090' \
  'DISCVAULT_NETWORK_NAME=discvault' \
  'DISCVAULT_API_WORKERS=2' \
  'DISCVAULT_API_TIMEOUT=180' \
  'DISCVAULT_WORKER_ID=worker-1' \
  'DISCVAULT_WORKER_POLL_INTERVAL=2' \
  'POSTGRES_DB=discvault' \
  'POSTGRES_USER=discvault' \
  "POSTGRES_PASSWORD=$POSTGRES_PASSWORD" \
  "JWT_SECRET=$JWT_SECRET" \
  'TZ=Europe/Amsterdam' \
  'RP_ID=localhost' \
  'RP_NAME=DiscVault' \
  'RP_ORIGINS=http://localhost:6080' > "$ENV_TMP"
chmod 0600 "$ENV_TMP"
ln "$ENV_TMP" "$ENV_FILE"
rm -f "$ENV_TMP"
trap - EXIT
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml config --quiet
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml pull
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml up -d
docker compose --env-file "$ENV_FILE" -p discvault -f compose.yaml ps
curl --fail http://localhost:6080/api/next/health
```

## Verwacht resultaat

Accepteer het resultaat voor **Configureren: Omgevingsvariabelen** alleen wanneer alle onderstaande resultaten kloppen: bestandsrechten alleen voor eigenaar (0600); het privébestand heeft modus 600, Compose-validatie slaagt en het gekozen kanaal wordt gezond. Paginaspecifieke controlepunten: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

- bestandsrechten alleen voor eigenaar (0600)
- het privébestand heeft modus 600, Compose-validatie slaagt en het gekozen kanaal wordt gezond

## Veiligheid en terugdraaien

:::caution
De veilige herstelgrens voor **Configureren: Omgevingsvariabelen** vereist deze acties voordat iets start of doorgaat: het omgevingsbestand van de docs-repository is nooit een DiscVault-runtimebestand · `.env.example`; houd JWT- en PostgreSQL-geheimen ongewijzigd bij gewone configuratiewijzigingen · `JWT_SECRET` · `POSTGRES_PASSWORD`; houd services bij een fout gestopt, herstel en valideer het vorige privé-omgevingsbestand en maak daarna dezelfde image opnieuw zonder geheimen te roteren. Paginaspecifieke controlepunten: `/opt/discvault/.env` · `DISCVAULT_IMAGE` · `chmod 0600` · `RP_ORIGINS`.

- het omgevingsbestand van de docs-repository is nooit een DiscVault-runtimebestand · `.env.example`
- houd JWT- en PostgreSQL-geheimen ongewijzigd bij gewone configuratiewijzigingen · `JWT_SECRET` · `POSTGRES_PASSWORD`
- houd services bij een fout gestopt, herstel en valideer het vorige privé-omgevingsbestand en maak daarna dezelfde image opnieuw zonder geheimen te roteren
:::

## Volgende stap

[Configureren: Authenticatie en RBAC](/nl/configure/auth-rbac/)

## Bron en status

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Bron en status: `2026-07-14`
- DiscVault Docs: `0.1.0`
