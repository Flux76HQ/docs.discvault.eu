---
title: 'Frissítés és migrálás: Biztonsági mentés'
description: 'Feladatspecifikus útmutató: Frissítés és migrálás: Biztonsági mentés: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.'
pageId: 'update-backup'
products: ['server']
platforms: ['docker', 'unraid']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · Unraid" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Cél és eredmény

Hajtsa végre az oldalhoz tartozó munkafolyamatot: **Frissítés és migrálás: Biztonsági mentés**. Ez az eljárás ezeket használja: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

## Érvényesség

Ez az oldal kizárólag erre vonatkozik: **Frissítés és migrálás: Biztonsági mentés** (Docker · Unraid). A csatorna- és adathatárok: DiscVault · `ghcr.io/helmerznl/discvault:latest`; PostgreSQL dump és fájlarchívum ugyanabból a leállított írási ablakból; PostgreSQL `/data`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; PostgreSQL dump és fájlarchívum ugyanabból a leállított írási ablakból; PostgreSQL `/data`. Oldalspecifikus ellenőrzési pontok: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- PostgreSQL dump és fájlarchívum ugyanabból a leállított írási ablakból
- PostgreSQL `/data`

### DiscVault v26 – béta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- PostgreSQL dump és fájlarchívum ugyanabból a leállított írási ablakból
- PostgreSQL `/data`

## Előfeltételek

- mentési cél az élő adatútvonalon kívül
- írható tartós tárhely és szabad mentési hely

## Eljárás

1. **Jegyezze fel:** `PREVIOUS_IMAGE` · `sha256`
2. **Állítsa le:** `next-api` `next-worker` `next-mcp`
3. **Készítsen mentést:** `pg_dump -Fc`
4. **Indítsa el:** `next-api` → → MCP

### Parancsok és beállítások

#### Mindkét csatorna

```bash
set -euo pipefail
cd /opt/discvault
install -d -m 0700 /srv/backups
BACKUP_ID="$(date -u +%Y%m%dT%H%M%SZ)"
API_ID="$(docker compose --env-file .env -p discvault -f compose.yaml ps -q next-api)"
IMAGE_ID="$(docker inspect "$API_ID" --format '{{.Image}}')"
PREVIOUS_IMAGE="$(docker image inspect "$IMAGE_ID" --format '{{index .RepoDigests 0}}')"
test -n "$PREVIOUS_IMAGE"
printf 'BACKUP_ID=%s\nPREVIOUS_IMAGE=%s\n' "$BACKUP_ID" "$PREVIOUS_IMAGE" \
  > "/srv/backups/discvault-$BACKUP_ID.env"
chmod 0600 "/srv/backups/discvault-$BACKUP_ID.env"
docker compose --env-file .env -p discvault -f compose.yaml stop next-worker next-mcp next-api
docker compose --env-file .env -p discvault -f compose.yaml exec -T postgres \
  sh -c 'pg_dump -Fc -U "$POSTGRES_USER" "$POSTGRES_DB"' \
  > "/srv/backups/discvault-$BACKUP_ID.dump"
tar --acls --xattrs -C /srv -czf \
  "/srv/backups/discvault-data-$BACKUP_ID.tgz" discvault
docker compose --env-file .env -p discvault -f compose.yaml start next-api
curl --fail http://localhost:6080/api/next/health
docker compose --env-file .env -p discvault -f compose.yaml start next-worker next-mcp
test -s "/srv/backups/discvault-$BACKUP_ID.dump"
gzip -t "/srv/backups/discvault-data-$BACKUP_ID.tgz"
```

## Várt eredmény

Az eredmény csak ennél fogadható el: **Frissítés és migrálás: Biztonsági mentés** csak akkor, ha minden alábbi eredmény teljesül: az archívum és dump nem üres, próba-visszaállításkor olvasható, és változtathatatlan image-digesthez kötött. Oldalspecifikus ellenőrzési pontok: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- az archívum és dump nem üres, próba-visszaállításkor olvasható, és változtathatatlan image-digesthez kötött

## Biztonság és visszaállítás

:::caution
A biztonságos helyreállítás határa ennél: **Frissítés és migrálás: Biztonsági mentés** ezeket a műveleteket igényli indítás vagy folytatás előtt: ha bármely mentési parancs hibázik, csak a változatlan szolgáltatásokat indítsa újra, és szakítsa meg a frissítést; PostgreSQL dump és fájlarchívum ugyanabból a leállított írási ablakból. Oldalspecifikus ellenőrzési pontok: `pg_dump -Fc` · `DISCVAULT_DATA_DIR` · `PREVIOUS_IMAGE` · `next-api`.

- ha bármely mentési parancs hibázik, csak a változatlan szolgáltatásokat indítsa újra, és szakítsa meg a frissítést
- PostgreSQL dump és fájlarchívum ugyanabból a leállított írási ablakból
:::

## Következő lépés

[Frissítés és migrálás: Visszaállítás](/hu/update/restore/)

## Forrás és állapot

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Forrás és állapot: `2026-07-14`
- DiscVault Docs: `0.1.0`
