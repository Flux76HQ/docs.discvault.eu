---
title: 'Konfigurálás: Hitelesítés és RBAC'
description: 'Feladatspecifikus útmutató: Konfigurálás: Hitelesítés és RBAC. /api/next/auth/status. /api/next/auth/rbac. WebAuthn. 48 h. iOS/iPadOS 16+. Android 9+. OIDC. Argon2id. TOTP.'
pageId: 'configure-auth-rbac'
products: ['server']
platforms: ['web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault', 'helmerzNL/DiscVault.EU', 'Flux76HQ/App-Guidance']
lastVerified: '2026-09-20'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="web" version="DiscVault v26" source="helmerzNL/DiscVault · helmerzNL/DiscVault.EU · Flux76HQ/App-Guidance" verified="2026-09-20" />

## Cél és eredmény

Hajtsa végre az oldalhoz tartozó munkafolyamatot: **Konfigurálás: Hitelesítés és RBAC**. Ez az eljárás ezeket használja: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h` · iOS/iPadOS `16+` · Android `9+` · OIDC · Argon2id · TOTP.

## Érvényesség

Ez az oldal kizárólag erre vonatkozik: **Konfigurálás: Hitelesítés és RBAC** (web). A csatorna- és adathatárok: DiscVault · `ghcr.io/helmerznl/discvault:latest`; WebAuthn; a passkey egy kriptográfiai kulcspár: a privát kulcs az eszközön vagy megbízható hitelesítőadat-kezelőben marad, a DiscVault csak a nyilvános kulcsot tárolja; DiscVault · `ghcr.io/helmerznl/discvault:beta`; WebAuthn OIDC; TOTP. Oldalspecifikus ellenőrzési pontok: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h` · iOS/iPadOS `16+` · Android `9+` · OIDC · Argon2id · TOTP.

### DiscVault v26 – stabil

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- WebAuthn
- a passkey egy kriptográfiai kulcspár: a privát kulcs az eszközön vagy megbízható hitelesítőadat-kezelőben marad, a DiscVault csak a nyilvános kulcsot tárolja

### DiscVault v26 – béta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- WebAuthn OIDC
- TOTP

## Előfeltételek

- tulajdonosi hozzáférés
- [Telepítés: Fordított proxy és hozzáférési kulcsok](/hu/install/reverse-proxy-passkeys/) · HTTPS FQDN
- második helyreállítási mód

## Eljárás

1. **Ellenőrizze:** a passkey egy kriptográfiai kulcspár: a privát kulcs az eszközön vagy megbízható hitelesítőadat-kezelőben marad, a DiscVault csak a nyilvános kulcsot tárolja
2. **Hasonlítsa össze:** a passkey adathalászat-ellenálló és webhelyenként egyedi; a bejelentkezés újrahasználható jelszó helyett az eszköz feloldását használja · az időkorlátos meghívókód elindítja a regisztrációt, de nem újrahasználható fiókjelszó; a normál bejelentkezés passkey használatával történik
3. **Ellenőrizze:** Windows rendszeren aktuális passkey-képes böngésző és legalább PIN-kóddal beállított Windows Hello szükséges; a natív kezeléshez Windows 11 22H2 vagy újabb ajánlott
4. **Ellenőrizze:** a macOS Ventura 13 vagy újabb rendszert és iCloud-kulcskarikát, illetve más passkey-képes hitelesítőadat-kezelőt igényel
5. **Ellenőrizze:** az iPhone és iPad iOS vagy iPadOS 16+ rendszert, iCloud-kulcskarikát és kétlépcsős hitelesítést igényel; az Android 9+ rendszert, passkey-szolgáltatót és képernyőzárat igényel
6. **Hozza létre:** regisztráljon több passkey-t, például telefonon és asztali eszközön, és az eszköz cseréje vagy elvesztése előtt tartson fenn független tulajdonosi helyreállítási utat
7. **Nyissa meg:** Adminisztráció → Biztonság → Hitelesítés engedélyezése
8. **Hozza létre:** Adminisztráció → Felhasználók → 48 órás meghívó létrehozása
9. **Állítsa be:** Adminisztráció → Szerepkörök → Alap / Speciális
10. **Állítsa be:** meglévő telepítésen engedélyezze a jelszavas belépést a Felhasználók és szerepkörök alatt, majd friss tulajdonosi vagy admin passkey-ellenőrzéssel hagyja jóvá · tulajdonosok és adminok ideiglenes jelszót adhatnak, cserét írhatnak elő, felhasználói MFA-házirendet állíthatnak és szabályozhatják a passkey-regisztrációt
11. **Hasonlítsa össze:** [Konfigurálás: Bejelentkezés OpenID Connecttel (OIDC)](/hu/configure/oidc/) · Az OIDC választható böngészős bejelentkezést ad; a helyi passkey-k, választható jelszavak, helyreállítási kódok, API-tokenek és MCP bearer tokenek megtartják szerepüket
12. **Jegyezze fel:** a helyreállító kódok hash-elt, egyszer használatos és a passkey-helyreállítással közös kódok; a mentésből kimarad a TOTP-titok és helyreállítási anyag, ezért az MFA-t újra be kell állítani · a Legacy Authentication kikapcsolásához aktív tulajdonosi passkey kell; csak helyi IP-t használó tulajdonosnak előbb érvényes FQDN-t és passkey-t kell létrehoznia
13. **Tesztelje:** `/api/next/auth/rbac`

### Parancsok és beállítások

#### DiscVault v26 – stabil

```bash
curl --fail http://localhost:6080/api/next/auth/status
curl --fail http://localhost:6080/api/next/auth/rbac
```

#### DiscVault v26 – béta

```bash
curl --fail http://localhost:6180/api/next/auth/status
curl --fail http://localhost:6180/api/next/auth/rbac
```

## Várt eredmény

Az eredmény csak ennél fogadható el: **Konfigurálás: Hitelesítés és RBAC** csak akkor, ha minden alábbi eredmény teljesül: a tulajdonosi passkey-belépés működik, a meghívásos regisztráció követi a beállítást, és a tesztfelhasználó csak kiosztott jogokat kap; a passkey adathalászat-ellenálló és webhelyenként egyedi; a bejelentkezés újrahasználható jelszó helyett az eszköz feloldását használja; az időkorlátos meghívókód elindítja a regisztrációt, de nem újrahasználható fiókjelszó; a normál bejelentkezés passkey használatával történik. Oldalspecifikus ellenőrzési pontok: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h` · iOS/iPadOS `16+` · Android `9+` · OIDC · Argon2id · TOTP.

- a tulajdonosi passkey-belépés működik, a meghívásos regisztráció követi a beállítást, és a tesztfelhasználó csak kiosztott jogokat kap
- a passkey adathalászat-ellenálló és webhelyenként egyedi; a bejelentkezés újrahasználható jelszó helyett az eszköz feloldását használja
- az időkorlátos meghívókód elindítja a regisztrációt, de nem újrahasználható fiókjelszó; a normál bejelentkezés passkey használatával történik

## Biztonság és visszaállítás

:::caution
A biztonságos helyreállítás határa ennél: **Konfigurálás: Hitelesítés és RBAC** ezeket a műveleteket igényli indítás vagy folytatás előtt: regisztráljon több passkey-t, például telefonon és asztali eszközön, és az eszköz cseréje vagy elvesztése előtt tartson fenn független tulajdonosi helyreállítási utat; a jelszó legalább 15 karakteres, nem lehet a felhasználónév vagy tiltott gyakori jelszó, és Argon2id hash védi; 15 percen belül öt hiba 15 perces zárolást okoz; a helyreállító kódok hash-elt, egyszer használatos és a passkey-helyreállítással közös kódok; a mentésből kimarad a TOTP-titok és helyreállítási anyag, ezért az MFA-t újra be kell állítani; a Legacy Authentication kikapcsolásához aktív tulajdonosi passkey kell; csak helyi IP-t használó tulajdonosnak előbb érvényes FQDN-t és passkey-t kell létrehoznia; az identitás az issuer és subject pontos párja; az e-mail soha nem kapcsolódik automatikusan, a kapcsoláshoz bejelentkezett böngészős munkamenet, a leválasztáshoz pedig másik használható bejelentkezési mód kell; tartsa meg a tulajdonosi munkamenetet és egy második helyreállítási módot; az utolsó tulajdonosi passkey soha ne törlődjön. Oldalspecifikus ellenőrzési pontok: `/api/next/auth/status` · `/api/next/auth/rbac` · WebAuthn · `48 h` · iOS/iPadOS `16+` · Android `9+` · OIDC · Argon2id · TOTP.

- regisztráljon több passkey-t, például telefonon és asztali eszközön, és az eszköz cseréje vagy elvesztése előtt tartson fenn független tulajdonosi helyreállítási utat
- a jelszó legalább 15 karakteres, nem lehet a felhasználónév vagy tiltott gyakori jelszó, és Argon2id hash védi; 15 percen belül öt hiba 15 perces zárolást okoz
- a helyreállító kódok hash-elt, egyszer használatos és a passkey-helyreállítással közös kódok; a mentésből kimarad a TOTP-titok és helyreállítási anyag, ezért az MFA-t újra be kell állítani
- a Legacy Authentication kikapcsolásához aktív tulajdonosi passkey kell; csak helyi IP-t használó tulajdonosnak előbb érvényes FQDN-t és passkey-t kell létrehoznia
- az identitás az issuer és subject pontos párja; az e-mail soha nem kapcsolódik automatikusan, a kapcsoláshoz bejelentkezett böngészős munkamenet, a leválasztáshoz pedig másik használható bejelentkezési mód kell
- tartsa meg a tulajdonosi munkamenetet és egy második helyreállítási módot; az utolsó tulajdonosi passkey soha ne törlődjön
:::

## Következő lépés

[Konfigurálás: Bejelentkezés OpenID Connecttel (OIDC)](/hu/configure/oidc/)

## Forrás és állapot

- [`helmerzNL/DiscVault@409da7b2dc25`](https://github.com/helmerzNL/DiscVault/commit/409da7b2dc25bbf621cd0a6682136d565cdf5070)
- [`helmerzNL/DiscVault.EU@583f85c55dc2`](https://github.com/helmerzNL/DiscVault.EU/commit/583f85c55dc22b63368f997a3b076a093ca0afa1)
- [`Flux76HQ/App-Guidance@41bfca3bfa39`](https://github.com/Flux76HQ/App-Guidance/commit/41bfca3bfa39f0b43924190d384e2d96be756020)
- [Moving beta source: OIDC deployment notes](https://github.com/helmerzNL/DiscVault/blob/release/v26-beta/app/deploy/next/README.md)
- [Pinned verification: OIDC environment template](https://github.com/helmerzNL/DiscVault/blob/409da7b2dc25bbf621cd0a6682136d565cdf5070/app/deploy/next/.env.example)
- [Pinned verification: OIDC implementation](https://github.com/helmerzNL/DiscVault/blob/409da7b2dc25bbf621cd0a6682136d565cdf5070/app/backend/next_oidc.py)
- [Authoritative OIDC authentication contract](https://github.com/Flux76HQ/App-Guidance/blob/41bfca3bfa39f0b43924190d384e2d96be756020/projects/discvault/specs/oidc-authentication.md)
- Forrás és állapot: `2026-09-20`
- DiscVault Docs: `0.1.14`
