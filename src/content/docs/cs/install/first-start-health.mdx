---
title: 'Instalace: První spuštění a kontrola stavu'
description: 'Pokyny pro konkrétní úlohu: Instalace: První spuštění a kontrola stavu: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.'
pageId: 'install-first-start-health'
products: ['server']
platforms: ['docker', 'web']
channels: ['stable', 'beta']
minVersion: 'DiscVault v26'
sourceRepos: ['helmerzNL/DiscVault']
lastVerified: '2026-07-14'
---

import PageMeta from '../../../../components/PageMeta.astro';

<PageMeta channels="stable,beta" platform="Docker · web" version="DiscVault v26" source="helmerzNL/DiscVault" verified="2026-07-14" />

## Cíl a výsledek

Dokončete postup určený pro **Instalace: První spuštění a kontrola stavu**. Tento postup pracuje s `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

## Platí pro

Tato stránka platí pouze pro **Instalace: První spuštění a kontrola stavu** (Docker · web). Hranice kanálu a dat jsou: DiscVault · `ghcr.io/helmerznl/discvault:latest`; `/api/next/auth/status`; `6080` → API `5000`; DiscVault · `ghcr.io/helmerznl/discvault:beta`; `/api/next/auth/status`; `6080` → API `5000`. Kontrolní body této stránky: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

### DiscVault v26 – stabilní

- DiscVault · `ghcr.io/helmerznl/discvault:latest`
- `/api/next/auth/status`
- `6080` → API `5000`

### DiscVault v26 – beta

- DiscVault · `ghcr.io/helmerznl/discvault:beta`
- `/api/next/auth/status`
- `6080` → API `5000`

## Požadavky

- `/api/next/health`
- `/api/next/auth/status`

## Postup

1. **Zkontrolujte:** `postgres`
2. **Ověřte:** `GET /api/next/health` · HTTP `2xx`
3. **Otevřete:** `6080:5000`
4. **Ověřte:** `GET /api/next/auth/status` · WebAuthn

### Příkazy a nastavení

#### Oba kanály

```bash
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml ps
docker compose --env-file /opt/discvault/.env -p discvault -f /opt/discvault/compose.yaml logs --tail=100 next-api next-worker next-mcp postgres
curl --fail http://localhost:6080/api/next/health
curl --fail http://localhost:6080/api/next/auth/status
```

## Očekávaný výsledek

Výsledek přijměte pro **Instalace: První spuštění a kontrola stavu** pouze pokud platí všechny následující výsledky: kontrola stavu projde, nastavený origin se otevře a první účet je vlastníkem. Kontrolní body této stránky: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

- kontrola stavu projde, nastavený origin se otevře a první účet je vlastníkem

## Bezpečnost a návrat

:::caution
Bezpečná hranice obnovy pro **Instalace: První spuštění a kontrola stavu** vyžaduje před spuštěním nebo pokračováním tyto kroky: protokoly s časem a skrytými soukromými údaji; odpovídající sada záloh. Kontrolní body této stránky: `/api/next/health` · `/api/next/auth/status` · `6080:5000` · WebAuthn.

- protokoly s časem a skrytými soukromými údaji
- odpovídající sada záloh
:::

## Další krok

[Aktualizace a migrace: Záloha](/cs/update/backup/)

## Zdroj a stav

- [`helmerzNL/DiscVault@6d27c689ac21`](https://github.com/helmerzNL/DiscVault/commit/6d27c689ac2166651d2c7c74833c1ee225b37ec3)
- Zdroj a stav: `2026-07-14`
- DiscVault Docs: `0.1.0`
